# Plan 29-19: Frontend VehicleDashboardView + 11 widgets + SVG canister + pulse-glow

**Phase:** 29 (Vehicle Fleet)
**Wave:** 4
**Depends on:** 29-10 (backend dashboard endpoints), 29-14 (route+stub)
**Estimated complexity:** L (~900 LOC = view 400 + widgets 500)
**Decisions covered:** D-16

## Goal
Полная замена stub VehicleDashboardView из 29-14: grid-layout-plus draggable layout с 11 виджетами (4 KPI + 6 chart/list/canister + 1 table). Custom SVG canister с pulse animation. Pulse-glow CSS для warning cards. localStorage persistence per-user. Reuse `useDashboardLayout` composable как basis.

## Files

### Replace
- `c:\Users\1\Desktop\Cursor\VSKS_CRM\frontend\src\views\property\VehicleDashboardView.vue`

### Create
- `c:\Users\1\Desktop\Cursor\VSKS_CRM\frontend\src\composables\useVehicleDashboardLayout.ts` — копия `useDashboardLayout.ts` с другим storage key
- `c:\Users\1\Desktop\Cursor\VSKS_CRM\frontend\src\components\vehicles\FuelCanisterWidget.vue` — кастомный SVG (R-2)
- `c:\Users\1\Desktop\Cursor\VSKS_CRM\frontend\src\components\vehicles\MaintenanceWarningWidget.vue` — list с pulse-glow (R-2, R-12)
- `c:\Users\1\Desktop\Cursor\VSKS_CRM\frontend\src\components\vehicles\VehiclesInRepairWidget.vue` — список с фото + status
- `c:\Users\1\Desktop\Cursor\VSKS_CRM\frontend\src\components\vehicles\VehicleKpiCard.vue` — переиспользуемый KPI tile (4× в layout)

## Tasks

**Task 1: useVehicleDashboardLayout.ts (~80 LOC)** — копия `useDashboardLayout.ts`. Подменить:
- storage key: `` `vehicle_dashboard_layout_u${userId}` `` (R-7)
- `DEFAULT_LAYOUT` — массив из R-7:
  ```typescript
  const DEFAULT_LAYOUT: LayoutItem[] = [
    { i: 'kpi-vehicles',     x: 0,  y: 0,  w: 3, h: 4, minW: 2, minH: 3 },
    { i: 'kpi-fuel',         x: 3,  y: 0,  w: 3, h: 4, minW: 2, minH: 3 },
    { i: 'kpi-repairs',      x: 6,  y: 0,  w: 3, h: 4, minW: 2, minH: 3 },
    { i: 'kpi-mileage',      x: 9,  y: 0,  w: 3, h: 4, minW: 2, minH: 3 },
    { i: 'canister',         x: 0,  y: 4,  w: 3, h: 10, minW: 2, minH: 8 },
    { i: 'in_repair',        x: 3,  y: 4,  w: 4, h: 10, minW: 3, minH: 6 },
    { i: 'to_warning',       x: 7,  y: 4,  w: 5, h: 10, minW: 3, minH: 6 },
    { i: 'bar_org',          x: 0,  y: 14, w: 6, h: 9,  minW: 4, minH: 6 },
    { i: 'donut_state',      x: 6,  y: 14, w: 3, h: 9,  minW: 2, minH: 6 },
    { i: 'line_fuel',        x: 9,  y: 14, w: 3, h: 9,  minW: 2, minH: 6 },
    { i: 'top10_table',      x: 0,  y: 23, w: 12, h: 10, minW: 6, minH: 6 },
  ]
  ```
- `colNum=12`, `rowHeight=30` (как в DashboardView).

**Task 2: VehicleKpiCard.vue (~80 LOC)** — props: `title`, `value`, `unit`, `icon`, `color`, `trendPct?`. Card с большой цифрой + label + опционально стрелка trendPct.

**Task 3: FuelCanisterWidget.vue (~150 LOC)** — точно из R-2:
- Props: `spentL: number`, `spentAmount: number`, `fuelBudget: number`, `level: number` (0..1), `periodLabel: string`
- Template: SVG 100×140 с rect canister outline + rect liquid с clipPath по wave path (computed). Label в центре: `{spentAmount} ₽` и `{Math.round(level*100)}%`. Под SVG: «Бюджет: {fuelBudget} ₽».
- Style: `@keyframes wave-sway` 2.5s + динамические цвета (red < 0.15, orange < 0.3, blue otherwise) per R-2.
- Watch level → если NaN/<0 → 0; >1 → 1 (Lesson 2026-04-19 clampScore — для SVG нельзя negative).

**Task 4: VehiclesInRepairWidget.vue (~120 LOC)** — `GET /api/vehicles-dashboard/in-repair`. Список карточек: photo (через `/api/vehicle-attachments/{photo_attachment_id}/download` как `<img src=...>` с auth-header через blob URL), brand+model+plate, status chip ('planned'/'in_progress' colored), repair_description, mileage_at_repair, expected_finish_date (если не NULL). Click card → router.push detail.

**Task 5: MaintenanceWarningWidget.vue (~150 LOC)** — `GET /api/vehicles-dashboard/maintenance-warning`. Список карточек с **pulse-glow** CSS:
```css
@keyframes pulse-warn { 0%,100% { box-shadow: 0 0 0 0 rgba(239,68,68,0); } 50% { box-shadow: 0 0 12px 4px rgba(239,68,68,0.35), 0 0 24px 8px rgba(239,68,68,0.15); } }
.vehicle-warning-card { animation: pulse-warn 1.8s ease-in-out infinite; }
.vehicle-warning-card.urgent { animation-duration: 1.2s; box-shadow-color: #ef4444; }
.vehicle-warning-card.expired { animation: none; border: 2px solid #ef4444; }
.vehicle-warning-card:hover { animation-play-state: paused; }
```
Intensity per R-12: `<500 km` или `<7 days` → `.urgent`; `0 km` или `expired` → `.expired` (no anim). Каждая карточка: brand+plate + warning_type label («ОСАГО до X», «ТО через Y км», «ВУ {driver}», «Медсправка {driver}») + days_left/km_left.

**Task 6: VehicleDashboardView.vue (~400 LOC)**:
- Top: filter bar (period: day/week/month/year + custom range, owner_org_ids multiselect, region search) + кнопка «Сбросить layout» (вызов resetLayout из composable) + toggle isEditing
- `<GridLayout>` с layout из composable, slot для каждого `{i}`:
  - `kpi-vehicles` → VehicleKpiCard(title='Машин всего', value=kpi.total_vehicles, icon='mdi-car-multiple')
  - `kpi-fuel` → VehicleKpiCard(title='Бензин за период', value=kpi.fuel_total_cost, unit='₽')
  - `kpi-repairs` → VehicleKpiCard(title='Ремонты за период', value=kpi.repairs_total_cost, unit='₽')
  - `kpi-mileage` → VehicleKpiCard(title='Пробег за период', value=kpi.total_mileage_km, unit='км')
  - `canister` → FuelCanisterWidget(spentL/spentAmount/fuelBudget/level из `/api/vehicles-dashboard/fuel-canister`)
  - `in_repair` → VehiclesInRepairWidget
  - `to_warning` → MaintenanceWarningWidget
  - `bar_org` → ApexCharts bar (`/api/vehicles-dashboard/by-org?mode=owner|assigned` с toggle)
  - `donut_state` → ApexCharts donut (`/api/vehicles-dashboard/state-donut`)
  - `line_fuel` → ApexCharts line (`/api/vehicles-dashboard/fuel-by-period?granularity=`)
  - `top10_table` → v-data-table с `/api/vehicles-dashboard/top-expenses?limit=10`
- ApexCharts theme: copy dark/light mode aware computed colors из DashboardView (`chartText, chartMuted, chartGrid`).
- Reactive watch filter → refetch all endpoints (debounce 300ms).
- Route transition `:key="route.fullPath"` (Gotcha 2026-04-19 — НЕ `mode="out-in"` для chart-routes).

**Task 7: Pre-push grep duplicate let/const** для всех 5 файлов.

**Task 8: Commit message:** `feat(29-19): frontend VehicleDashboardView + 11 widgets + SVG canister + pulse-glow CSS`

## Verification
- `ls frontend/src/components/vehicles/*.vue` — теперь 13+ файлов.
- `grep -c "GridLayout\|GridItem" frontend/src/views/property/VehicleDashboardView.vue` — найдено.
- `grep -c "wave-sway\|pulse-warn" frontend/src/components/vehicles/FuelCanisterWidget.vue frontend/src/components/vehicles/MaintenanceWarningWidget.vue` — найдено.
- `grep -n "ApexCharts\|VueApexCharts" frontend/src/views/property/VehicleDashboardView.vue` — найдено.
- Pre-push grep duplicate — пусто.
- Push → autodeploy → bundle hash сменился.
- Visual smoke (Lesson 2026-05-11 — обязательно открыть в браузере):
  - `/property/vehicles/dashboard` → 11 widgets рендерятся
  - SVG canister показывает level + анимация waves
  - Pulse-glow на maintenance-warning карточках
  - Drag widget → переместился → reload → позиция сохранена
  - Period filter → все widgets refetch
- `git log origin/claude..HEAD` — пусто.

## Risks / Gotchas
- **Lesson 2026-04-19 clampScore:** SVG canister level **MUST** быть clamped 0..1. NaN/negative → 0. Если не сделать — `<rect r="-X">` сломает SVG, остальная dashboard убьётся.
- **Lesson 2026-04-19 transition mode:** **НЕ** использовать `mode="out-in"` для route с chart'ами. Использовать `:key="route.fullPath"`.
- **Lesson 2026-05-11 — Sonnet врёт про push:** обязательно `git log origin/claude..HEAD` И visual smoke в браузере **самим**, не на основе отчёта.
- **ApexCharts dark/light:** copy computed colors pattern из DashboardView.
- **Image fetch с auth-header:** photo через `<img>` с blob URL после fetch(`/api/vehicle-attachments/{id}/download`) → arrayBuffer → URL.createObjectURL. Иначе img не пошлёт Bearer token.
- **grid-layout-plus reset:** `resetLayout()` из composable перезаписывает localStorage и emit'ит fresh DEFAULT_LAYOUT.
- **Lesson 2026-05-05:** дубли let/const в большом view — pre-push grep ОБЯЗАТЕЛЬНО.

## Definition of Done
- [ ] `useVehicleDashboardLayout.ts` создан с правильным storage key
- [ ] 4 widget-компонента + VehicleKpiCard созданы
- [ ] VehicleDashboardView.vue полностью реализован с GridLayout + 11 widgets
- [ ] SVG canister с wave animation работает, level clamped
- [ ] Pulse-glow CSS на maintenance-warning
- [ ] localStorage persistence per-user работает (drag → reload → позиция сохранилась)
- [ ] Filter bar refetch'ит все endpoints с debounce
- [ ] No `mode="out-in"` для этого route
- [ ] Pre-push grep duplicate — пусто
- [ ] Commit `feat(29-19): frontend VehicleDashboardView + 11 widgets …` запушен в claude
- [ ] Bundle hash сменился
- [ ] Visual smoke в браузере: dashboard рендерится, drag работает, filter работает
- [ ] `git log origin/claude..HEAD` — пусто

## Executor boilerplate
1. No verification loops.
2. После push — `git log origin/claude..HEAD` + bundle hash check + **visual browser smoke** (Lesson 2026-05-11).
3. **OBLIGATORY:** clampScore-pattern для SVG attributes (Lesson 2026-04-19).
4. **OBLIGATORY:** pre-push grep duplicate let/const.
5. **NO** `mode="out-in"` на роут (Lesson 2026-04-19).
6. dict-detail для всех ошибок (Lesson 2026-05-04).
