---
phase: 15-reusable-purchase-items-editor
plan: 04
plan_id: 15-04
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - frontend/src/views/WishesView.vue
files_created: []
autonomous: true
estimated_tokens: 12000
requirements:
  - ITEMS-EDITOR-07
requirements_addressed:
  - "ITEMS-EDITOR-07: WishesView.vue Section 2 «Позиции» (lines 326-399) replaced with `<PurchaseItemsEditor v-model=\"wishForm.items\" item-shape=\"wish\" :purchase-id=\"null\">`. Заявка now has: products autocomplete picker, photo tooltip + mini-thumb, full product card dialog with photo upload, Excel import (2-step drag-map with client-side apply since no pid), smart import preview. `saveWish()` gains a `.map(({ _selectedProduct, _photo_url, _description, ...rest }) => rest)` stripper because helper fields now flow through items."

must_haves:
  truths:
    - "WishesView.vue contains `<PurchaseItemsEditor v-model=\"wishForm.items\" item-shape=\"wish\" :purchase-id=\"null\">` — exactly one mount"
    - "The former simple v-table at lines 326-399 (with raw v-text-field on item_name, no autocomplete/photo/import) is REMOVED from WishesView.vue"
    - "WishesView's `saveWish` strips helper fields: `items: wishForm.items.map(({ _selectedProduct, _photo_url, _description, ...rest }) => rest)` — otherwise the PUT body contains UI-only props that the backend rejects or ignores"
    - "The component is passed `:purchase-id=\"null\"` explicitly, so the Excel import dialog uses the client-side apply path (preview → build rows → emit update:modelValue), not the pid-bound `/api/purchases/{pid}/items/import-mapped`"
    - "Opening «Создать заявку» → add position → typing name triggers the catalogue picker (not a raw text input)"
    - "Uploading an .xlsx with items → drag-map columns → «Применить» populates `wishForm.items` — verified via a console.log or network tab (no call to `/api/purchases/.../items/import-mapped`)"
    - "Saving the wish with items POSTs `/api/wishes/` with items body matching the WishItemCreate schema (no `_selectedProduct`, no `_photo_url`, no `_description`, no `final_unit_price`, no `feo_planned_item_id` since WishItem has no such columns)"
    - "`cd frontend && npm run build` exits 0"
  artifacts:
    - path: "frontend/src/views/WishesView.vue"
      provides: "Wishes create/edit view, mounts PurchaseItemsEditor for Section 2"
      imports:
        - "PurchaseItemsEditor from '@/components/PurchaseItemsEditor.vue'"
      contains:
        - "<PurchaseItemsEditor"
        - "item-shape=\"wish\""
        - ":purchase-id=\"null\""
        - "_selectedProduct"
      line_reduction: "between 40 and 100 lines net smaller"
  key_links:
    - from: "WishesView.vue"
      to: "PurchaseItemsEditor.vue"
      via: "direct mount + v-model=\"wishForm.items\""
      pattern: "<PurchaseItemsEditor"
---

<objective>
Replace the simple position table in `frontend/src/views/WishesView.vue` Section 2 (lines 326-399) with `<PurchaseItemsEditor>` — same component CreateOrderView now uses — configured for the Wish item shape (7 columns, no `final_*`, no FEO) and without a `purchaseId` so Excel imports run client-side. Patch `saveWish` to strip UI-local helper fields from items before PUT/POST. This achieves full parity «Заявка ↔ Новый заказ» on the position editor and closes the user's original complaint.
</objective>

<execution_context>
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-reusable-purchase-items-editor/15-CONTEXT.md
@.planning/phases/15-reusable-purchase-items-editor/15-RESEARCH.md
@.planning/phases/15-reusable-purchase-items-editor/15-01-PLAN.md
@.planning/phases/15-reusable-purchase-items-editor/15-01-SUMMARY.md
@CLAUDE.md
@frontend/src/views/WishesView.vue
@frontend/src/components/PurchaseItemsEditor.vue
@backend/app/models/wish_item.py
@backend/app/routers/wishes.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Mount PurchaseItemsEditor in WishesView.vue Section 2 and remove the old table</name>
  <files>frontend/src/views/WishesView.vue</files>
  <read_first>
    - frontend/src/components/PurchaseItemsEditor.vue (verify the wish-shape template branch exists per Plan 15-01)
    - .planning/phases/15-reusable-purchase-items-editor/15-01-SUMMARY.md
    - frontend/src/views/WishesView.vue lines 326-399 (current simple items table — TO BE REPLACED)
    - frontend/src/views/WishesView.vue (search for `wishForm.items`, `addItem`, `removeItem`, `calcItemTotal` — existing wish-side counterparts)
    - frontend/src/views/WishesView.vue (find `saveWish` function — Task 2 patches this)
  </read_first>
  <action>
    In WishesView.vue:

    1. Replace the Section 2 `<v-card-text>` body (lines 334-398 approximately — re-read for exact bounds) with:
       ```vue
       <v-card-text class="pa-4 pt-2">
         <PurchaseItemsEditor
           v-model="wishForm.items"
           item-shape="wish"
           :purchase-id="null"
           :default-item-type="'товар'"
           :default-unit="'шт.'"
           :default-country="'Россия'"
           :allowed-item-types="['товар','услуга','работа']"
           :supports-excel-import="true"
           :supports-smart-import="true"
           :supports-full-product-dialog="true"
           :supports-photo-upload="true"
           :readonly="wishForm.status === 'approved' || wishForm.status === 'converted'"
         />
       </v-card-text>
       ```
       Adjust the `readonly` binding to match WishesView's actual "is this wish editable" rule (check how existing inputs in WishesView gate disabled — copy the same condition).

    2. Remove the wrapping `<v-card-title>` block's "Добавить позицию" button (lines 328-332) — the component now owns that button in its toolbar.

    3. Delete WishesView-local refs/methods that are now owned by the component:
       - `addItem()`, `removeItem()`, `calcItemTotal()` — delete if they exist in WishesView script block. Verify with grep first. If the method is named differently (e.g. `addWishItem`), delete that.
       - Any WishesView-local `UNIT_OPTIONS` constant — safe to delete; component has its own.

    4. Add the import at the top of `<script setup>`:
       ```ts
       import PurchaseItemsEditor from '@/components/PurchaseItemsEditor.vue'
       ```

    5. Ensure `wishForm.items` is initialised as an array (should already be the case — check `wishForm` default). Initial wish edit path: when loading an existing wish, existing items from the API get assigned to `wishForm.items`. PurchaseItemsEditor will copy into its internal `localItems` via the `watch(() => props.modelValue, ...)` from Plan 15-01.

    Do NOT add `final_unit_price`, `final_total`, or `feo_planned_item_id` to `wishForm.items` entries — those columns don't exist on WishItem in the backend and the component's `itemShape="wish"` branch doesn't render them.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<PurchaseItemsEditor" frontend/src/views/WishesView.vue` ≥ 1
    - `grep -c "item-shape=\"wish\"" frontend/src/views/WishesView.vue` ≥ 1
    - `grep -c ":purchase-id=\"null\"" frontend/src/views/WishesView.vue` ≥ 1
    - `grep -c "import PurchaseItemsEditor" frontend/src/views/WishesView.vue` ≥ 1
    - The old raw `<v-text-field` on `item.item_name` (WishesView.vue:352-358) is GONE: `grep -n 'v-model="item.item_name"' frontend/src/views/WishesView.vue` returns 0 matches
    - `grep -c "function addItem\\|function calcItemTotal" frontend/src/views/WishesView.vue` returns 0 (methods deleted)
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    WishesView Section 2 now renders PurchaseItemsEditor in wish mode. Old simple table gone. Type-check passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Patch saveWish to strip UI-local helper fields from items</name>
  <files>frontend/src/views/WishesView.vue</files>
  <read_first>
    - frontend/src/views/WishesView.vue (find `saveWish` function — see lines building body for POST /api/wishes/ and PUT /api/wishes/{id})
    - frontend/src/views/CreateOrderView.vue lines 5441-5460 (reference pattern for helper-field stripping — `.map(({ _selectedProduct, _photo_url, _description, _description_44fz, ...rest }) => rest)`)
    - backend/app/routers/wishes.py lines 115-162 (WishItem create/update API — verify the schema fields accepted)
    - backend/app/models/wish_item.py (authoritative field list for WishItem)
  </read_first>
  <action>
    Locate `saveWish` (or whatever the wish save function is called) in WishesView.vue. Find where the request body is assembled — typically something like:

    ```ts
    const body = {
      subsidy_id: wishForm.subsidy_id,
      ...
      items: wishForm.items,   // <-- problematic after the refactor
    }
    ```

    Replace the `items` line with:
    ```ts
    items: wishForm.items.map(({ _selectedProduct, _photo_url, _description, _description_44fz, ...rest }) => rest),
    ```

    (Include `_description_44fz` even though WishItem doesn't use it — PurchaseItemsEditor's EditorItem interface includes it and some Excel imports may populate it. Stripping it is safer than letting it through.)

    Also verify WishItemCreate schema in the backend does NOT allow extra fields (pydantic strict mode) — if it's permissive (`extra="ignore"` or default), this is a nice-to-have; if it's strict, it's REQUIRED to avoid 422 errors. Either way, add the stripper.

    If the wish save also goes through a PUT path for edits, apply the same stripper there.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "wishForm\\.items\\.map\\(\\(\\{[^}]*_selectedProduct[^}]*\\}" frontend/src/views/WishesView.vue` ≥ 1
    - `grep -c "_description_44fz" frontend/src/views/WishesView.vue` ≥ 1 (in the stripper)
    - `grep -c "_photo_url" frontend/src/views/WishesView.vue` ≥ 1
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>
    Helper fields are stripped from wish items on save. No chance of polluting the wish_items table with UI-only keys.
  </done>
</task>

<task type="checkpoint:manual" tdd="false">
  <name>Task 3: Manual smoke — create a wish, add items via picker, upload photo, Excel-import, save</name>
  <files></files>
  <read_first>
    - .planning/phases/15-reusable-purchase-items-editor/15-CONTEXT.md section "Success test"
  </read_first>
  <action>
    In browser (dev or deployed), log in and:

    1. Navigate to «Заявки» → «Создать заявку»
    2. Pick a subsidy + FEO 3-level
    3. Click «Добавить позицию» → product picker opens
    4. Type a product name → catalogue suggestions appear with photo + price
    5. Select one → row fills (name, type, unit_price, country_origin="Россия"). Photo thumb visible.
    6. Hover photo thumb → 200×200 preview tooltip appears
    7. Click «Создать полную карточку» → dialog opens
    8. Fill name, category, price, upload a photo (v-file-input) → preview renders → Save
    9. Row populates with the newly created product, photo thumb visible
    10. Click «Импорт из файла» → upload a 3-row .xlsx with columns (Наименование, Кол-во, Цена) → Step 2 drag-map column cards → «Применить»
    11. Verify 3 new rows appear with data. **Open Network tab** — confirm there was NO call to `/api/purchases/.../items/import-mapped` (purchaseId is null, so the apply must run client-side)
    12. Click «Сохранить» → POST /api/wishes/ with 5+ items in body
    13. **Open the Network tab** — inspect the POST body `items` array — verify each item has ONLY: product_id, item_name, item_type, quantity, unit, unit_price, total_price, country_origin (and optional id). Verify there are NO `_selectedProduct`, `_photo_url`, `_description` keys
    14. Reload the wish from the list → all 5 items persist with photos, prices, quantities

    If any step fails, document in SUMMARY and DO NOT mark plan complete.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -5 ; cd frontend && npm run build 2>&1 | tail -5</automated>
    <manual>14-step browser smoke. Document pass/fail per step in 15-04-SUMMARY.md with network payload screenshot for step 13.</manual>
  </verify>
  <acceptance_criteria>
    - All 14 smoke steps pass
    - Network tab confirms no `/api/purchases/.../items/import-mapped` call during Wish Excel import (step 11)
    - Save-body items array contains no helper-field keys (step 13)
    - Wish reload shows persisted items with photos (step 14)
  </acceptance_criteria>
  <done>
    Заявка has full parity with Новый заказ on the position editor. User's original complaint resolved. Ready for Plan 15-05 (E2E automation + UAT gate).
  </done>
</task>

</tasks>

<verification>
Plan-level:
1. `cd frontend && npm run build` exits 0
2. `grep -c "<PurchaseItemsEditor" frontend/src/views/WishesView.vue` ≥ 1
3. `grep -c ":purchase-id=\"null\"" frontend/src/views/WishesView.vue` ≥ 1
4. `grep -c "_selectedProduct" frontend/src/views/WishesView.vue` ≥ 1 (stripper)
5. 14-step manual smoke documented with network payloads
</verification>

<success_criteria>
- PurchaseItemsEditor mounted in WishesView Section 2 with item-shape=wish and purchase-id=null
- Old simple `<v-text-field v-model="item.item_name">` table removed
- saveWish strips helper fields from items
- Заявка now supports: products autocomplete, photo tooltip, full product card dialog with upload, Excel import (client-side apply), smart import
- Git commit: `feat(15-04): wire PurchaseItemsEditor into WishesView — parity with Новый заказ`
</success_criteria>

<output>
Create `.planning/phases/15-reusable-purchase-items-editor/15-04-SUMMARY.md`:
- Line count delta for WishesView.vue
- 14-step smoke checklist with pass/fail
- Network payload sample (step 13) with sensitive fields redacted
- Any WishesView-specific wiring quirks (e.g. how `readonly` is derived from wish status)
</output>
