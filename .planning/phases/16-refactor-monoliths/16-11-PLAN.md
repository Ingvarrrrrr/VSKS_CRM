---
phase: 16-refactor-monoliths
plan: 11
type: execute
wave: 3
depends_on: [16-07, 16-08, 16-09, 16-10]
files_modified:
  - backend/app/routers/tasks.py
  - backend/app/routers/purchases.py
autonomous: true
requirements: [REFACTOR-02, REFACTOR-04, REFACTOR-08]
must_haves:
  truths:
    - "tasks.py ≤ 500 lines (D-09 target ~450)"
    - "tasks.py contains ONLY CRUD + list + get + subtasks + categories + departments + patch + review-complete"
    - "purchases.py L1400 cross-import updated: from app.routers.task_visibility import _enrich_tasks"
    - "No residual comment/badge/delegation endpoints in tasks.py"
    - "GET /api/tasks/ list still enriches visibility correctly"
  artifacts:
    - path: "backend/app/routers/tasks.py"
      provides: "Residual CRUD router — create/read/update/delete + subtasks + categories + departments"
      max_lines: 500
  key_links:
    - from: "backend/app/routers/purchases.py"
      to: "backend/app/routers/task_visibility.py"
      via: "imports _enrich_tasks (updated from tasks.py)"
      pattern: "from app.routers.task_visibility import _enrich_tasks"
    - from: "backend/app/routers/tasks.py"
      to: "backend/app/routers/task_visibility.py"
      via: "imports _get_visible_user_ids, _enrich_tasks"
      pattern: "from app.routers.task_visibility import"
---

<objective>
Finalize tasks.py decomposition: residual CRUD-only module. Fix cross-file import in purchases.py (line 1400 per grep: `from app.routers.tasks import _enrich_tasks` → `from app.routers.task_visibility import _enrich_tasks`) per RESEARCH.md §G-06 and Pitfall 1. Verify tasks.py now ≤ 500 lines. Implements D-09 finalization + cleanup. No new router created in this plan.
</objective>

<execution_context>
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-refactor-monoliths/16-CONTEXT.md
@.planning/phases/16-refactor-monoliths/16-RESEARCH.md
@.planning/phases/16-refactor-monoliths/16-VALIDATION.md

<interfaces>
<!-- Pre-existing cross-file import at purchases.py L1400 (confirmed by grep): -->
<!--   from app.routers.tasks import _enrich_tasks -->
<!-- After Plan 16-07 moved _enrich_tasks to task_visibility.py, tasks.py imports it back (so old import still works). -->
<!-- This plan tightens the dependency graph: purchases.py should import directly from task_visibility. -->
<!-- After this fix: tasks.py no longer needs to re-export _enrich_tasks — but keep it, future-proof. -->
<!-- tasks.py residual should contain (D-09 baseline): list_tasks, get_task, list_subtasks, create_task, update_task, patch_task, review_complete, categories, departments endpoints. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix purchases.py cross-import (line 1400)</name>
  <read_first>
    - backend/app/routers/purchases.py (verify exact line: `grep -n "from app.routers.tasks import _enrich_tasks" backend/app/routers/purchases.py`)
    - backend/app/routers/task_visibility.py (Plan 16-07 — confirms `_enrich_tasks` is exported)
    - .planning/phases/16-refactor-monoliths/16-RESEARCH.md §G-06, Pitfall 1
  </read_first>
  <files>backend/app/routers/purchases.py</files>
  <action>
    1. Open purchases.py, locate the line (currently L1400 based on pre-refactor grep — if numbering shifted due to prior extractions, grep again: `grep -n "from app.routers.tasks import _enrich_tasks" backend/app/routers/purchases.py`).
    2. Replace:
       ```python
       from app.routers.tasks import _enrich_tasks
       ```
       with:
       ```python
       from app.routers.task_visibility import _enrich_tasks
       ```
       Leave surrounding code (the function body this import lives inside — `list_purchase_tasks`) unchanged.
    3. Grep confirms:
       - `grep -c "from app.routers.tasks import _enrich_tasks" backend/app/routers/purchases.py` returns 0
       - `grep -c "from app.routers.task_visibility import _enrich_tasks" backend/app/routers/purchases.py` returns 1
    4. Global sanity: `grep -rn "from app.routers.tasks import _enrich_tasks" backend/` MUST return 0 hits (entire backend tree clean).
  </action>
  <verify>
    <automated>! grep -rn "from app.routers.tasks import _enrich_tasks" backend/ && grep -q "from app.routers.task_visibility import _enrich_tasks" backend/app/routers/purchases.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "from app.routers.tasks import _enrich_tasks" backend/` returns 0 hits
    - `grep -c "from app.routers.task_visibility import _enrich_tasks" backend/app/routers/purchases.py` returns 1
    - `docker exec vsks_crm-backend-1 python -c "from app.routers.purchases import list_purchase_tasks"` exits 0
  </acceptance_criteria>
  <done>
    Cross-file import updated — dependency graph now clean: purchases → task_visibility (direct), no transit through tasks.py.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify tasks.py residual size ≤ 500 lines + content sanity</name>
  <read_first>
    - backend/app/routers/tasks.py (current state post-16-10)
    - .planning/phases/16-refactor-monoliths/16-CONTEXT.md D-09
  </read_first>
  <files>backend/app/routers/tasks.py</files>
  <action>
    1. Line count check: `wc -l backend/app/routers/tasks.py` — MUST be ≤ 500 (D-09 target ~450).
    2. If > 500 lines, investigate residue:
       - `grep -n "^@router\." backend/app/routers/tasks.py` — enumerate remaining endpoints
       - Allowed in residual tasks.py: `list_tasks` (GET /), `get_task` (GET /{id}), `list_subtasks` (GET /{id}/subtasks), `create_task` (POST /), `update_task` (PUT /{id}), `patch_task` (PATCH /{id}), `review_complete` (POST /{id}/review-complete), `categories` (GET /categories), `departments` (GET /departments), `delete_task` (DELETE /{id})
       - If any extra endpoints remain, note in SUMMARY as "follow-up extraction candidate" but do NOT re-extract in this plan (scope).
    3. Sanity check — no stale imports from extracted modules:
       - tasks.py should still have `from app.routers.task_visibility import _get_visible_user_ids, _enrich_tasks, _create_task_chat_room` (Plan 16-07)
       - tasks.py should still have `from app.routers.task_delegation import _set_assignees` (Plan 16-10)
       - tasks.py should NOT have stale duplicates of these names.
    4. Content sanity greps:
       - `grep -c '@router.get("/{id}/comments"' backend/app/routers/tasks.py` returns 0 (moved in 16-08)
       - `grep -c '@router.get("/badges"' backend/app/routers/tasks.py` returns 0 (moved in 16-09)
       - `grep -c '@router.get("/pending-consent"' backend/app/routers/tasks.py` returns 0 (moved in 16-10)
       - `grep -c '@router.post("/{id}/broadcast"' backend/app/routers/tasks.py` returns 0 (moved in 16-08)
  </action>
  <verify>
    <automated>[ $(wc -l < backend/app/routers/tasks.py) -le 500 ] && ! grep -nE '@router.(get|post|delete)\("/{id}/comments|@router.get\("/badges"|@router.get\("/pending-consent"' backend/app/routers/tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l backend/app/routers/tasks.py` ≤ 500
    - `grep -c "from app.routers.task_visibility import" backend/app/routers/tasks.py` ≥ 1
    - `grep -c "from app.routers.task_delegation import _set_assignees" backend/app/routers/tasks.py` returns 1
    - All 4 extracted-endpoint decorators (comments/badges/pending-consent/broadcast) return 0 from grep on tasks.py
    - `docker compose build backend` exits 0
  </acceptance_criteria>
  <done>
    tasks.py residual is CRUD-only and within D-09 size target.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full verification + commit atomic cross-file fix</name>
  <read_first>
    - backend/tests/test_routers_mounted.py
  </read_first>
  <files>backend/app/routers/tasks.py, backend/app/routers/purchases.py</files>
  <action>
    1. Python-level sanity: `docker exec vsks_crm-backend-1 python -c "from app.routers.purchases import list_purchase_tasks; from app.routers.tasks import list_tasks; print('ok')"` → `ok`.
    2. Rebuild: `docker compose build backend` exits 0.
    3. Mount smoke (all routers still mounted): `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` → all pass.
    4. Push to deploy. Full E2E: `BASE_URL=http://85.239.53.155 npx playwright test` → 70/70 pass (REFACTOR-05).
    5. Commit: `refactor(16-11): tighten cross-import purchases→task_visibility` — files: purchases.py (modified), tasks.py (verified, possibly no diff if already trimmed). NO change to `backend/app/__init__.py` (no new router).
    6. This commit concludes tasks.py decomposition (REFACTOR-02). Backend decomposition complete.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -qE "refactor\(16-11\): (tighten cross-import|finalize tasks)" && [ $(wc -l < backend/app/routers/tasks.py) -le 500 ]</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1` matches `^[a-f0-9]+ refactor\(16-11\):`
    - `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` exits 0
    - `BASE_URL=http://85.239.53.155 npx playwright test` exits 0 (full 70/70 suite on deploy)
    - `wc -l backend/app/routers/tasks.py` ≤ 500
    - `wc -l backend/app/routers/purchases.py` ≤ 1500 (overall purchases still in limit)
    - Zero `from app.routers.tasks import _enrich_tasks` hits in entire `backend/` tree
  </acceptance_criteria>
  <done>
    Backend decomposition DONE. REFACTOR-01 + REFACTOR-02 complete. All 70 E2E green on deploy.
  </done>
</task>

</tasks>

<verification>
- `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` → all pass
- Full E2E: `BASE_URL=http://85.239.53.155 npx playwright test` → 70/70 pass
- `wc -l backend/app/routers/tasks.py` ≤ 500
- All backend module sizes within D-03..D-13 targets (document actuals in SUMMARY)
- No circular import in entire app: `docker exec vsks_crm-backend-1 python -c "import app"` exits 0
</verification>

<success_criteria>
Backend decomposition complete. tasks.py ≤ 500 lines. purchases.py clean cross-import. REFACTOR-01, REFACTOR-02, REFACTOR-04, REFACTOR-05, REFACTOR-06, REFACTOR-08 all GREEN. Commit: `refactor(16-11): tighten cross-import purchases→task_visibility`.
</success_criteria>

<output>
After completion, create `.planning/phases/16-refactor-monoliths/16-11-SUMMARY.md`. Document final line counts for all 11 backend modules (purchases + 5 purchase_* + tasks + 4 task_*) for retrospective.
</output>
