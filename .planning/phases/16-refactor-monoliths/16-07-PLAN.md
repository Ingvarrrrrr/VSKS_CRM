---
phase: 16-refactor-monoliths
plan: 07
type: execute
wave: 1
depends_on: [16-01]
files_modified:
  - backend/app/routers/task_visibility.py
  - backend/app/routers/tasks.py
autonomous: true
requirements: [REFACTOR-02, REFACTOR-04, REFACTOR-08]
must_haves:
  truths:
    - "_get_visible_user_ids importable from app.routers.task_visibility"
    - "_enrich_tasks importable from app.routers.task_visibility"
    - "_create_task_chat_room importable from app.routers.task_visibility"
    - "GET /api/tasks/ list results unchanged (same visibility filter applied)"
    - "tasks.py no longer defines _get_visible_user_ids or _enrich_tasks bodies"
  artifacts:
    - path: "backend/app/routers/task_visibility.py"
      provides: "Helper-only module: _get_visible_user_ids + _enrich_tasks + _create_task_chat_room"
      min_lines: 150
  key_links:
    - from: "backend/app/routers/tasks.py"
      to: "backend/app/routers/task_visibility.py"
      via: "imports _get_visible_user_ids, _enrich_tasks"
      pattern: "from app.routers.task_visibility import"
---

<objective>
Extract visibility + enrichment helpers into `task_visibility.py` — helper-only module (no router, no endpoints) per RESEARCH.md §1.1-1.2 and Section 1.2 extraction step 1. Implements D-10 + D-21. This is the FIRST tasks.py extract and must land before 16-08..16-10 so they can import from it.
</objective>

<execution_context>
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-refactor-monoliths/16-CONTEXT.md
@.planning/phases/16-refactor-monoliths/16-RESEARCH.md
@.planning/phases/16-refactor-monoliths/16-VALIDATION.md

<interfaces>
<!-- Current locations in tasks.py (per RESEARCH.md §1.2): -->
<!--   _get_visible_user_ids ~ L25-75 -->
<!--   _enrich_tasks ~ L111-277 -->
<!--   _create_task_chat_room ~ search tasks.py — called by consent/delegation -->
<!-- Callers (cross-file): -->
<!--   purchases.py L1400 does `from app.routers.tasks import _enrich_tasks` — fix in Plan 16-11 -->
<!--   task_badges.py / task_delegation.py / task_comments.py / tasks.py will all import _get_visible_user_ids and/or _enrich_tasks -->
<!-- This plan ONLY creates task_visibility.py + removes bodies from tasks.py + adds import-back inside tasks.py. -->
<!-- Cross-file fix in purchases.py is handled in Plan 16-11. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create task_visibility.py with visibility + enrichment helpers</name>
  <read_first>
    - backend/app/routers/tasks.py (full file — focus on L25-75 for _get_visible_user_ids, L111-277 for _enrich_tasks, and grep for `def _create_task_chat_room`)
    - .planning/phases/16-refactor-monoliths/16-RESEARCH.md §1.2 step 1, §2.3 underscore convention, §G-06
    - .planning/phases/16-refactor-monoliths/16-CONTEXT.md D-10, D-21
  </read_first>
  <files>backend/app/routers/task_visibility.py</files>
  <action>
    1. Create `backend/app/routers/task_visibility.py` with imports required by the moved helpers (read tasks.py top-of-file to confirm). Likely includes:
       ```python
       from sqlalchemy import select, func, or_, and_
       from sqlalchemy.ext.asyncio import AsyncSession
       from typing import Optional, List, Set, Dict

       from app.models.user import User
       from app.models.user_hierarchy import UserHierarchy
       from app.models.task import Task
       from app.models.chat import ChatRoom, ChatParticipant, ChatMessage
       # add further model imports as needed after reading current tasks.py
       ```
       **No `APIRouter` import, no `router = APIRouter(...)` line. Helper-only.**
    2. MOVE (cut from tasks.py, paste into task_visibility.py) these identifiers:
       - Function `_get_visible_user_ids` (L25-75 per §1.2)
       - Function `_enrich_tasks` (L111-277 per §1.2)
       - Function `_create_task_chat_room` (search tasks.py)
    3. Preserve function signatures BYTE-FOR-BYTE. No logic refactor.
    4. Keep underscore prefixes (§2.3 — importable but marked internal).
  </action>
  <verify>
    <automated>test -f backend/app/routers/task_visibility.py && grep -q "def _get_visible_user_ids" backend/app/routers/task_visibility.py && grep -q "def _enrich_tasks" backend/app/routers/task_visibility.py && grep -q "def _create_task_chat_room" backend/app/routers/task_visibility.py && ! grep -q "APIRouter" backend/app/routers/task_visibility.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/routers/task_visibility.py` exists
    - Contains `def _get_visible_user_ids(`
    - Contains `def _enrich_tasks(`
    - Contains `def _create_task_chat_room(`
    - Does NOT contain `APIRouter` (`grep -c "APIRouter" backend/app/routers/task_visibility.py` returns 0)
    - Does NOT contain `@router.` decorator (`grep -c "@router\." backend/app/routers/task_visibility.py` returns 0)
    - `wc -l backend/app/routers/task_visibility.py` ≥ 150 and ≤ 300
  </acceptance_criteria>
  <done>
    task_visibility.py is a pure helper module exposing visibility + enrichment + chat-room functions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete moved helpers from tasks.py and import them back</name>
  <read_first>
    - backend/app/routers/tasks.py (current state)
    - backend/app/routers/task_visibility.py (confirm moved list)
  </read_first>
  <files>backend/app/routers/tasks.py</files>
  <action>
    1. Delete from tasks.py:
       - Function body `def _get_visible_user_ids(...)` (L25-75)
       - Function body `def _enrich_tasks(...)` (L111-277)
       - Function body `def _create_task_chat_room(...)`
    2. Add import near top of tasks.py (after existing `from app.*` imports, before `router = APIRouter(...)`):
       ```python
       from app.routers.task_visibility import _get_visible_user_ids, _enrich_tasks, _create_task_chat_room
       ```
    3. Verify tasks.py CRUD endpoints still call these by name (`_enrich_tasks(...)`, `_get_visible_user_ids(...)`) — no code change needed in call sites; the import brings them into scope.
    4. Grep external callers:
       - `grep -rn "from app.routers.tasks import _get_visible_user_ids" backend/` — update any hits to `task_visibility`
       - `grep -rn "from app.routers.tasks import _create_task_chat_room" backend/` — update
       - DO NOT update `purchases.py` L1400 (`from app.routers.tasks import _enrich_tasks`) in THIS plan — that cross-file fix is handled in Plan 16-11 to keep commit atomicity (this plan touches only tasks.py + task_visibility.py). Leave the purchases.py import pointing at `tasks` for now; it will still resolve because tasks.py also imports `_enrich_tasks` from task_visibility (so `from app.routers.tasks import _enrich_tasks` still works until Plan 16-11 tightens it).
  </action>
  <verify>
    <automated>! grep -nE "^def _get_visible_user_ids|^async def _enrich_tasks|^def _enrich_tasks|^async def _create_task_chat_room|^def _create_task_chat_room" backend/app/routers/tasks.py && grep -q "from app.routers.task_visibility import" backend/app/routers/tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^(async )?def _get_visible_user_ids" backend/app/routers/tasks.py` returns 0
    - `grep -cE "^(async )?def _enrich_tasks" backend/app/routers/tasks.py` returns 0
    - `grep -cE "^(async )?def _create_task_chat_room" backend/app/routers/tasks.py` returns 0
    - `grep -c "from app.routers.task_visibility import" backend/app/routers/tasks.py` returns 1
    - `wc -l backend/app/routers/tasks.py` ≤ 1500 (was 1698; visibility is ~200 lines)
    - purchases.py still builds: `docker exec vsks_crm-backend-1 python -c "from app.routers.purchases import list_purchase_tasks" 2>&1` exits 0
  </acceptance_criteria>
  <done>
    Helpers moved; tasks.py imports them back via task_visibility. Cross-file purchases.py import remains compatible (tasks.py re-exports via its own import).
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify build + smoke + commit atomic extract</name>
  <read_first>
    - backend/tests/test_routers_mounted.py
  </read_first>
  <files>backend/app/routers/task_visibility.py, backend/app/routers/tasks.py</files>
  <action>
    1. Verify Python-level import: `docker exec vsks_crm-backend-1 python -c "from app.routers.task_visibility import _get_visible_user_ids, _enrich_tasks, _create_task_chat_room; print('ok')"` → prints `ok`.
    2. Rebuild: `docker compose build backend` exits 0. No circular import.
    3. Run mount smoke: `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` → all pass (no new router mounted; existing `/api/tasks/` still responds).
    4. Push to deploy. E2E fast subset: `BASE_URL=http://85.239.53.155 npx playwright test e2e/02-all-pages.spec.ts e2e/05-orders.spec.ts` → pass (my-tasks page still loads with task enrichment).
    5. Commit: `refactor(16-07): extract task_visibility from tasks` — files: task_visibility.py (new), tasks.py (modified). NO change to `backend/app/__init__.py` (helper module, no router).
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -qE "refactor\(16-07\): extract task_visibility" && docker exec vsks_crm-backend-1 python -c "from app.routers.task_visibility import _get_visible_user_ids, _enrich_tasks; print('ok')" | grep -q ok</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1` matches `^[a-f0-9]+ refactor\(16-07\): extract task_visibility from tasks`
    - Commit diff: exactly 2 files (task_visibility.py new, tasks.py modified)
    - `backend/app/__init__.py` is NOT in commit diff
    - `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` exits 0
    - `BASE_URL=http://85.239.53.155 npx playwright test e2e/02-all-pages.spec.ts` exits 0
  </acceptance_criteria>
  <done>
    Visibility helpers extracted. No router mounted. Build green. E2E green. purchases.py cross-file import compatibility preserved for Plan 16-11.
  </done>
</task>

</tasks>

<verification>
- `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` → all pass
- `BASE_URL=http://85.239.53.155 npx playwright test e2e/02-all-pages.spec.ts e2e/05-orders.spec.ts` → all pass
- `wc -l backend/app/routers/task_visibility.py` ≥ 150 AND ≤ 300
- No circular import: `docker exec vsks_crm-backend-1 python -c "import app"` exits 0
</verification>

<success_criteria>
Visibility cluster extracted as helper-only module. Plans 16-08..16-11 can now import from it. Commit: `refactor(16-07): extract task_visibility from tasks`.
</success_criteria>

<output>
After completion, create `.planning/phases/16-refactor-monoliths/16-07-SUMMARY.md`.
</output>
