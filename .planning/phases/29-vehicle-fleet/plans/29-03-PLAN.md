# Plan 29-03: Permission seed — vehicles tab + 6 actions (programmatic в lifespan)

**Phase:** 29 (Vehicle Fleet)
**Wave:** 0
**Depends on:** 29-02
**Estimated complexity:** S (~120 LOC)
**Decisions covered:** D-05

## Goal
Seed permissions модуля vehicles программно в `backend/app/__init__.py` lifespan — БЕЗ SQL-файла (alembic chain сломан, R-9). После мерджа: `permission_tabs` содержит `vehicles`, `permission_actions` — 6 новых action_key, `role_permissions` — defaults для 6 ролей.

## Files

### Modify
- `c:\Users\1\Desktop\Cursor\VSKS_CRM\backend\app\__init__.py` — добавить блок Phase 29 permission seed ПОСЛЕ блока Phase 22 (~line 241-284, тот же паттерн)

## Tasks
1. В `backend/app/__init__.py` найти блок Phase 22 permission seed (по комментарию или `vehicles` поиск даст 0 совпадений — это новый seed). Вставить новый `try/except` блок ПОСЛЕ Phase 22 блока и ПЕРЕД блоком Phase 27.1 schema:
   ```python
   # Phase 29: vehicles tab + 6 actions
   try:
       from sqlalchemy import select as _sel29
       from .models.permission import PermissionTab, PermissionAction, RolePermission
       async with async_session() as db:
           # 1. Tab
           ex = await db.execute(_sel29(PermissionTab).where(PermissionTab.tab_key == 'vehicles'))
           if not ex.scalar_one_or_none():
               db.add(PermissionTab(tab_key='vehicles', title='Автотранспорт'))
               await db.commit()
           # 2. Actions
           for action_key, desc in [
               ('vehicle.edit',           'Редактирование карточки ТС'),
               ('vehicle.delete',         'Удаление ТС'),
               ('vehicle.import',         'Импорт Excel реестра ТС'),
               ('vehicle.odometer.write', 'Ввод пробега'),
               ('vehicle.repair.write',   'Добавление ремонтов'),
               ('vehicle.trip.create',    'Создание путевых листов'),
           ]:
               ex = await db.execute(_sel29(PermissionAction).where(PermissionAction.action_key == action_key))
               if not ex.scalar_one_or_none():
                   db.add(PermissionAction(action_key=action_key, description=desc))
           await db.commit()
           # 3. Role defaults
           ROLE_PERMS = [
               *[('vehicles', r, True) for r in ['superadmin','account_owner','admin','org_admin','manager','employee']],
               *[('vehicle.edit', r, True) for r in ['superadmin','account_owner','admin','org_admin','manager']],
               ('vehicle.edit', 'employee', False),
               *[('vehicle.delete', r, True) for r in ['superadmin','account_owner','admin']],
               *[('vehicle.delete', r, False) for r in ['org_admin','manager','employee']],
               *[('vehicle.import', r, True) for r in ['superadmin','account_owner','admin','org_admin']],
               *[('vehicle.import', r, False) for r in ['manager','employee']],
               *[('vehicle.odometer.write', r, True) for r in ['superadmin','account_owner','admin','org_admin','manager','employee']],
               *[('vehicle.repair.write', r, True) for r in ['superadmin','account_owner','admin','org_admin','manager']],
               ('vehicle.repair.write', 'employee', False),
               *[('vehicle.trip.create', r, True) for r in ['superadmin','account_owner','admin','org_admin','manager']],
               ('vehicle.trip.create', 'employee', False),
           ]
           for key, role, granted in ROLE_PERMS:
               ex = await db.execute(_sel29(RolePermission).where(
                   RolePermission.role_name == role, RolePermission.key == key
               ))
               if not ex.scalar_one_or_none():
                   db.add(RolePermission(role_name=role, key=key, granted=granted))
           await db.commit()
   except Exception as e:
       logging.getLogger(__name__).warning(f"Phase 29 permission seed skipped: {e}")
   ```
2. Проверить точное имя класса `RolePermission` через `grep -n "class RolePermission" backend/app/models/permission.py` — если другое (например `RolePermissionDefault`), скорректировать.
3. Проверить точное имя поля для default permission — у RolePermission может быть `granted` (BOOLEAN) или композитный ключ. Скопировать паттерн из Phase 22 блока в том же файле, чтобы убедиться в названиях.
4. Commit message: `feat(29-03): permission seed — vehicles tab + 6 actions`

## Verification
- `grep -n "vehicle.edit\|vehicle.delete\|vehicle.import\|vehicle.odometer.write\|vehicle.repair.write\|vehicle.trip.create" backend/app/__init__.py` — 6 совпадений в Phase 29 блоке (плюс могут быть в коде роутеров — отфильтровать `grep -n ... backend/app/__init__.py`).
- `grep -n "Phase 29: vehicles tab" backend/app/__init__.py` — 1 совпадение.
- Push → autodeploy → SSH проверка одним запросом: `docker exec vsks_crm-db-1 psql -U vsks -d vsks_crm -c "SELECT action_key FROM permission_actions WHERE action_key LIKE 'vehicle.%' ORDER BY action_key"` — должно вернуть 6 строк.
- `git log origin/claude..HEAD` — пусто.

## Risks / Gotchas
- **Идемпотентность:** все INSERT обёрнуты в `SELECT ... if not scalar_one_or_none(): db.add()`. На рестарте backend дополнительные записи не создаются.
- **Имена классов / полей** могут отличаться — открыть `backend/app/models/permission.py` и Phase 22 блок в `__init__.py` ДО написания кода, скопировать актуальные имена. НЕ изобретать.
- **Гранулярность granted=False** для employee/manager — explicit, чтобы Phase 17 override-матрица понимала «у этой роли запрещено по дефолту, можно дать override на пользователя».
- Если в `permission.py` нет класса `RolePermission` (используется другое имя для defaults) — найти Phase 22 seed и копировать его pattern.

## Definition of Done
- [ ] Phase 29 permission seed блок добавлен в `__init__.py` ПОСЛЕ Phase 22 и ДО Phase 27.1
- [ ] Commit `feat(29-03): permission seed — vehicles tab + 6 actions` запушен в claude
- [ ] SSH-проверка: `SELECT action_key FROM permission_actions WHERE action_key LIKE 'vehicle.%'` возвращает 6 строк
- [ ] SSH-проверка: `SELECT tab_key FROM permission_tabs WHERE tab_key = 'vehicles'` возвращает 1 строку
- [ ] `git log origin/claude..HEAD` — пусто

## Executor boilerplate
1. No verification loops. SSH-команда ОДИН раз — не цикл.
2. Перед push: открыть Phase 22 блок в `__init__.py` и убедиться что класс/поле имена совпадают.
3. NO `from sqlalchemy import select as _sel29` коллизий — посмотреть существующие импорты, использовать `_sel29` или `_sel_phase29` чтобы не пересекаться с другими блоками.
