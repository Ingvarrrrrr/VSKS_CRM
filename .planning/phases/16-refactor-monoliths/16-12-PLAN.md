---
phase: 16-refactor-monoliths
plan: 12
type: execute
wave: 4
depends_on: [16-11]
files_modified:
  - frontend/src/components/my-tasks/OrgSelector.vue
  - frontend/src/components/my-tasks/OrgSummaryBar.vue
  - frontend/src/views/MyTasksView.vue
autonomous: true
requirements: [REFACTOR-03, REFACTOR-05, REFACTOR-07]
must_haves:
  truths:
    - "OrgSelector.vue renders org cards + 'Все организации' card + counters (visually unchanged)"
    - "OrgSummaryBar.vue renders header counters + badges (visually unchanged)"
    - "frontend/src/components/my-tasks/ folder exists"
    - "MyTasksView.vue imports OrgSelector + OrgSummaryBar and passes orgSummary, selectedOrgId via props"
    - "Click on org card still triggers selectedOrgId update via emit"
    - "npm run build exits 0 with zero TypeScript warnings"
  artifacts:
    - path: "frontend/src/components/my-tasks/OrgSelector.vue"
      provides: "Org card grid: all-orgs card + per-org cards, selection via emit"
      min_lines: 150
    - path: "frontend/src/components/my-tasks/OrgSummaryBar.vue"
      provides: "Header counters + badges bar"
      min_lines: 100
    - path: "frontend/src/views/MyTasksView.vue"
      provides: "Orchestrator view — delegates org selection UI to OrgSelector child"
      max_lines: 1900
  key_links:
    - from: "frontend/src/views/MyTasksView.vue"
      to: "frontend/src/components/my-tasks/OrgSelector.vue"
      via: "renders OrgSelector child with props + select-org emit listener"
      pattern: "import OrgSelector from"
    - from: "frontend/src/views/MyTasksView.vue"
      to: "frontend/src/components/my-tasks/OrgSummaryBar.vue"
      via: "renders OrgSummaryBar child with props"
      pattern: "import OrgSummaryBar from"
---

<objective>
First frontend extraction — begin MyTasksView.vue decomposition (REFACTOR-03). Create `frontend/src/components/my-tasks/` folder. Extract `OrgSelector.vue` (D-15) and `OrgSummaryBar.vue` (D-18) — lowest-risk components (pure UI, no data-fetching). State via props + emits per RESEARCH §3.1 and §3.3. Implements D-14 + D-15 + D-18.
</objective>

<execution_context>
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-refactor-monoliths/16-CONTEXT.md
@.planning/phases/16-refactor-monoliths/16-RESEARCH.md
@.planning/phases/16-refactor-monoliths/16-VALIDATION.md

<interfaces>
Parent orchestrator: MyTasksView.vue — all apiFetch calls stay here (D-14 + RESEARCH §3.3).
State to pass: orgSummary, selectedOrgId, orgCardsOpen.
Events to emit from OrgSelector: 'select-org' (orgId number or null), 'update:orgCardsOpen' (boolean).
CSS to move per §3.2 / G-10: .org-cards-grid, .org-sel-card, .org-sel-card--all, .osc-*.
Pattern reference: frontend/src/components/PurchaseItemsEditor.vue (Phase 15 extraction).
Folder does NOT yet exist — Task 1 creates it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OrgSelector.vue in new my-tasks folder</name>
  <read_first>
    - frontend/src/views/MyTasksView.vue (full file — identify template block containing org-cards-grid, the selectOrg handler, v-for of orgSummary; note exact CSS selectors in style scoped)
    - frontend/src/components/PurchaseItemsEditor.vue (Phase 15 pattern reference: script setup + defineProps + defineEmits)
    - .planning/phases/16-refactor-monoliths/16-RESEARCH.md §1.3, §3.1, §3.2, §G-10
    - .planning/phases/16-refactor-monoliths/16-CONTEXT.md D-14, D-15
  </read_first>
  <files>frontend/src/components/my-tasks/OrgSelector.vue</files>
  <action>
    1. Create folder `frontend/src/components/my-tasks/` (writing the first file creates it).
    2. Create `frontend/src/components/my-tasks/OrgSelector.vue` as a Vue 3 SFC with `<template>`, `<script setup lang="ts">`, and `<style scoped>` sections.
    3. Template section: COPY BYTE-FOR-BYTE the `.org-cards-grid` block from MyTasksView.vue, including the "Все организации" all-orgs card and the `v-for="org in orgSummary"` per-org cards. Replace internal handler calls like `@click="selectOrg(null)"` with `@click="$emit('select-org', null)"` and `@click="selectOrg(org.org_id)"` with `@click="$emit('select-org', org.org_id)"`. Do NOT change visual markup, class names, icons, text.
    4. Script setup: define interface `OrgSummaryItem` with fields { org_id: number | null; org_name: string; task_count: number; purchase_count: number; unseen_count: number } (verify exact shape from MyTasksView.vue's orgSummary ref type). Declare `defineProps` typed as { orgSummary: OrgSummaryItem[]; selectedOrgId: number | null; orgCardsOpen: boolean }. Declare `defineEmits` with 'select-org' event carrying `orgId: number | null`.
    5. Style scoped: MOVE (cut from MyTasksView.vue style scoped, paste here) every selector that only targets elements inside this component — .org-cards-grid, .org-sel-card, .org-sel-card--all, .org-sel-card--active, .osc-* and any nested descendants. Per §3.2: if uncertain whether a selector is also used elsewhere, keep it in parent AND copy to child (duplication of scoped style is safe).
    6. Do NOT add any apiFetch call, no axios, no store import. This is pure presentation (D-14 + §3.3).
  </action>
  <verify>
    <automated>test -f frontend/src/components/my-tasks/OrgSelector.vue && grep -q "defineProps<" frontend/src/components/my-tasks/OrgSelector.vue && grep -q "defineEmits<" frontend/src/components/my-tasks/OrgSelector.vue && grep -q "select-org" frontend/src/components/my-tasks/OrgSelector.vue && grep -q "org-cards-grid" frontend/src/components/my-tasks/OrgSelector.vue && ! grep -q "apiFetch" frontend/src/components/my-tasks/OrgSelector.vue</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/my-tasks/OrgSelector.vue` exists
    - Contains `<script setup lang="ts">`
    - Contains `defineProps<`
    - Contains `defineEmits<`
    - Contains `select-org` emit name
    - Contains `org-cards-grid` class (in template OR style)
    - Does NOT contain `apiFetch` (`grep -c "apiFetch" frontend/src/components/my-tasks/OrgSelector.vue` returns 0)
    - `wc -l frontend/src/components/my-tasks/OrgSelector.vue` ≥ 150
  </acceptance_criteria>
  <done>
    OrgSelector.vue self-contained. Props + emits wired. No API calls. CSS scoped.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create OrgSummaryBar.vue (header counters + badges)</name>
  <read_first>
    - frontend/src/views/MyTasksView.vue (identify header-counter / badges bar block — typically above tab switcher, renders badges map such as { pending_consent, pending_approvals, unseen_comments })
    - frontend/src/components/my-tasks/OrgSelector.vue (pattern reference just created)
    - .planning/phases/16-refactor-monoliths/16-CONTEXT.md D-18
  </read_first>
  <files>frontend/src/components/my-tasks/OrgSummaryBar.vue</files>
  <action>
    1. Create `frontend/src/components/my-tasks/OrgSummaryBar.vue` as a Vue 3 SFC.
    2. Template: COPY BYTE-FOR-BYTE the header counters + badges bar block from MyTasksView.vue. This is the area rendering total task count, pending-consent badge, pending-approvals badge, consent-declines indicator, etc.
    3. Script setup: define interface `TaskBadges` matching the shape returned by `GET /api/tasks/badges` — inspect existing usage in MyTasksView.vue (ref `badges`) to confirm fields. Typical shape: { pending_consent_count: number; pending_approvals_count: number; consent_declines_count: number; unseen_comments_count: number }. Declare `defineProps` typed as { badges: TaskBadges; totalTasks: number; totalPurchases: number } — the exact props required by the markup.
    4. Emit ONLY if the bar has clickable badges that navigate — e.g. 'click-badge' (type: 'consent' | 'approvals' | 'declines'). Check MyTasksView.vue for @click handlers in this region.
    5. Style scoped: MOVE (or duplicate) selectors that target only this component's DOM.
    6. No apiFetch. Pure presentation.
  </action>
  <verify>
    <automated>test -f frontend/src/components/my-tasks/OrgSummaryBar.vue && grep -q "defineProps<" frontend/src/components/my-tasks/OrgSummaryBar.vue && grep -q "script setup lang=\"ts\"" frontend/src/components/my-tasks/OrgSummaryBar.vue && ! grep -q "apiFetch" frontend/src/components/my-tasks/OrgSummaryBar.vue</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/my-tasks/OrgSummaryBar.vue` exists
    - Contains `<script setup lang="ts">`
    - Contains `defineProps<`
    - Does NOT contain `apiFetch`
    - `wc -l frontend/src/components/my-tasks/OrgSummaryBar.vue` ≥ 100
  </acceptance_criteria>
  <done>
    OrgSummaryBar.vue self-contained. Props-only, no API.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire OrgSelector + OrgSummaryBar into MyTasksView.vue and verify build</name>
  <read_first>
    - frontend/src/views/MyTasksView.vue (current state)
    - frontend/src/components/my-tasks/OrgSelector.vue (just created)
    - frontend/src/components/my-tasks/OrgSummaryBar.vue (just created)
  </read_first>
  <files>frontend/src/views/MyTasksView.vue</files>
  <action>
    1. At top of `<script setup>` in MyTasksView.vue, add imports:
       ```
       import OrgSelector from '@/components/my-tasks/OrgSelector.vue'
       import OrgSummaryBar from '@/components/my-tasks/OrgSummaryBar.vue'
       ```
       (Use project's existing alias — `@` or relative path based on what surrounding imports use.)
    2. In template, REPLACE the original `.org-cards-grid` block with:
       ```
       <OrgSelector
         :org-summary="orgSummary"
         :selected-org-id="selectedOrgId"
         :org-cards-open="orgCardsOpen"
         @select-org="handleSelectOrg"
       />
       ```
       And REPLACE the header counters/badges block with:
       ```
       <OrgSummaryBar
         :badges="badges"
         :total-tasks="tasks.length"
         :total-purchases="purchases.length"
       />
       ```
       (Adjust prop bindings to match the exact refs/computeds used today.)
    3. Ensure a handler `handleSelectOrg(orgId: number | null)` exists in script setup — likely renamed from current `selectOrg` or equivalent. Its body remains the same (set selectedOrgId, refetch data).
    4. Remove from MyTasksView.vue `<style scoped>` the CSS selectors that were MOVED (not duplicated) to child components — only selectors that now have no parent-template consumer.
    5. Build check: `docker exec vsks_crm-frontend-1 sh -c "cd /app && npm run build"` exits 0 with zero TypeScript errors and zero warnings (REFACTOR-07).
    6. Line-count check: `wc -l frontend/src/views/MyTasksView.vue` — MUST be ≤ 1900 (was 2188; org selector ~200 + summary bar ~100 removed = ~300 lines saved).
    7. Push to deploy. Run E2E: `BASE_URL=http://85.239.53.155 npx playwright test e2e/02-all-pages.spec.ts` → pass (my-tasks loads, org cards clickable).
    8. Commit: `refactor(16-12): extract OrgSelector + OrgSummaryBar from MyTasksView` — files: OrgSelector.vue (new), OrgSummaryBar.vue (new), MyTasksView.vue (modified).
  </action>
  <verify>
    <automated>grep -q "import OrgSelector from" frontend/src/views/MyTasksView.vue && grep -q "import OrgSummaryBar from" frontend/src/views/MyTasksView.vue && grep -q "<OrgSelector" frontend/src/views/MyTasksView.vue && git log --oneline -1 | grep -qE "refactor\(16-12\): extract OrgSelector"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/views/MyTasksView.vue` contains `import OrgSelector from`
    - `frontend/src/views/MyTasksView.vue` contains `import OrgSummaryBar from`
    - `frontend/src/views/MyTasksView.vue` contains `<OrgSelector` in template
    - `frontend/src/views/MyTasksView.vue` contains `<OrgSummaryBar` in template
    - `wc -l frontend/src/views/MyTasksView.vue` ≤ 1900
    - `docker exec vsks_crm-frontend-1 sh -c "cd /app && npm run build"` exits 0
    - `BASE_URL=http://85.239.53.155 npx playwright test e2e/02-all-pages.spec.ts` exits 0
    - `git log --oneline -1` matches `^[a-f0-9]+ refactor\(16-12\): extract OrgSelector`
    - Commit diff: exactly 3 files (2 new Vue + 1 modified view)
  </acceptance_criteria>
  <done>
    OrgSelector + OrgSummaryBar mounted in MyTasksView. Build green. E2E green. Zero visual regression expected.
  </done>
</task>

</tasks>

<verification>
- `docker exec vsks_crm-frontend-1 sh -c "cd /app && npm run build"` exits 0 with 0 warnings (REFACTOR-07)
- `BASE_URL=http://85.239.53.155 npx playwright test e2e/02-all-pages.spec.ts` → pass
- `wc -l frontend/src/components/my-tasks/OrgSelector.vue` ≥ 150
- `wc -l frontend/src/components/my-tasks/OrgSummaryBar.vue` ≥ 100
- `wc -l frontend/src/views/MyTasksView.vue` ≤ 1900
- Manual: open /my-tasks in browser, verify org cards render visually identical (pixel-level) to pre-refactor.
</verification>

<success_criteria>
my-tasks folder created. Two leaf components extracted. MyTasksView smaller. Zero build warnings. E2E green. Commit: `refactor(16-12): extract OrgSelector + OrgSummaryBar from MyTasksView`.
</success_criteria>

<output>
After completion, create `.planning/phases/16-refactor-monoliths/16-12-SUMMARY.md`.
</output>
