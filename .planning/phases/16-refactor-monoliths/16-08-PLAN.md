---
phase: 16-refactor-monoliths
plan: 08
type: execute
wave: 2
depends_on: [16-01, 16-07]
files_modified:
  - backend/app/routers/task_comments.py
  - backend/app/routers/tasks.py
  - backend/app/__init__.py
autonomous: true
requirements: [REFACTOR-02, REFACTOR-04, REFACTOR-08]
must_haves:
  truths:
    - "GET /api/tasks/{id}/comments returns same shape as before refactor"
    - "POST /api/tasks/{id}/comments adds comment, response unchanged"
    - "DELETE /api/tasks/{id}/comments/{comment_id} behaves same"
    - "POST /api/tasks/{id}/broadcast still dispatches broadcast"
    - "POST /api/tasks/{id}/dismiss-field works"
    - "GET /api/tasks/broadcast/scopes returns same list"
    - "tasks.py no longer contains comment/broadcast/dismiss-field endpoint bodies"
  artifacts:
    - path: "backend/app/routers/task_comments.py"
      provides: "Comments + broadcast + dismiss-field + broadcast scopes router"
      min_lines: 250
  key_links:
    - from: "backend/app/__init__.py"
      to: "backend/app/routers/task_comments.py"
      via: "app.include_router(task_comments.router)"
      pattern: "app\\.include_router\\(task_comments\\.router\\)"
---

<objective>
Extract comments/broadcast/dismiss-field endpoints into `task_comments.py` per RESEARCH.md §1.2 extraction step 2. Zero cross-deps on other split modules (§1.2 confirms). Implements D-13.
</objective>

<execution_context>
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-refactor-monoliths/16-CONTEXT.md
@.planning/phases/16-refactor-monoliths/16-RESEARCH.md
@.planning/phases/16-refactor-monoliths/16-VALIDATION.md

<interfaces>
<!-- Endpoints to move (per §1.2): -->
<!--   GET/POST/DELETE /{id}/comments (comment CRUD) -->
<!--   POST /{id}/broadcast -->
<!--   POST /{id}/dismiss-field -->
<!--   GET /broadcast/scopes -->
<!-- Models needed: TaskComment, TaskChange, TaskFieldSeen, Task -->
<!-- No cross-module deps (no import of task_visibility, badges, delegation) — simplest extract. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create task_comments.py with comment/broadcast/dismiss-field endpoints</name>
  <read_first>
    - backend/app/routers/tasks.py (grep for `@router.get("/{id}/comments"`, `@router.post("/{id}/comments"`, `@router.delete("/{id}/comments`, `@router.post("/{id}/broadcast"`, `@router.post("/{id}/dismiss-field"`, `@router.get("/broadcast/scopes"`)
    - backend/app/routers/purchase_files.py (extraction pattern reference)
    - backend/app/routers/task_visibility.py (from Plan 16-07 — may need to import `_get_visible_user_ids` if broadcast uses it)
    - .planning/phases/16-refactor-monoliths/16-RESEARCH.md §1.2, §2.1 template
    - .planning/phases/16-refactor-monoliths/16-CONTEXT.md D-13
  </read_first>
  <files>backend/app/routers/task_comments.py</files>
  <action>
    1. Create `backend/app/routers/task_comments.py` with header:
       ```python
       from fastapi import APIRouter, Depends, HTTPException, Body
       from sqlalchemy import select, func, and_
       from sqlalchemy.ext.asyncio import AsyncSession
       from typing import Optional, List
       from datetime import datetime

       from app.database import get_db
       from app.models.task import Task
       from app.models.task_comment import TaskComment
       from app.models.task_change import TaskChange
       from app.models.task_field_seen import TaskFieldSeen
       from app.models.user import User
       from app.auth.jwt import get_current_user, require_role

       router = APIRouter(prefix="/api/tasks", tags=["task-comments"])
       ```
       Adjust model import paths if project uses different module structure.
    2. MOVE (cut from tasks.py, paste into task_comments.py) these endpoints + supporting helpers:
       - `GET /{id}/comments` (list comments for a task)
       - `POST /{id}/comments` (add comment)
       - `DELETE /{id}/comments/{comment_id}` (delete comment)
       - `POST /{id}/broadcast` (broadcast dispatch)
       - `POST /{id}/dismiss-field` (dismiss field-change marker)
       - `GET /broadcast/scopes` (list broadcast scopes)
       - Any private helper used exclusively by above endpoints (e.g., `_format_comment`, `_serialize_broadcast_scope` — inspect tasks.py for these)
    3. If any moved endpoint uses `_get_visible_user_ids` (e.g., broadcast needs visibility filter), add: `from app.routers.task_visibility import _get_visible_user_ids`.
    4. Preserve every `Depends(require_role(...))` exactly (G-03). Do NOT rename functions or URLs.
  </action>
  <verify>
    <automated>test -f backend/app/routers/task_comments.py && grep -q 'router = APIRouter(prefix="/api/tasks"' backend/app/routers/task_comments.py && grep -q '@router.get("/{id}/comments"' backend/app/routers/task_comments.py && grep -q '@router.post("/{id}/comments"' backend/app/routers/task_comments.py && grep -q '@router.post("/{id}/broadcast"' backend/app/routers/task_comments.py && grep -q '@router.post("/{id}/dismiss-field"' backend/app/routers/task_comments.py && grep -q '@router.get("/broadcast/scopes"' backend/app/routers/task_comments.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/routers/task_comments.py` exists
    - Contains `router = APIRouter(prefix="/api/tasks"`
    - Contains `@router.get("/{id}/comments"`
    - Contains `@router.post("/{id}/comments"`
    - Contains `@router.delete("/{id}/comments` (can be `/{comment_id}` suffix)
    - Contains `@router.post("/{id}/broadcast"`
    - Contains `@router.post("/{id}/dismiss-field"`
    - Contains `@router.get("/broadcast/scopes"`
    - `wc -l backend/app/routers/task_comments.py` ≥ 250 and ≤ 500
  </acceptance_criteria>
  <done>
    Comments module has all comment/broadcast/dismiss-field endpoints.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete moved endpoints from tasks.py</name>
  <read_first>
    - backend/app/routers/tasks.py (current state, post-16-07)
    - backend/app/routers/task_comments.py (confirm moved list)
  </read_first>
  <files>backend/app/routers/tasks.py</files>
  <action>
    1. Delete from tasks.py every endpoint moved in Task 1 + any helper only used by them.
    2. Grep to confirm zero residue:
       - `grep -c '@router.get("/{id}/comments"' backend/app/routers/tasks.py` returns 0
       - `grep -c '@router.post("/{id}/comments"' backend/app/routers/tasks.py` returns 0
       - `grep -c '@router.delete("/{id}/comments' backend/app/routers/tasks.py` returns 0
       - `grep -c '@router.post("/{id}/broadcast"' backend/app/routers/tasks.py` returns 0
       - `grep -c '@router.post("/{id}/dismiss-field"' backend/app/routers/tasks.py` returns 0
       - `grep -c '@router.get("/broadcast/scopes"' backend/app/routers/tasks.py` returns 0
    3. Check external callers: `grep -rn "from app.routers.tasks import.*broadcast\|comment" backend/` — update any hits.
  </action>
  <verify>
    <automated>! grep -nE '@router.(get|post|delete)\("/{id}/comments|@router.post\("/{id}/broadcast"|@router.post\("/{id}/dismiss-field"|@router.get\("/broadcast/scopes"' backend/app/routers/tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - All 6 comment/broadcast/dismiss-field decorators return 0 from grep on tasks.py
    - `wc -l backend/app/routers/tasks.py` ≤ 1200 (was ~1500 after Plan 16-07; comments is ~350 lines)
    - No other file imports removed identifiers from `app.routers.tasks`
  </acceptance_criteria>
  <done>
    tasks.py no longer owns comments/broadcast/dismiss logic.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register task_comments.router + verify + commit</name>
  <read_first>
    - backend/app/__init__.py (router registration block)
    - .planning/phases/16-refactor-monoliths/16-RESEARCH.md §5 insertion point (after tasks.router)
  </read_first>
  <files>backend/app/__init__.py</files>
  <action>
    1. In `backend/app/__init__.py`, add after `app.include_router(tasks.router)`:
       ```python
       from app.routers import task_comments
       app.include_router(task_comments.router)
       ```
       (Match existing import-batch style.)
    2. Rebuild: `docker compose build backend` exits 0.
    3. Run mount smoke: `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py::test_comments_mount -q` → passes.
    4. Push to deploy. E2E: `BASE_URL=http://85.239.53.155 npx playwright test e2e/02-all-pages.spec.ts e2e/05-orders.spec.ts` → pass.
    5. Commit: `refactor(16-08): extract task_comments from tasks` — files: task_comments.py (new), tasks.py (modified), app/__init__.py (modified).
  </action>
  <verify>
    <automated>grep -q "app.include_router(task_comments.router)" backend/app/__init__.py && git log --oneline -1 | grep -qE "refactor\(16-08\): extract task_comments"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/__init__.py` contains `app.include_router(task_comments.router)`
    - `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py::test_comments_mount -q` exits 0
    - `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` exits 0 (full suite)
    - `git log --oneline -1` matches `^[a-f0-9]+ refactor\(16-08\): extract task_comments`
    - Commit diff: exactly 3 files (task_comments.py new, tasks.py modified, app/__init__.py modified)
  </acceptance_criteria>
  <done>
    Comments router mounted. Atomic commit pushed. E2E green.
  </done>
</task>

</tasks>

<verification>
- `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` → all pass
- `BASE_URL=http://85.239.53.155 npx playwright test e2e/05-orders.spec.ts e2e/02-all-pages.spec.ts` → all pass
- `wc -l backend/app/routers/task_comments.py` ≥ 250 AND ≤ 500
- No circular import: `docker exec vsks_crm-backend-1 python -c "import app"` exits 0
</verification>

<success_criteria>
Comments cluster in own module. HTTP URLs frozen. Commit: `refactor(16-08): extract task_comments from tasks`.
</success_criteria>

<output>
After completion, create `.planning/phases/16-refactor-monoliths/16-08-SUMMARY.md`.
</output>
