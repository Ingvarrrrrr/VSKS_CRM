---
phase: 16-refactor-monoliths
plan: 06
type: execute
wave: 2
depends_on: [16-01, 16-04, 16-05]
files_modified:
  - backend/app/routers/purchase_transitions.py
  - backend/app/routers/purchases.py
  - backend/app/__init__.py
autonomous: true
requirements: [REFACTOR-01, REFACTOR-04, REFACTOR-08]
must_haves:
  truths:
    - "POST /api/purchases/{pid}/transition?status=X returns same shape/status as before refactor"
    - "POST /api/purchases/{pid}/convert-to-order still creates Order with linked Purchase"
    - "TRANSITION_REQUIRED dict lives in purchase_transitions.py"
    - "STATUS_ORDER REMAINS in purchases.py (G-08) and is imported back by transitions module"
    - "_check_budget + _assign_framework_seq imported from purchase_budget"
  artifacts:
    - path: "backend/app/routers/purchase_transitions.py"
      provides: "Router for transition and convert-to-order endpoints + TRANSITION_REQUIRED dict"
      min_lines: 200
  key_links:
    - from: "backend/app/__init__.py"
      to: "backend/app/routers/purchase_transitions.py"
      via: "app.include_router(purchase_transitions.router)"
      pattern: "app\\.include_router\\(purchase_transitions\\.router\\)"
    - from: "backend/app/routers/purchase_transitions.py"
      to: "backend/app/routers/purchase_budget.py"
      via: "imports _check_budget, _assign_framework_seq"
      pattern: "from app.routers.purchase_budget import"
    - from: "backend/app/routers/purchase_transitions.py"
      to: "backend/app/routers/purchases.py"
      via: "imports _purchase_to_full, STATUS_ORDER"
      pattern: "from app.routers.purchases import"
---

<objective>
Extract state-machine transitions into `purchase_transitions.py` per RESEARCH.md §1.1 step 5 — the LAST purchases.py extract. Endpoints: `POST /{pid}/transition`, `POST /{pid}/convert-to-order`. Move `TRANSITION_REQUIRED` dict (G-08). Keep `STATUS_ORDER` in purchases.py (G-08 — used by CRUD and members). Implements D-04.
</objective>

<execution_context>
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-refactor-monoliths/16-CONTEXT.md
@.planning/phases/16-refactor-monoliths/16-RESEARCH.md
@.planning/phases/16-refactor-monoliths/16-VALIDATION.md

<interfaces>
<!-- Current locations in purchases.py (post-16-05 state): -->
<!--   TRANSITION_REQUIRED ~ L373-377 (G-08) — MOVE to transitions module -->
<!--   STATUS_ORDER ~ L186 (G-08) — KEEP in purchases.py, import back from transitions -->
<!--   transition_status function (bound to POST /{pid}/transition) ~ L1016-1210 — 200 lines with 40+ branches per D-04 rationale -->
<!--   convert_service_note_to_order function (bound to POST /{pid}/convert-to-order) -->
<!-- Dependencies (already extracted): -->
<!--   purchase_budget (_check_budget, _assign_framework_seq, FRAMEWORK_TYPES) — Plan 16-04 -->
<!--   purchases (_purchase_to_full, STATUS_ORDER) — stays -->
<!--   contracts.ensure_contract_linked (already external) -->
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create purchase_transitions.py with transition + convert-to-order endpoints</name>
  <read_first>
    - backend/app/routers/purchases.py (current state — grep `def transition_status`, `def convert_service_note_to_order`, `TRANSITION_REQUIRED`)
    - backend/app/routers/purchase_budget.py (from Plan 16-04 — confirms import path for helpers)
    - backend/app/routers/purchase_members.py (from Plan 16-05 — reference pattern)
    - .planning/phases/16-refactor-monoliths/16-RESEARCH.md §1.1 step 5, §G-08, §G-09, §2.2 route ordering
    - .planning/phases/16-refactor-monoliths/16-CONTEXT.md D-04, D-22
  </read_first>
  <files>backend/app/routers/purchase_transitions.py</files>
  <action>
    1. Create `backend/app/routers/purchase_transitions.py` with header:
       ```python
       from fastapi import APIRouter, Depends, HTTPException, Query
       from sqlalchemy import select, func
       from sqlalchemy.ext.asyncio import AsyncSession
       from typing import Optional
       from datetime import datetime

       from app.database import get_db
       from app.models.purchase import Purchase
       from app.models.subsidy import Subsidy
       from app.auth.jwt import get_current_user, require_role
       from app.routers.purchase_budget import _check_budget, _assign_framework_seq, FRAMEWORK_TYPES
       from app.routers.purchases import _purchase_to_full, _item_to_out, STATUS_ORDER

       router = APIRouter(prefix="/api/purchases", tags=["purchase-transitions"])

       # G-08: TRANSITION_REQUIRED lives HERE (only transition endpoint uses it)
       ```
       Adjust model/import paths to match project conventions.
    2. MOVE from purchases.py into purchase_transitions.py:
       - Constant `TRANSITION_REQUIRED` dict (L373-377 per G-08)
       - Function `transition_status` (bound to `POST /{pid}/transition`)
       - Function `convert_service_note_to_order` (bound to `POST /{pid}/convert-to-order`)
       - Any `ensure_contract_linked` call — do NOT move that helper itself (it lives in contracts.py per RESEARCH §1.1); just ensure `from app.routers.contracts import ensure_contract_linked` import is added if used.
    3. Preserve EVERY `Depends(require_role(...))` exactly (G-03).
    4. Route ordering check (§2.2): transition endpoint uses specific path segments (`/{pid}/transition`), so registration order after `purchases.router` is safe — FastAPI resolves static segments before bare `/{pid}`.
    5. Do NOT change URLs. Do NOT change response shapes (D-22, D-23).
  </action>
  <verify>
    <automated>test -f backend/app/routers/purchase_transitions.py && grep -q 'router = APIRouter(prefix="/api/purchases"' backend/app/routers/purchase_transitions.py && grep -q '@router.post("/{pid}/transition"' backend/app/routers/purchase_transitions.py && grep -q '@router.post("/{pid}/convert-to-order"' backend/app/routers/purchase_transitions.py && grep -q "TRANSITION_REQUIRED" backend/app/routers/purchase_transitions.py && grep -q "from app.routers.purchase_budget import" backend/app/routers/purchase_transitions.py && grep -q "from app.routers.purchases import" backend/app/routers/purchase_transitions.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/routers/purchase_transitions.py` exists
    - Contains `router = APIRouter(prefix="/api/purchases"`
    - Contains `@router.post("/{pid}/transition"`
    - Contains `@router.post("/{pid}/convert-to-order"`
    - Contains `TRANSITION_REQUIRED` (dict definition)
    - Contains `from app.routers.purchase_budget import _check_budget`
    - Contains `from app.routers.purchases import _purchase_to_full`
    - Contains `STATUS_ORDER` (imported from purchases, not redefined — `grep -c "^STATUS_ORDER = " backend/app/routers/purchase_transitions.py` returns 0)
    - `wc -l backend/app/routers/purchase_transitions.py` ≥ 200 and ≤ 400
  </acceptance_criteria>
  <done>
    Transitions module self-contained. Imports budget helpers from purchase_budget, CRUD helpers from purchases.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete transition endpoints from purchases.py (keep STATUS_ORDER)</name>
  <read_first>
    - backend/app/routers/purchases.py (current state)
    - backend/app/routers/purchase_transitions.py (confirm moved list)
  </read_first>
  <files>backend/app/routers/purchases.py</files>
  <action>
    1. Delete from purchases.py:
       - `TRANSITION_REQUIRED` dict (lines L373-377)
       - Function `transition_status` (bound to `POST /{pid}/transition`)
       - Function `convert_service_note_to_order` (bound to `POST /{pid}/convert-to-order`)
    2. DO NOT delete:
       - `STATUS_ORDER` constant (L186 — G-08: CRUD + my-tasks still use it)
       - `_purchase_to_full`, `_item_to_out` (D-20 — central helpers)
       - `_check_budget` usage in CRUD create/update endpoints — those already imported from purchase_budget per Plan 16-04
    3. Grep to confirm:
       - `grep -c '@router.post("/{pid}/transition"' backend/app/routers/purchases.py` returns 0
       - `grep -c '@router.post("/{pid}/convert-to-order"' backend/app/routers/purchases.py` returns 0
       - `grep -c "TRANSITION_REQUIRED" backend/app/routers/purchases.py` returns 0
       - `grep -c "^STATUS_ORDER = " backend/app/routers/purchases.py` returns 1 (STAYS)
    4. Check external callers: `grep -rn "from app.routers.purchases import TRANSITION_REQUIRED" backend/` — if any, update to `from app.routers.purchase_transitions import TRANSITION_REQUIRED`.
  </action>
  <verify>
    <automated>! grep -nE '@router.post\("/{pid}/transition"|@router.post\("/{pid}/convert-to-order"|^TRANSITION_REQUIRED' backend/app/routers/purchases.py && grep -q "^STATUS_ORDER = " backend/app/routers/purchases.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@router.post("/{pid}/transition"' backend/app/routers/purchases.py` returns 0
    - `grep -c '@router.post("/{pid}/convert-to-order"' backend/app/routers/purchases.py` returns 0
    - `grep -c "^TRANSITION_REQUIRED" backend/app/routers/purchases.py` returns 0
    - `grep -c "^STATUS_ORDER = " backend/app/routers/purchases.py` returns 1 (still present)
    - `grep -c "def transition_status" backend/app/routers/purchases.py` returns 0
    - `wc -l backend/app/routers/purchases.py` ≤ 1550 (was ~1800 after Plan 16-05; transitions is ~250 lines)
  </acceptance_criteria>
  <done>
    purchases.py keeps STATUS_ORDER but has no transition logic.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register purchase_transitions.router + run full verification + commit</name>
  <read_first>
    - backend/app/__init__.py (post-16-05 state)
    - backend/tests/test_routers_mounted.py
    - .planning/phases/16-refactor-monoliths/16-RESEARCH.md §5 insertion point
  </read_first>
  <files>backend/app/__init__.py</files>
  <action>
    1. In `backend/app/__init__.py`, add after `app.include_router(purchase_members.router)` (from Plan 16-05):
       ```python
       from app.routers import purchase_transitions
       app.include_router(purchase_transitions.router)
       ```
       (Match project's existing import-batch style if applicable.)
    2. Rebuild: `docker compose build backend` exits 0.
    3. Verify no route conflicts — read Docker logs: `docker compose logs backend --tail 30` — MUST NOT contain `ValueError: Duplicate Operation` or similar FastAPI startup error (Pitfall 5).
    4. Run mount smoke: `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py::test_transitions_mount -q` → passes. Full: `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` → all pass.
    5. Push to deploy. Run full E2E: `BASE_URL=http://85.239.53.155 npx playwright test` → 70/70 pass.
    6. Commit: `refactor(16-06): extract purchase_transitions from purchases` — files: purchase_transitions.py (new), purchases.py (modified), app/__init__.py (modified).
    7. After this commit, purchases.py decomposition (REFACTOR-01) is COMPLETE. `wc -l backend/app/routers/purchases.py` MUST be ≤ 800 (D-03 target ~600).
  </action>
  <verify>
    <automated>grep -q "app.include_router(purchase_transitions.router)" backend/app/__init__.py && git log --oneline -1 | grep -qE "refactor\(16-06\): extract purchase_transitions" && [ $(wc -l < backend/app/routers/purchases.py) -le 1550 ]</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/__init__.py` contains `app.include_router(purchase_transitions.router)`
    - `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` exits 0 (all probes pass)
    - `docker compose logs backend --tail 30` contains NO `ValueError` or `Duplicate` mentions
    - `git log --oneline -1` matches `^[a-f0-9]+ refactor\(16-06\): extract purchase_transitions`
    - `wc -l backend/app/routers/purchases.py` ≤ 1550 (and target ≤ 800 after final pass; this plan may not yet hit target if other CRUD endpoints still there — acceptable per progressive extraction)
    - `BASE_URL=http://85.239.53.155 npx playwright test e2e/05-orders.spec.ts` exits 0
    - Full E2E suite: 70/70 pass on deploy
  </acceptance_criteria>
  <done>
    purchases.py decomposition complete. Transitions in own module. STATUS_ORDER preserved for cross-module use. No URL change. No startup conflict.
  </done>
</task>

</tasks>

<verification>
- `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` → all pass
- Full E2E on deploy: `BASE_URL=http://85.239.53.155 npx playwright test` → 70/70 pass
- `wc -l backend/app/routers/purchase_transitions.py` ≥ 200 AND ≤ 400
- Route-conflict check: `docker compose logs backend --tail 50 | grep -iE "duplicate|conflict|ValueError"` returns 0 hits
- Phase 15 smoke still green: `npx playwright test e2e/18-purchase-items-editor.spec.ts`
</verification>

<success_criteria>
purchases.py ≤ ~1500 lines post-06 (D-03 target ≤ 800 may need additional CRUD cleanup — assess in 16-06 SUMMARY). 5 purchases-sub-modules mounted. E2E 70/70. Commit: `refactor(16-06): extract purchase_transitions from purchases`.
</success_criteria>

<output>
After completion, create `.planning/phases/16-refactor-monoliths/16-06-SUMMARY.md`. If purchases.py still exceeds 800 lines, document remaining candidates for extraction in SUMMARY (potential follow-up plan).
</output>
