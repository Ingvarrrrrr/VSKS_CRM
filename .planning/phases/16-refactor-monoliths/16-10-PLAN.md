---
phase: 16-refactor-monoliths
plan: 10
type: execute
wave: 2
depends_on: [16-01, 16-07]
files_modified:
  - backend/app/routers/task_delegation.py
  - backend/app/routers/tasks.py
  - backend/app/__init__.py
autonomous: true
requirements: [REFACTOR-02, REFACTOR-04, REFACTOR-08]
must_haves:
  truths:
    - "GET /api/tasks/pending-consent returns same shape as before"
    - "POST /api/tasks/{id}/consent still accepts/declines delegation"
    - "GET /api/tasks/consent-declines returns list of declined delegations"
    - "POST /api/tasks/consent-declines/{id}/acknowledge works"
    - "_set_assignees importable from app.routers.task_delegation"
    - "tasks.py imports _set_assignees back from task_delegation"
  artifacts:
    - path: "backend/app/routers/task_delegation.py"
      provides: "Router: pending-consent, consent, consent-declines + _set_assignees helper"
      min_lines: 250
  key_links:
    - from: "backend/app/__init__.py"
      to: "backend/app/routers/task_delegation.py"
      via: "app.include_router(task_delegation.router)"
      pattern: "app\\.include_router\\(task_delegation\\.router\\)"
    - from: "backend/app/routers/tasks.py"
      to: "backend/app/routers/task_delegation.py"
      via: "imports _set_assignees"
      pattern: "from app.routers.task_delegation import"
---

<objective>
Extract delegation/consent endpoints + `_set_assignees` helper into `task_delegation.py` per RESEARCH.md §1.2 extraction step 4. Endpoints: `GET /pending-consent`, `POST /{id}/consent`, `GET /consent-declines`, `POST /consent-declines/{id}/acknowledge`. Helper `_set_assignees` stays here (called by tasks.py CRUD create/update via import). Implements D-12.
</objective>

<execution_context>
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-refactor-monoliths/16-CONTEXT.md
@.planning/phases/16-refactor-monoliths/16-RESEARCH.md
@.planning/phases/16-refactor-monoliths/16-VALIDATION.md

<interfaces>
<!-- Endpoints (per RESEARCH §1.2): -->
<!--   GET /pending-consent -->
<!--   POST /{id}/consent -->
<!--   GET /consent-declines -->
<!--   POST /consent-declines/{id}/acknowledge -->
<!-- Helpers in this module: -->
<!--   _set_assignees (called by tasks.py CRUD — tasks.py imports back) -->
<!-- External deps: -->
<!--   from task_visibility: _get_visible_user_ids, _enrich_tasks, _create_task_chat_room (for notification on consent accept) -->
<!-- Cross-edges: tasks → task_delegation (import only), task_delegation → task_visibility (import only). No cycles. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create task_delegation.py with consent endpoints + _set_assignees helper</name>
  <read_first>
    - backend/app/routers/tasks.py (grep for `@router.get("/pending-consent"`, `@router.post("/{id}/consent"`, `@router.get("/consent-declines"`, `@router.post("/consent-declines/{id}/acknowledge"`, `def _set_assignees`)
    - backend/app/routers/task_visibility.py (Plan 16-07 — confirms helper imports)
    - backend/app/routers/task_badges.py (Plan 16-09, if landed — pattern)
    - .planning/phases/16-refactor-monoliths/16-RESEARCH.md §1.2 step 4
    - .planning/phases/16-refactor-monoliths/16-CONTEXT.md D-12
  </read_first>
  <files>backend/app/routers/task_delegation.py</files>
  <action>
    1. Create `backend/app/routers/task_delegation.py` with header:
       ```python
       from fastapi import APIRouter, Depends, HTTPException, Body
       from sqlalchemy import select, func, and_
       from sqlalchemy.ext.asyncio import AsyncSession
       from typing import Optional, List
       from datetime import datetime

       from app.database import get_db
       from app.models.task import Task
       from app.models.user import User
       from app.models.task_assignee import TaskAssignee  # adjust to actual model
       from app.auth.jwt import get_current_user, require_role
       from app.routers.task_visibility import _get_visible_user_ids, _enrich_tasks, _create_task_chat_room

       router = APIRouter(prefix="/api/tasks", tags=["task-delegation"])
       ```
       Adjust model imports to actual project paths.
    2. MOVE (cut from tasks.py, paste into task_delegation.py) these identifiers:
       - Helper `_set_assignees` (used by tasks.py CRUD create/update — tasks.py will import back)
       - Endpoint `GET /pending-consent`
       - Endpoint `POST /{id}/consent`
       - Endpoint `GET /consent-declines`
       - Endpoint `POST /consent-declines/{id}/acknowledge`
       - Any private helper used ONLY by above (e.g., `_notify_consent_decision`, `_format_pending` — inspect tasks.py)
    3. Preserve every `Depends(require_role(...))` exactly. Do NOT rename URLs or response keys.
    4. Keep underscore prefix on `_set_assignees` per §2.3.
  </action>
  <verify>
    <automated>test -f backend/app/routers/task_delegation.py && grep -q 'router = APIRouter(prefix="/api/tasks"' backend/app/routers/task_delegation.py && grep -q '@router.get("/pending-consent"' backend/app/routers/task_delegation.py && grep -q '@router.post("/{id}/consent"' backend/app/routers/task_delegation.py && grep -q '@router.get("/consent-declines"' backend/app/routers/task_delegation.py && grep -q '@router.post("/consent-declines/{id}/acknowledge"' backend/app/routers/task_delegation.py && grep -q "def _set_assignees" backend/app/routers/task_delegation.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/routers/task_delegation.py` exists
    - Contains `router = APIRouter(prefix="/api/tasks"`
    - Contains `@router.get("/pending-consent"`
    - Contains `@router.post("/{id}/consent"`
    - Contains `@router.get("/consent-declines"`
    - Contains `@router.post("/consent-declines/{id}/acknowledge"`
    - Contains `def _set_assignees` (or `async def _set_assignees`)
    - Contains `from app.routers.task_visibility import` (at least `_get_visible_user_ids`)
    - `wc -l backend/app/routers/task_delegation.py` ≥ 250 and ≤ 500
  </acceptance_criteria>
  <done>
    Delegation module has all consent endpoints + _set_assignees.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete moved endpoints from tasks.py and import _set_assignees back</name>
  <read_first>
    - backend/app/routers/tasks.py (current state)
    - backend/app/routers/task_delegation.py (confirm moved list)
  </read_first>
  <files>backend/app/routers/tasks.py</files>
  <action>
    1. Delete from tasks.py:
       - `@router.get("/pending-consent")` handler
       - `@router.post("/{id}/consent")` handler
       - `@router.get("/consent-declines")` handler
       - `@router.post("/consent-declines/{id}/acknowledge")` handler
       - `def _set_assignees(...)` body (function definition itself)
       - Any delegation-only private helpers
    2. Add import near top of tasks.py (alongside existing `from app.routers.task_visibility import ...` from Plan 16-07):
       ```python
       from app.routers.task_delegation import _set_assignees
       ```
    3. Verify tasks.py CRUD endpoints (create_task, update_task) still reference `_set_assignees(...)` — the import brings it into scope.
    4. External caller check: `grep -rn "from app.routers.tasks import _set_assignees\|pending_consent" backend/` — update any hits to point to `task_delegation`.
    5. Grep zero-residue confirmation:
       - `grep -c '@router.get("/pending-consent"' backend/app/routers/tasks.py` returns 0
       - `grep -c '@router.post("/{id}/consent"' backend/app/routers/tasks.py` returns 0
       - `grep -c "^def _set_assignees\|^async def _set_assignees" backend/app/routers/tasks.py` returns 0
  </action>
  <verify>
    <automated>! grep -nE '@router.get\("/pending-consent"|@router.post\("/{id}/consent"|@router.get\("/consent-declines"|^(async )?def _set_assignees' backend/app/routers/tasks.py && grep -q "from app.routers.task_delegation import" backend/app/routers/tasks.py</automated>
  </verify>
  <acceptance_criteria>
    - All 4 consent/pending decorators return 0 from grep on tasks.py
    - `grep -cE "^(async )?def _set_assignees" backend/app/routers/tasks.py` returns 0
    - `grep -c "from app.routers.task_delegation import" backend/app/routers/tasks.py` returns 1
    - `wc -l backend/app/routers/tasks.py` ≤ 650 (was ~900 after Plan 16-09; delegation is ~350 lines)
  </acceptance_criteria>
  <done>
    tasks.py no longer owns consent/delegation; imports _set_assignees back.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register task_delegation.router + verify + commit</name>
  <read_first>
    - backend/app/__init__.py (registration block)
  </read_first>
  <files>backend/app/__init__.py</files>
  <action>
    1. In `backend/app/__init__.py`, add after `app.include_router(task_badges.router)` (Plan 16-09):
       ```python
       from app.routers import task_delegation
       app.include_router(task_delegation.router)
       ```
       (Match existing import-batch style.)
    2. Rebuild: `docker compose build backend` exits 0.
    3. Mount smoke: `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py::test_delegation_mount -q` → passes. Full: `pytest /app/tests/test_routers_mounted.py -q` → all pass.
    4. Push to deploy. E2E fast subset: `BASE_URL=http://85.239.53.155 npx playwright test e2e/02-all-pages.spec.ts e2e/05-orders.spec.ts` → pass (consent flow intact).
    5. Commit: `refactor(16-10): extract task_delegation from tasks` — files: task_delegation.py (new), tasks.py (modified), app/__init__.py (modified).
  </action>
  <verify>
    <automated>grep -q "app.include_router(task_delegation.router)" backend/app/__init__.py && git log --oneline -1 | grep -qE "refactor\(16-10\): extract task_delegation"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/__init__.py` contains `app.include_router(task_delegation.router)`
    - `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py::test_delegation_mount -q` exits 0
    - `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` exits 0
    - `git log --oneline -1` matches `^[a-f0-9]+ refactor\(16-10\): extract task_delegation`
    - Commit diff: exactly 3 files
  </acceptance_criteria>
  <done>
    Delegation router mounted. Atomic commit pushed. Consent flow preserved.
  </done>
</task>

</tasks>

<verification>
- `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` → all pass
- `BASE_URL=http://85.239.53.155 npx playwright test e2e/02-all-pages.spec.ts e2e/05-orders.spec.ts` → all pass
- `wc -l backend/app/routers/task_delegation.py` ≥ 250 AND ≤ 500
- No circular import: `docker exec vsks_crm-backend-1 python -c "import app"` exits 0
</verification>

<success_criteria>
Delegation cluster in own module. HTTP URLs frozen. Commit: `refactor(16-10): extract task_delegation from tasks`.
</success_criteria>

<output>
After completion, create `.planning/phases/16-refactor-monoliths/16-10-SUMMARY.md`.
</output>
