---
phase: 16-refactor-monoliths
plan: 15
type: execute
wave: 6
depends_on: [16-13, 16-14]
files_modified:
  - frontend/src/views/MyTasksView.vue
  - .planning/phases/16-refactor-monoliths/16-15-UAT.md
autonomous: false
requirements: [REFACTOR-03, REFACTOR-05, REFACTOR-07]
must_haves:
  truths:
    - "MyTasksView.vue ≤ 600 lines (D-14 hard limit)"
    - "MyTasksView.vue is pure orchestrator: router + state + apiFetch + tab switching + dialog wiring — no inline tables/kanban/cards"
    - "All 5 child components mounted: OrgSelector, OrgSummaryBar, TasksTable, TasksKanban, PurchasesTable, PurchasesKanban"
    - "Full E2E 70/70 GREEN on deploy"
    - "npm run build exits 0 with ZERO warnings"
    - "Human visual UAT sign-off captured in 16-15-UAT.md (status: approved)"
  artifacts:
    - path: "frontend/src/views/MyTasksView.vue"
      provides: "Slim orchestrator: no inline component bodies, only imports + state + handlers + child composition"
      max_lines: 600
    - path: ".planning/phases/16-refactor-monoliths/16-15-UAT.md"
      provides: "Before/after screenshot comparison + manual UAT checklist (status: approved/needs-fix)"
      min_lines: 30
  key_links:
    - from: "frontend/src/views/MyTasksView.vue"
      to: "frontend/src/components/my-tasks/*"
      via: "imports all 6 child components + composes them in template"
      pattern: "import.*from '@/components/my-tasks/"
---

<objective>
Final frontend plan — slim MyTasksView.vue to ≤ 600 lines (D-14 target). Remove ANY residual inline markup that was duplicated/left behind by Plans 16-12..16-14. Human checkpoint: visual diff sign-off in 16-15-UAT.md before phase verify can green-light REFACTOR-03. REQUIRES full E2E 70/70 green. Implements D-14 finalization + D-28 manual visual snapshot.
</objective>

<execution_context>
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-refactor-monoliths/16-CONTEXT.md
@.planning/phases/16-refactor-monoliths/16-RESEARCH.md
@.planning/phases/16-refactor-monoliths/16-VALIDATION.md

<interfaces>
After Plans 16-12..16-14, MyTasksView.vue should contain:
- Template: top-level shell + OrgSummaryBar + OrgSelector + tab switcher + conditional TasksTable/TasksKanban + conditional PurchasesTable/PurchasesKanban + global TaskDetailDialog (may remain inline or be further extracted — discretion).
- Script: router setup, state refs (selectedOrgId, orgSummary, tasks, purchases, badges, viewMode, taskViewMode, activeTab, loading, dialog state), apiFetch functions (loadOrgSummary, loadTasks, loadPurchases, loadBadges, loadPendingConsent), handlers (handleSelectOrg, handleUpdateTaskStatus, handleUpdateKanbanStatus, openTaskDialog, openPurchaseDialog).
- Style: only global layout + any shared selectors still used by direct children in this file.

Hard gate: `wc -l frontend/src/views/MyTasksView.vue` MUST be ≤ 600.
Manual UAT gate: human must view /my-tasks at BASE_URL, compare against pre-refactor screenshot, approve in 16-15-UAT.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Slim MyTasksView.vue to ≤ 600 lines (orchestrator only)</name>
  <read_first>
    - frontend/src/views/MyTasksView.vue (current state post-16-14)
    - frontend/src/components/my-tasks/OrgSelector.vue (Plan 16-12)
    - frontend/src/components/my-tasks/OrgSummaryBar.vue (Plan 16-12)
    - frontend/src/components/my-tasks/TasksTable.vue (Plan 16-13)
    - frontend/src/components/my-tasks/TasksKanban.vue (Plan 16-13)
    - frontend/src/components/my-tasks/PurchasesTable.vue (Plan 16-14)
    - frontend/src/components/my-tasks/PurchasesKanban.vue (Plan 16-14)
    - .planning/phases/16-refactor-monoliths/16-CONTEXT.md D-14
  </read_first>
  <files>frontend/src/views/MyTasksView.vue</files>
  <action>
    1. Line-count audit: `wc -l frontend/src/views/MyTasksView.vue`. If > 600, proceed with slim; if ≤ 600, skip to Task 2.
    2. Template slim:
       - Remove any `v-data-table` of tasks or purchases that should already be in child components (search: `grep -n "v-data-table" frontend/src/views/MyTasksView.vue` — if hits remain, investigate duplication and remove).
       - Remove residual kanban column markup (search: `grep -n "kanban-column\|kanban-card" frontend/src/views/MyTasksView.vue` — remove if already in children).
       - Remove residual org-card markup (search: `grep -n "org-cards-grid\|org-sel-card" frontend/src/views/MyTasksView.vue` — remove if already in OrgSelector).
    3. Script slim:
       - Remove any helper function whose only caller was a template block that's now a child component (e.g., if `formatTaskStatus` was only used by TasksTable — move definition into TasksTable in revision commit).
       - Keep only: router setup, state refs, apiFetch functions, orchestration handlers, dialog controls.
    4. Style slim:
       - Remove CSS selectors that no longer have matching template elements in THIS file (they were moved to children's scoped styles).
       - Keep: global layout (.my-tasks-view, .tab-wrapper, etc.) and any selector targeting directly-rendered elements in MyTasksView template.
    5. Re-run: `wc -l frontend/src/views/MyTasksView.vue` MUST now be ≤ 600.
    6. Build: `docker exec vsks_crm-frontend-1 sh -c "cd /app && npm run build"` exits 0 with 0 warnings.
  </action>
  <verify>
    <automated>[ $(wc -l < frontend/src/views/MyTasksView.vue) -le 600 ] && ! grep -q "kanban-column" frontend/src/views/MyTasksView.vue && ! grep -q "org-cards-grid" frontend/src/views/MyTasksView.vue</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l frontend/src/views/MyTasksView.vue` ≤ 600
    - `grep -c "v-data-table" frontend/src/views/MyTasksView.vue` returns 0 (all tables now in child components)
    - `grep -c "org-cards-grid" frontend/src/views/MyTasksView.vue` returns 0 (in OrgSelector)
    - `grep -c "kanban-column" frontend/src/views/MyTasksView.vue` returns 0 (in children)
    - `grep -c "import.*from '@/components/my-tasks/" frontend/src/views/MyTasksView.vue` ≥ 6 (all 6 children imported)
    - `docker exec vsks_crm-frontend-1 sh -c "cd /app && npm run build"` exits 0 with 0 warnings
  </acceptance_criteria>
  <done>
    MyTasksView is a slim orchestrator. All 6 children imported. D-14 hard target met.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual UAT — side-by-side comparison + sign-off</name>
  <what-built>
    MyTasksView.vue decomposed into 1 orchestrator + 6 child components across Plans 16-12..16-15. No functional or visual changes expected; only file layout changed.
  </what-built>
  <how-to-verify>
    Before starting this task, the user (or Claude-on-behalf) should have a baseline screenshot of `/my-tasks` taken BEFORE Plan 16-12 started (per VALIDATION.md Manual-Only Verifications). If missing, take best-effort post-refactor screenshot and compare against git-history / memory.

    Steps:

    1. Open https://85.239.53.155/my-tasks (or local `npm run dev` build) in browser.
    2. Login as a regular user (not superadmin) so org-selector renders naturally.
    3. Visual checklist (TICK each manually in 16-15-UAT.md):
       - [ ] Org cards grid renders identically: all-orgs card + per-org cards, counters visible, hover states unchanged
       - [ ] Clicking an org card selects it (highlight applied, tasks/purchases filter to that org)
       - [ ] Tab switcher (Задачи / Закупки / Report) — tabs visible, active indicator styled correctly
       - [ ] Tasks tab → list mode: v-data-table renders columns in same order, row hover + click opens task dialog
       - [ ] Tasks tab → kanban mode: status columns visible in correct order, drag-drop a task between columns triggers backend PATCH and persists
       - [ ] Purchases tab → list mode: v-data-table columns identical
       - [ ] Purchases tab → kanban mode: drag-drop a purchase updates kanban_status
       - [ ] Header counters / badges bar (OrgSummaryBar): pending-consent, pending-approvals, consent-declines all render with correct counts
       - [ ] Consent card (if user has pending delegations): accept/decline buttons work
       - [ ] Task dialog (open any task): comments, broadcast button, dismiss-field all work
       - [ ] No console errors in devtools
       - [ ] No layout shifts / jank / spacing differences vs pre-refactor memory/screenshot
    4. Create/update `.planning/phases/16-refactor-monoliths/16-15-UAT.md` with:
       - frontmatter `status: approved` OR `status: needs-fix`
       - Checklist above with ticks
       - Screenshots embedded (optional but preferred): `screenshots/before.png` and `screenshots/after.png` under phase dir
       - If `needs-fix`: list specific issues → triggers `/gsd:plan-phase --gaps` for Plan 16-16+
    5. If APPROVED: commit `.planning/phases/16-refactor-monoliths/16-15-UAT.md` with message `docs(16-15): UAT sign-off for MyTasksView refactor`.
  </how-to-verify>
  <resume-signal>
    Type `approved` if visual UAT passes and 16-15-UAT.md is committed with status: approved.
    Type `needs-fix: <list of issues>` if any visual or functional regression found — orchestrator will plan gap closure.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Full E2E verification + final commit closing Phase 16</name>
  <read_first>
    - .planning/phases/16-refactor-monoliths/16-15-UAT.md (just approved)
  </read_first>
  <files>frontend/src/views/MyTasksView.vue</files>
  <action>
    1. Ensure 16-15-UAT.md has frontmatter `status: approved` (fail-loudly if not — block commit).
    2. Run full E2E on deploy: `BASE_URL=http://85.239.53.155 npx playwright test` → MUST be 70/70 pass (REFACTOR-05 hard gate).
    3. Run full backend smoke: `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` → all pass.
    4. Final build check: `docker exec vsks_crm-frontend-1 sh -c "cd /app && npm run build"` → 0 warnings.
    5. Commit: `refactor(16-15): slim MyTasksView to orchestrator + UAT approved` — files: MyTasksView.vue (modified), 16-15-UAT.md (if not already committed in Task 2).
    6. After this commit, Phase 16 implementation is complete. Orchestrator should proceed to `/gsd:verify-work 16` for formal phase verification.
  </action>
  <verify>
    <automated>grep -q "^status: approved" .planning/phases/16-refactor-monoliths/16-15-UAT.md && git log --oneline -1 | grep -qE "refactor\(16-15\): slim MyTasksView"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/16-refactor-monoliths/16-15-UAT.md` frontmatter `status: approved`
    - `BASE_URL=http://85.239.53.155 npx playwright test` exits 0 (70/70 pass)
    - `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` exits 0
    - `docker exec vsks_crm-frontend-1 sh -c "cd /app && npm run build"` exits 0 with 0 warnings
    - `wc -l frontend/src/views/MyTasksView.vue` ≤ 600
    - `git log --oneline -1` matches `^[a-f0-9]+ refactor\(16-15\): slim MyTasksView`
  </acceptance_criteria>
  <done>
    Phase 16 implementation complete. All 8 REFACTOR-* requirements green. Ready for `/gsd:verify-work 16`.
  </done>
</task>

</tasks>

<verification>
- `wc -l frontend/src/views/MyTasksView.vue` ≤ 600 (D-14 hard limit)
- `wc -l backend/app/routers/purchases.py` ≤ 800 (D-03 hard limit) — verify
- `wc -l backend/app/routers/tasks.py` ≤ 500 (D-09 hard limit) — verify
- `BASE_URL=http://85.239.53.155 npx playwright test` → 70/70 pass (REFACTOR-05)
- `docker exec vsks_crm-frontend-1 sh -c "cd /app && npm run build"` exits 0 with 0 warnings (REFACTOR-07)
- 16-15-UAT.md status: approved (REFACTOR-03 manual gate)
- `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` → all pass (REFACTOR-06)
</verification>

<success_criteria>
Phase 16 COMPLETE. MyTasksView.vue ≤ 600 lines. Visual UAT approved. Full E2E 70/70. Zero build warnings. All REFACTOR-01..REFACTOR-08 requirements GREEN. Commit: `refactor(16-15): slim MyTasksView to orchestrator + UAT approved`.
</success_criteria>

<output>
After completion, create `.planning/phases/16-refactor-monoliths/16-15-SUMMARY.md`. Document final line counts for ALL modified files (backend 11 modules + frontend 7 files). Note any deferred follow-ups (e.g., `backend/app/services/` business-layer as candidate for Phase 17 per CONTEXT.md deferred list).
</output>
