---
phase: 09-vnutrenniy-chat
plan: 04
type: execute
wave: 3
depends_on: [09-01, 09-02, 09-03]
files_modified:
  - frontend/src/views/ChatView.vue
  - frontend/src/composables/useChat.ts
  - frontend/src/router/index.ts
autonomous: true
requirements: [CHAT-01, CHAT-02, CHAT-04, CHAT-06, CHAT-07, CHAT-08, CHAT-10]

must_haves:
  truths:
    - "Пользователь открывает /chat и видит список своих чатов с именем, последним сообщением и счётчиком"
    - "Клик на чат — открывается история сообщений"
    - "Пользователь может отправить текстовое сообщение через поле ввода"
    - "Сообщение появляется у отправителя без перезагрузки страницы"
    - "WS соединение автоматически переподключается через 3 секунды после обрыва"
    - "Открытие чата вызывает POST /read — счётчик непрочитанных сбрасывается"
    - "Пользователь может отправить файл — он отображается в чате и скачивается по клику"
    - "Кнопка 'Новый чат' открывает диалог выбора сотрудника из GET /api/chat/staff"
    - "На мобильном (<md breakpoint) показывается либо список чатов, либо сообщения — не оба"
  artifacts:
    - path: "frontend/src/composables/useChat.ts"
      provides: "WebSocket composable: connect/disconnect/send/reactive messages"
      exports: ["useChat"]
    - path: "frontend/src/views/ChatView.vue"
      provides: "Главный вид чата: sidebar + message area + input"
  key_links:
    - from: "frontend/src/composables/useChat.ts"
      to: "/api/ws/chat"
      via: "new WebSocket(`${protocol}://${location.host}/api/ws/chat?token=${token}`)"
      pattern: "api/ws/chat"
    - from: "frontend/src/views/ChatView.vue"
      to: "/api/chat/rooms"
      via: "apiFetch('/chat/rooms')"
      pattern: "chat/rooms"
    - from: "frontend/src/router/index.ts"
      to: "frontend/src/views/ChatView.vue"
      via: "{ path: '/chat', component: ChatView }"
      pattern: "path.*chat"
---

<objective>
Создать Vue 3 фронтенд чата: composable для WebSocket соединения, ChatView.vue с sidebar и message area, запись в роутер.

Purpose: Пользователь должен иметь интерфейс для общения. Без этого плана чат существует только как API.
Output: Полнофункциональный /chat маршрут с Telegram-подобным интерфейсом.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-vnutrenniy-chat/09-RESEARCH.md
@.planning/phases/09-vnutrenniy-chat/09-03-SUMMARY.md

<interfaces>
<!-- Существующие паттерны фронтенда -->

From frontend/src/composables/ (project pattern):
```typescript
// apiFetch — существующий helper для API вызовов
import { apiFetch } from '@/utils/api'  // или аналогичный путь

// Проверить реальный путь:
// grep -r "apiFetch\|export.*apiFetch" frontend/src/utils/ frontend/src/composables/
```

From frontend/src/router/index.ts (existing routes pattern):
```typescript
{ path: '/chat', meta: { requiresAuth: true }, component: () => import('../views/ChatView.vue') }
// Добавить по аналогии с другими lazy-loaded маршрутами
```

WS connection pattern (from RESEARCH.md):
```typescript
const protocol = location.protocol === 'https:' ? 'wss' : 'ws'
const ws = new WebSocket(`${protocol}://${location.host}/api/ws/chat?token=${localStorage.getItem('auth_token')}`)
```

WS message envelope (from RESEARCH.md):
```typescript
// Incoming from server:
{ type: 'message', room_id: 42, message: {id, sender_id, sender_name, content, file_name, created_at, has_file} }
{ type: 'read', room_id: 42, user_id: 7, last_read_message_id: 1001 }
{ type: 'unread_count', total_unread: 3 }
```

REST API endpoints (from Plan 03):
```
GET  /api/chat/rooms                      → RoomOut[]
POST /api/chat/rooms/direct               body: {target_user_id}
POST /api/chat/rooms                      body: {name, participant_ids}
GET  /api/chat/rooms/{id}/messages        ?before_id=&limit=50
POST /api/chat/rooms/{id}/messages        multipart: content?, file?
POST /api/chat/rooms/{id}/read
GET  /api/chat/staff                      → [{id, full_name, username, avatar, department, position}]
GET  /api/chat/rooms/{id}/files/{msg_id}  → file download
```

Vuetify components to use (from RESEARCH.md):
- v-navigation-drawer (permanent on desktop, temporary on mobile)
- v-list + v-list-item with v-avatar for chat list
- v-virtual-scroll for message list
- v-text-field with append-inner-icon for input
- v-badge for unread count
- v-dialog for new chat creation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useChat.ts composable</name>
  <files>frontend/src/composables/useChat.ts</files>
  <action>
Создать `frontend/src/composables/useChat.ts`:

Composable управляет:
1. WebSocket соединением (connect/disconnect/reconnect)
2. Глобальным счётчиком непрочитанных (`totalUnread` ref — обновляется по WS событию unread_count)
3. Обработкой входящих событий от сервера

```typescript
import { ref, onMounted, onUnmounted } from 'vue'

// Global state — shared across components via module-level refs
export const totalUnread = ref(0)
export const wsConnected = ref(false)

// Callbacks registered by ChatView to receive events
type MessageCallback = (event: { type: string; room_id?: number; message?: any; user_id?: number; last_read_message_id?: number; total_unread?: number }) => void
const listeners: MessageCallback[] = []

export function onChatEvent(cb: MessageCallback) {
  listeners.push(cb)
  return () => { const i = listeners.indexOf(cb); if (i >= 0) listeners.splice(i, 1) }
}

let ws: WebSocket | null = null
let reconnectTimer: ReturnType<typeof setTimeout> | null = null
let pingTimer: ReturnType<typeof setInterval> | null = null

function connect() {
  const token = localStorage.getItem('auth_token')
  if (!token) return

  const protocol = location.protocol === 'https:' ? 'wss' : 'ws'
  ws = new WebSocket(`${protocol}://${location.host}/api/ws/chat?token=${token}`)

  ws.onopen = () => {
    wsConnected.value = true
    // Ping every 30s to keep WS alive
    pingTimer = setInterval(() => ws?.readyState === WebSocket.OPEN && ws.send('ping'), 30000)
  }

  ws.onmessage = (e) => {
    if (e.data === 'pong') return
    try {
      const event = JSON.parse(e.data)
      if (event.type === 'unread_count') totalUnread.value = event.total_unread ?? 0
      listeners.forEach(cb => cb(event))
    } catch {}
  }

  ws.onclose = (e) => {
    wsConnected.value = false
    if (pingTimer) { clearInterval(pingTimer); pingTimer = null }
    // Close code 4001 = auth error, don't reconnect
    if (e.code !== 4001) {
      reconnectTimer = setTimeout(connect, 3000)
    }
  }

  ws.onerror = () => { ws?.close() }
}

function disconnect() {
  if (reconnectTimer) { clearTimeout(reconnectTimer); reconnectTimer = null }
  if (pingTimer) { clearInterval(pingTimer); pingTimer = null }
  ws?.close()
  ws = null
}

// useChat composable — call in App.vue or a persistent component to keep WS alive
export function useChat() {
  onMounted(connect)
  onUnmounted(disconnect)
  return { totalUnread, wsConnected }
}

// initChat — call once at app startup (from App.vue) without lifecycle binding
export function initChat() {
  connect()
}

export function destroyChat() {
  disconnect()
}
```
  </action>
  <verify>
    <automated>grep -c "totalUnread\|wsConnected\|onChatEvent\|initChat\|useChat" /c/Users/1/Desktop/Cursor/VSKS_CRM/frontend/src/composables/useChat.ts</automated>
  </verify>
  <done>Файл создан; содержит totalUnread и wsConnected как module-level refs; содержит onChatEvent для регистрации колбэков; содержит ping-pong логику; reconnect с 3s задержкой; код 4001 = no reconnect.</done>
</task>

<task type="auto">
  <name>Task 2: Create ChatView.vue</name>
  <files>frontend/src/views/ChatView.vue</files>
  <action>
Создать `frontend/src/views/ChatView.vue` — Telegram-подобный интерфейс на Vuetify 3.

**Структура компонента:**

```
┌──────────────────────────────────────────────────────────────┐
│  v-navigation-drawer (left, 320px, permanent desktop)        │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ "Чат" заголовок + кнопка "+" (новый чат)             │    │
│  │ v-list: v-list-item per room                         │    │
│  │   - v-avatar с первой буквой имени                   │    │
│  │   - title: room name или имя собеседника             │    │
│  │   - subtitle: последнее сообщение                    │    │
│  │   - v-badge: unread_count если > 0                   │    │
│  └──────────────────────────────────────────────────────┘    │
├──────────────────────────────────────────────────────────────┤
│  Основная область (flex column, 100% height)                 │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ Toolbar: имя/название комнаты + иконка группы        │    │
│  ├──────────────────────────────────────────────────────┤    │
│  │ v-virtual-scroll / обычный div с сообщениями         │    │
│  │   - пузыри: своё (right), чужое (left)               │    │
│  │   - имя отправителя для групповых                    │    │
│  │   - файл: иконка скрепки + имя файла (кликабельно)  │    │
│  ├──────────────────────────────────────────────────────┤    │
│  │ ChatInput: text-field + 📎 (file) + Send button      │    │
│  └──────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────┘
```

**Логика компонента (script setup):**

Refs: `rooms: ref<Room[]>([])`, `selectedRoom: ref<Room|null>(null)`, `messages: ref<Message[]>([])`, `inputText: ref('')`, `fileInput: ref<File|null>(null)`, `loadingMessages: ref(false)`, `showNewChatDialog: ref(false)`, `staff: ref([])`, `selectedStaffIds: ref<number[]>([])`, `newGroupName: ref('')`, `isGroup: ref(false)`, `sending: ref(false)`

**loadRooms():**
```typescript
const data = await apiFetch<Room[]>('/chat/rooms')
rooms.value = data
```

**selectRoom(room):**
```typescript
selectedRoom.value = room
messages.value = []
await loadMessages()
await markAsRead()
// На мобильном: переключиться в вид сообщений
```

**loadMessages(beforeId?: number):**
```typescript
const params = beforeId ? `?before_id=${beforeId}&limit=50` : '?limit=50'
const data = await apiFetch<Message[]>(`/chat/rooms/${selectedRoom.value.id}/messages${params}`)
if (beforeId) messages.value = [...data, ...messages.value]  // prepend
else messages.value = data
await nextTick(() => scrollToBottom())
```

**sendMessage():**
```typescript
if (!inputText.value.trim() && !fileInput.value) return
sending.value = true
const form = new FormData()
if (inputText.value.trim()) form.append('content', inputText.value.trim())
if (fileInput.value) form.append('file', fileInput.value)
await apiFetch(`/chat/rooms/${selectedRoom.value.id}/messages`, { method: 'POST', body: form })
inputText.value = ''
fileInput.value = null
sending.value = false
// Сервер пришлёт WS событие message — обработать в onChatEvent
```

**markAsRead():**
```typescript
await apiFetch(`/chat/rooms/${selectedRoom.value.id}/read`, { method: 'POST' })
selectedRoom.value.unread_count = 0
// WS событие unread_count обновит totalUnread автоматически
```

**WS event handler (onChatEvent):**
```typescript
onChatEvent((event) => {
  if (event.type === 'message') {
    if (event.room_id === selectedRoom.value?.id) {
      messages.value.push(event.message)
      nextTick(scrollToBottom)
      markAsRead()  // автоматически прочитать если чат открыт
    }
    // Обновить last_message в списке комнат
    const room = rooms.value.find(r => r.id === event.room_id)
    if (room) { room.last_message = event.message; /* переместить вверх */ }
  }
  if (event.type === 'unread_count') {
    // totalUnread обновляется автоматически в useChat.ts
    // Обновить счётчик конкретной комнаты через перезагрузку
    loadRooms()
  }
})
```

**Новый чат диалог:**
- GET /api/chat/staff → список сотрудников с поиском
- v-checkbox или v-select для выбора участников
- Если выбран 1 — POST /rooms/direct
- Если выбраны 2+ — показать поле "Название группы" + POST /rooms

**Pagination при скролле вверх:**
```typescript
function onScroll(e: Event) {
  const el = e.target as HTMLElement
  if (el.scrollTop < 50 && messages.value.length >= 50) {
    loadMessages(messages.value[0].id)
  }
}
```

**Стили:**
- Пузырь "моё": `bg-primary text-white border-radius` выровнен вправо
- Пузырь "чужое": `bg-surface-variant border-radius` выровнен влево
- Список чатов: hover effect, активный элемент bg-primary-lighten
- Файл в сообщении: кликабельный чип с иконкой mdi-paperclip, href = /api/chat/rooms/{id}/files/{msg_id}

**Мобильная адаптация:**
- `const showSidebar = computed(() => !selectedRoom.value || smAndUp.value)`
- На xs: navigation-drawer скрыт когда открыт чат, показана кнопка "←" для возврата
- Использовать `useDisplay()` из Vuetify для определения брейкпоинта

**onMounted:** loadRooms(); зарегистрировать onChatEvent
**onUnmounted:** снять регистрацию onChatEvent
  </action>
  <verify>
    <automated>grep -c "loadRooms\|selectRoom\|sendMessage\|markAsRead\|onChatEvent\|loadMessages\|showNewChatDialog" /c/Users/1/Desktop/Cursor/VSKS_CRM/frontend/src/views/ChatView.vue</automated>
  </verify>
  <done>Файл создан; содержит минимум 6 функций (loadRooms, selectRoom, sendMessage, markAsRead, loadMessages, onChatEvent handler); содержит v-navigation-drawer; содержит файловый input; содержит pagination при скролле вверх; содержит диалог нового чата.</done>
</task>

<task type="auto">
  <name>Task 3: Add /chat route to Vue Router</name>
  <files>frontend/src/router/index.ts</files>
  <action>
Открыть `frontend/src/router/index.ts`. Найти массив `routes: [`. Добавить новый маршрут перед закрывающей `]` массива routes:

```typescript
{
  path: '/chat',
  meta: { requiresAuth: true },
  component: () => import('../views/ChatView.vue'),
},
```

Добавить по аналогии с другими lazy-loaded маршрутами (например, StaffView.vue или ContractsView.vue).

ВАЖНО: Не трогать существующие маршруты, только добавить новый.
  </action>
  <verify>
    <automated>grep -n "path.*chat\|ChatView" /c/Users/1/Desktop/Cursor/VSKS_CRM/frontend/src/router/index.ts</automated>
  </verify>
  <done>grep находит строку с path: '/chat' и компонентом ChatView.vue в файле роутера.</done>
</task>

</tasks>

<verification>
1. `grep "path.*chat" frontend/src/router/index.ts` — маршрут добавлен
2. `grep -c "onChatEvent\|totalUnread\|wsConnected" frontend/src/composables/useChat.ts` — ≥ 3
3. `grep -c "v-navigation-drawer\|v-virtual-scroll\|sendMessage\|markAsRead" frontend/src/views/ChatView.vue` — ≥ 4
4. `grep "apiFetch.*chat\|/chat/rooms" frontend/src/views/ChatView.vue` — API вызовы присутствуют
5. `grep "useDisplay\|smAndUp\|mobile" frontend/src/views/ChatView.vue` — мобильная адаптация присутствует
</verification>

<success_criteria>
- /chat маршрут доступен в браузере после сборки
- ChatView отображает список комнат слева и сообщения справа (Telegram layout)
- Отправка сообщения работает без перезагрузки страницы
- WS соединение автопереподключается через 3s
- Открытие чата вызывает mark-as-read
- Файловые вложения: выбор через кнопку 📎, отображение как чип в сообщении, скачивание по клику
- Pagination: при скролле вверх загружаются более старые сообщения
- На мобильном: sidebar или messages — не оба одновременно
</success_criteria>

<output>
После завершения создать `.planning/phases/09-vnutrenniy-chat/09-04-SUMMARY.md`
</output>
