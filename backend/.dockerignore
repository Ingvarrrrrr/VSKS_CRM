# Локальная незакоммиченная alembic-миграция автопарка, оставшаяся от
# параллельного диалога (не в git, на проде её нет, это чужая незавершённая
# работа — не трогаем). Не удаляем с диска, но и не тащим в образ: иначе
# локальная сборка получает лишнюю alembic-голову, которой нет и не будет
# на проде. См. фикс 580377689b17/5324bd4399ee (2026-09-01).
#
# price_freshness_and_history (p9r2t5v8x1z4) исключать здесь больше не нужно:
# 2026-09-02 её down_revision починен на текущую единственную голову
# n4p6r8t0v2x4 (раньше указывал на q7r8s9t0u1v2, на которой уже сидела
# aa1b2c3d4e5f — это и давало вторую голову). Теперь это штатное продолжение
# цепочки, файл должен попадать в образ и применяться через alembic upgrade head.
alembic/versions/w2x3y4z5a6b7_autoblock_vehicle_fields.py
