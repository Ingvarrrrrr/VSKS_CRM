---
phase: 08-торговые-площадки
plan: "04"
type: execute
wave: 2
depends_on:
  - "08-01"
  - "08-02"
  - "08-03"
files_modified:
  - e2e/12-publications.spec.ts
autonomous: true
requirements:
  - PUB-08
  - PUB-09
  - PUB-10

must_haves:
  truths:
    - "Тест Фабрикант test mode проходит: создаёт publication, mock callback → status=published, chip 'Опубликовано' виден в UI"
    - "Тест Росэлторг error проходит: создаёт publication с procedure_type, mock callback → status=error, error_text виден в таблице публикаций"
    - "Тест SMTP endpoint проходит: POST /api/settings/smtp/test возвращает 200 или 400 (не 500)"
    - "Тест API Росэлторг с procedure_type проходит: POST с procedure_type принимается (не 422)"
    - "Все 4 теста в файле выполняются без timeout и без flakiness"
  artifacts:
    - path: "e2e/12-publications.spec.ts"
      provides: "4 E2E теста на публикации и email"
      contains: "mock callback"
  key_links:
    - from: "POST /api/publications/purchases/{id}"
      to: "PATCH /api/publications/{pub.id}/status"
      via: "прямой вызов в тесте (mock n8n callback)"
      pattern: "request.patch.*publications.*status"
    - from: "PATCH callback status=error"
      to: "UI таблица публикаций"
      via: "page.goto + waitForLoadState"
      pattern: "error_text.*Требуется Bearer"
---

<objective>
Написать E2E тесты на публикации и email в файле e2e/12-publications.spec.ts.

Purpose: Верифицировать что весь flow публикаций работает корректно — ошибочные сценарии показывают понятные сообщения пользователю. Акцент на тестировании error paths (решение заблокировано). Mock callback через прямой PATCH вместо ожидания n8n.
Output: 4 автоматизированных Playwright теста которые проходят в CI.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claire/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

<interfaces>
Из e2e/helpers.ts (существующие утилиты):
```typescript
export async function login(page: Page): Promise<void>
// Логинится как admin/admin123, ждёт загрузки

export async function collectApiErrors(page: Page): Promise<string[]>
// Собирает все API ошибки 4xx/5xx во время теста

export async function clickButton(page: Page, text: string): Promise<void>
// Кликает кнопку по тексту

export async function waitForOverlays(page: Page): Promise<void>
// Ждёт закрытия диалогов/overlay
```

Из e2e/05-orders.spec.ts (паттерн тестов):
```typescript
import { test, expect } from '@playwright/test'
import { login, collectApiErrors } from './helpers'

const BASE_URL = process.env.BASE_URL || 'http://localhost'

test.describe('Orders', () => {
  test.beforeEach(async ({ page }) => {
    await login(page)
  })
  // тесты...
})
```

API endpoints (из plan 08-01):
```
POST /api/publications/purchases/{purchase_id}
  body: { platform: string, procedure_type?: string }
  response: { id: number, status: string, ... }

PATCH /api/publications/{pub_id}/status
  body: { status: string, external_id?: string, external_url?: string, error_text?: string }
  response: { id, status, external_id, error_text, ... }

POST /api/settings/smtp/test?to_email={email}
  response: 200 OK или 400 с { detail: string }

GET /api/purchases?limit=5
  response: [{ id, subject, status, ... }, ...]
```

Токен из localStorage после login:
```typescript
// Паттерн получения токена из page context
const token = await page.evaluate(() => localStorage.getItem('auth_token'))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать e2e/12-publications.spec.ts с 4 тестами</name>
  <files>e2e/12-publications.spec.ts</files>
  <action>
Создать файл `e2e/12-publications.spec.ts` с 4 тестами. Паттерн из `e2e/05-orders.spec.ts`.

```typescript
import { test, expect } from '@playwright/test'
import { login } from './helpers'

const BASE_URL = process.env.BASE_URL || 'http://localhost'

test.describe('Publications & Email', () => {
  let purchaseId: number
  let token: string

  test.beforeAll(async ({ browser }) => {
    const page = await browser.newPage()
    await login(page)
    token = await page.evaluate(() => localStorage.getItem('auth_token') || '')

    // Получить первую закупку для тестов
    const resp = await page.request.get(`${BASE_URL}/api/purchases?limit=5`, {
      headers: { Authorization: `Bearer ${token}` },
    })
    expect(resp.ok()).toBeTruthy()
    const purchases = await resp.json()
    expect(purchases.length).toBeGreaterThan(0)
    purchaseId = purchases[0].id
    await page.close()
  })

  test('Фабрикант test mode: mock callback → status=published, chip виден в UI', async ({ page, request }) => {
    await login(page)
    const tok = await page.evaluate(() => localStorage.getItem('auth_token') || '')

    // 1. Создать публикацию на Фабрикант
    const pubResp = await request.post(`${BASE_URL}/api/publications/purchases/${purchaseId}`, {
      data: { platform: 'fabrikant' },
      headers: { Authorization: `Bearer ${tok}` },
    })
    expect(pubResp.status(), 'POST publication should return 200').toBe(200)
    const pub = await pubResp.json()
    expect(pub.id, 'publication должна иметь id').toBeTruthy()

    // 2. Mock n8n callback — симулируем ответ от Фабрикант (test mode success)
    const callbackResp = await request.patch(`${BASE_URL}/api/publications/${pub.id}/status`, {
      data: {
        status: 'published',
        external_id: 'TEST-FAB-' + Date.now(),
        external_url: 'https://fabrikant.ru/test-lot',
      },
      headers: { Authorization: `Bearer ${tok}` },
    })
    expect(callbackResp.status(), 'PATCH callback should return 200').toBe(200)
    const updated = await callbackResp.json()
    expect(updated.status).toBe('published')
    expect(updated.external_id).toMatch(/TEST-FAB-/)

    // 3. Проверить в UI что chip "Опубликовано" виден
    await page.goto(`/create-order/${purchaseId}`)
    await page.waitForLoadState('networkidle')
    await page.waitForTimeout(1000)

    // Ищем chip или текст "Опубликовано" в секции публикаций
    const publishedEl = page.locator('.v-chip, td, .v-table').filter({ hasText: /Опубликовано|published/i }).first()
    await expect(publishedEl, 'chip "Опубликовано" должен быть виден').toBeVisible({ timeout: 5000 })
  })

  test('Росэлторг — нет токена: error_text виден в таблице публикаций', async ({ page, request }) => {
    await login(page)
    const tok = await page.evaluate(() => localStorage.getItem('auth_token') || '')

    // 1. Создать публикацию на Росэлторг с procedure_type
    const pubResp = await request.post(`${BASE_URL}/api/publications/purchases/${purchaseId}`, {
      data: { platform: 'roseltorg_rb', procedure_type: 'request_quotations' },
      headers: { Authorization: `Bearer ${tok}` },
    })
    expect(pubResp.status(), 'POST roseltorg publication should return 200').toBe(200)
    const pub = await pubResp.json()

    // 2. Mock n8n callback — симулируем error (нет токена)
    const errorText = 'Требуется Bearer Token Росэлторг — настройте в параметрах n8n (переменная ROSELTORG_TOKEN)'
    const callbackResp = await request.patch(`${BASE_URL}/api/publications/${pub.id}/status`, {
      data: { status: 'error', error_text: errorText },
      headers: { Authorization: `Bearer ${tok}` },
    })
    expect(callbackResp.status(), 'PATCH error callback should return 200').toBe(200)
    const updated = await callbackResp.json()
    expect(updated.status).toBe('error')
    expect(updated.error_text).toContain('Требуется Bearer Token')

    // 3. Проверить в UI что error_text виден на странице закупки
    await page.goto(`/create-order/${purchaseId}`)
    await page.waitForLoadState('networkidle')
    await page.waitForTimeout(1000)

    // Error text должен быть виден где-то на странице (в таблице публикаций)
    const errorEl = page.locator('text=Требуется Bearer Token').first()
    await expect(errorEl, 'error_text "Требуется Bearer Token" должен быть виден в UI').toBeVisible({ timeout: 5000 })
  })

  test('Росэлторг API принимает procedure_type без ошибки валидации', async ({ request, page }) => {
    await login(page)
    const tok = await page.evaluate(() => localStorage.getItem('auth_token') || '')

    // POST с procedure_type не должен возвращать 422
    const resp = await request.post(`${BASE_URL}/api/publications/purchases/${purchaseId}`, {
      data: { platform: 'roseltorg_rb', procedure_type: 'auction' },
      headers: { Authorization: `Bearer ${tok}` },
    })
    // 200 = ok; 409 = уже опубликовано (тоже приемлемо в контексте теста)
    expect([200, 409], `Expected 200 or 409, got ${resp.status()}`).toContain(resp.status())
  })

  test('SMTP test endpoint отвечает без 500 ошибки', async ({ request, page }) => {
    await login(page)
    const tok = await page.evaluate(() => localStorage.getItem('auth_token') || '')

    // POST /api/settings/smtp/test
    // На сервере с настроенным SMTP → 200
    // На локальной машине без SMTP → 400 с понятным сообщением
    // В любом случае НЕ 500
    const resp = await request.post(
      `${BASE_URL}/api/settings/smtp/test?to_email=zakupki@vsks.ru`,
      { headers: { Authorization: `Bearer ${tok}` } }
    )
    expect(resp.status(), 'SMTP test endpoint должен вернуть 200 или 400, не 500').not.toBe(500)

    // Если 400 — проверить что в теле есть понятное сообщение (не пустое)
    if (resp.status() === 400) {
      const body = await resp.json()
      expect(body.detail || body.message || body.error, 'Сообщение об ошибке SMTP должно быть непустым').toBeTruthy()
    }
  })
})
```

ВАЖНО:
- `test.beforeAll` использует `{ browser }` fixture (не `{ page }`) для создания изолированной страницы
- Все API вызовы через `request` fixture (не `page.request`) для независимости от браузерного контекста — НО в beforeAll используем `page.request` т.к. там browser fixture
- Тесты независимы друг от друга (каждый делает собственный login)
- Mock callback — прямой PATCH без ожидания n8n (n8n может не работать в тестовом окружении)
- `purchaseId` получается из реальных данных через GET /api/purchases (не хардкод)
  </action>
  <verify>
    <automated>cd /c/Users/1/VSKS_CRM && npx playwright test e2e/12-publications.spec.ts --reporter=line 2>&1 | tail -20</automated>
  </verify>
  <done>Файл 12-publications.spec.ts создан, все 4 теста выполняются (passed или skipped), нет failed тестов из-за синтаксических ошибок или timeout</done>
</task>

</tasks>

<verification>
1. `ls e2e/12-publications.spec.ts` — файл существует
2. `npx playwright test e2e/12-publications.spec.ts --reporter=line 2>&1 | tail -30` — все тесты passed или known-fail с понятным сообщением
3. Нет TypeScript ошибок: `npx tsc --noEmit --project playwright.config.ts 2>&1 | head -10`
</verification>

<success_criteria>
- 4 E2E теста в файле: Фабрикант test mode, Росэлторг error, Росэлторг procedure_type API, SMTP test endpoint
- Тесты используют mock PATCH callback (не ждут реального n8n)
- Тест SMTP gracefully обрабатывает 400 (SMTP не настроен локально)
- Все тесты проходят при запуске `npx playwright test e2e/12-publications.spec.ts`
</success_criteria>

<output>
После завершения создать `.planning/phases/08-email-e2e-n8n-workflow-rest-api-test-mode-smtp-z-vsks-ru-zakupki-vsks-ru-e2e-playwright-email/08-04-SUMMARY.md`
</output>
