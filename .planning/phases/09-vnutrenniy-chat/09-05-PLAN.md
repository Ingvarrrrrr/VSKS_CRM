---
phase: 09-vnutrenniy-chat
plan: 05
type: execute
wave: 3
depends_on: [09-01, 09-02, 09-03]
files_modified:
  - frontend/src/components/AppBar.vue
autonomous: true
requirements: [CHAT-03, CHAT-09]

must_haves:
  truths:
    - "В боковом меню AppBar появился пункт 'Чат' с иконкой mdi-message-outline"
    - "Рядом с пунктом 'Чат' отображается badge с числом непрочитанных если > 0"
    - "Badge обновляется через WS событие unread_count в реальном времени"
    - "Polling loadBadges каждые 60s служит fallback и тоже обновляет badge"
    - "После открытия чата и прочтения сообщений badge сбрасывается до 0"
  artifacts:
    - path: "frontend/src/components/AppBar.vue"
      provides: "Chat nav item + badgeChatUnread ref + WS integration"
      contains: "badgeChatUnread"
  key_links:
    - from: "frontend/src/components/AppBar.vue"
      to: "frontend/src/composables/useChat.ts"
      via: "import { totalUnread, initChat } from '@/composables/useChat'"
      pattern: "totalUnread|initChat"
    - from: "frontend/src/components/AppBar.vue"
      to: "/api/tasks/badges"
      via: "loadBadges() → data.chat_unread"
      pattern: "chat_unread"
---

<objective>
Интегрировать счётчик непрочитанных чатов в AppBar: добавить пункт меню "Чат", badge с числом непрочитанных, обновление через WS и polling.

Purpose: Без badge пользователь не знает о новых сообщениях. Это CHAT-03 и CHAT-09 — видимое уведомление.
Output: AppBar.vue с chat nav item + badge + WS/polling интеграция.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-vnutrenniy-chat/09-RESEARCH.md
@.planning/phases/09-vnutrenniy-chat/09-03-SUMMARY.md

<interfaces>
<!-- Существующий код AppBar.vue — критически важен для понимания паттерна -->

From frontend/src/components/AppBar.vue (existing badge pattern, lines 372-387):
```typescript
// Существующие badge refs:
const badgeNewTasks = ref(0)
const badgeTaskChanges = ref(0)
const badgeNewPurchases = ref(0)
const badgePurchaseChanges = ref(0)

async function loadBadges() {
  try {
    const data = await apiFetch<any>('/tasks/badges')
    badgeNewTasks.value = data.new_tasks || 0
    badgeTaskChanges.value = data.task_changes || 0
    badgeNewPurchases.value = data.new_purchases || 0
    badgePurchaseChanges.value = data.purchase_changes || 0
    // ДОБАВИТЬ: badgeChatUnread.value = data.chat_unread || 0
  } catch {}
}
```

From frontend/src/components/AppBar.vue (nav items array, line ~428):
```typescript
const allNavItems = [
  { title: 'Дашборд', icon: 'mdi-view-dashboard', route: '/dashboard', roles: MANAGER_ROLES },
  { title: 'Мои задачи и закупки', icon: 'mdi-clipboard-account', route: '/my-tasks', roles: ALL_ROLES },
  // ... другие пункты ...
]
// ДОБАВИТЬ: { title: 'Чат', icon: 'mdi-message-outline', route: '/chat', roles: ALL_ROLES }
```

From frontend/src/components/AppBar.vue (badge in template, lines 235-245):
```html
<template v-slot:append>
  <div class="d-flex align-center ga-1">
    <span v-if="item.route === '/my-tasks' && badgeNewTasks > 0"
      class="sidebar-badge sidebar-badge--new">{{ badgeNewTasks }}</span>
    <!-- ДОБАВИТЬ: -->
    <!-- <span v-if="item.route === '/chat' && badgeChatUnread > 0"
      class="sidebar-badge sidebar-badge--new">{{ badgeChatUnread }}</span> -->
    <v-icon icon="mdi-drag-horizontal-variant" size="16" class="sidebar-drag-handle" />
  </div>
</template>
```

From frontend/src/composables/useChat.ts (Plan 04):
```typescript
export const totalUnread = ref(0)  // обновляется WS событием unread_count
export function initChat()  // запустить WS соединение
export function destroyChat()  // остановить WS соединение
```

AppBar lifecycle pattern:
```typescript
// AppBar.vue onMounted (~line 733):
loadBadges()
_badgeInterval = setInterval(loadBadges, 60_000)
// ДОБАВИТЬ initChat() здесь же
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add chat nav item, badge ref, WS integration to AppBar</name>
  <files>frontend/src/components/AppBar.vue</files>
  <action>
Открыть `frontend/src/components/AppBar.vue`. Выполнить 5 целевых изменений:

**Изменение 1 — Добавить импорт useChat.ts в script setup:**
Найти блок импортов (в начале script setup). Добавить:
```typescript
import { totalUnread, initChat, destroyChat } from '@/composables/useChat'
```

**Изменение 2 — Добавить badge ref:**
Найти блок с `const badgeNewTasks = ref(0)` и подобными. Добавить после них:
```typescript
const badgeChatUnread = ref(0)
```

**Изменение 3 — Добавить chat_unread в loadBadges():**
Найти функцию `loadBadges()`. Внутри `try {}` блока после строки
`badgePurchaseChanges.value = data.purchase_changes || 0`
добавить:
```typescript
    badgeChatUnread.value = data.chat_unread || 0
```

**Изменение 4 — Добавить пункт меню в allNavItems:**
Найти массив allNavItems (или аналогичный — смотри переменную используемую в orderedMenuItems).
Исходя из кода AppBar, найти правильное имя массива (это может быть `allNavShortcuts` — проверить grep).
В конце массива (перед закрывающей `]`) добавить:
```typescript
  { title: 'Чат', icon: 'mdi-message-outline', route: '/chat', roles: ALL_ROLES },
```

**Изменение 5 — Добавить badge в template:**
Найти блок `<template v-slot:append>` внутри `v-for="(item, idx) in orderedMenuItems"`.
После последнего существующего `<span v-if="item.route === '/orders' ...">` добавить:
```html
            <span v-if="item.route === '/chat' && badgeChatUnread > 0"
              class="sidebar-badge sidebar-badge--new" :title="`${badgeChatUnread} непрочитанных`">{{ badgeChatUnread }}</span>
```

**Изменение 6 — Инициализация WS в onMounted:**
Найти `onMounted` или блок где вызывается `loadBadges()` (строка ~733).
Добавить рядом:
```typescript
initChat()
```

Найти `onUnmounted` или блок где `clearInterval(_badgeInterval)`. Добавить:
```typescript
destroyChat()
```

Если `onMounted`/`onUnmounted` явно не используются (код в `setup` выполняется напрямую), добавить:
```typescript
onMounted(() => { initChat() })
onUnmounted(() => { destroyChat() })
```

Убедиться что `onMounted` и `onUnmounted` импортированы из 'vue'.

**ВАЖНО:** Также связать `totalUnread` с `badgeChatUnread` — добавить watch:
```typescript
watch(totalUnread, (val) => { badgeChatUnread.value = val })
```

Это гарантирует что WS события обновляют badge мгновенно, а polling каждые 60s служит fallback.
  </action>
  <verify>
    <automated>grep -n "badgeChatUnread\|initChat\|mdi-message-outline\|chat_unread\|totalUnread" /c/Users/1/Desktop/Cursor/VSKS_CRM/frontend/src/components/AppBar.vue</automated>
  </verify>
  <done>grep находит: badgeChatUnread (ref определён); mdi-message-outline (nav item добавлен); chat_unread (в loadBadges); totalUnread (импорт и watch); initChat (вызов в onMounted или setup блоке); sidebar-badge span с item.route === '/chat'.</done>
</task>

</tasks>

<verification>
1. `grep -c "badgeChatUnread\|chat_unread\|mdi-message-outline\|initChat\|totalUnread" frontend/src/components/AppBar.vue` — ≥ 5 совпадений
2. `grep "path.*chat\|ChatView" frontend/src/router/index.ts` — маршрут добавлен (из плана 09-04)
3. Логика обновления badge двойная: polling (loadBadges → data.chat_unread) + WS push (watch totalUnread)
4. destroyChat вызывается при unmount — WS соединение корректно закрывается
</verification>

<success_criteria>
- В боковом меню AppBar добавлен пункт "Чат" с иконкой mdi-message-outline
- badgeChatUnread отображается как badge рядом с /chat в sidebar
- Badge обновляется двумя путями: WS event (мгновенно) + polling каждые 60s (fallback)
- WS соединение запускается при монтировании AppBar (который всегда присутствует в layout)
- WS соединение корректно закрывается при unmount
- Badge = 0 после прочтения сообщений (серверный POST /read → WS unread_count event)
</success_criteria>

<output>
После завершения создать `.planning/phases/09-vnutrenniy-chat/09-05-SUMMARY.md`
</output>
