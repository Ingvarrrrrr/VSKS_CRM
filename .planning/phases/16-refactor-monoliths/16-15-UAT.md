---
phase: 16-refactor-monoliths
plan: 15
type: uat
status: pending
created: 2026-04-19
---

# 16-15 UAT: MyTasksView Refactor Visual Sign-Off

## What Was Built

MyTasksView.vue decomposed from 1736 lines into 1 slim orchestrator (533 lines) + 8 child components across Plans 16-12..16-15. No functional or visual changes — only file layout changed.

### Components Extracted (Plans 16-12 through 16-15)

| Component | Plan | Lines | Responsibility |
|-----------|------|-------|----------------|
| `OrgSelector.vue` | 16-12 | 175 | Org cards grid + selection |
| `OrgSummaryBar.vue` | 16-12 | 296 | Header counters + consent banners |
| `TasksTable.vue` | 16-13 | 254 | Tasks list view |
| `TasksKanban.vue` | 16-13 | 389 | Tasks kanban view |
| `PurchasesTable.vue` | 16-14 | 263 | Purchases list view |
| `PurchasesKanban.vue` | 16-14 | 349 | Purchases kanban view |
| `TaskEditDialog.vue` | 16-15 | ~310 | Task create/edit + delegation + broadcast + link-purchase dialogs |
| `TasksReport.vue` | 16-15 | ~100 | Report tab by department |

**MyTasksView.vue final: 533 lines** (target was ≤600, D-14 met)

## UAT Checklist

Visit `https://85.239.53.155/my-tasks` (or local dev) as a regular user and verify each item:

### Org Selector
- [ ] Org cards grid renders identically: all-orgs card + per-org cards, counters visible, hover states unchanged
- [ ] Clicking an org card selects it (highlight applied, tasks/purchases filter to that org)
- [ ] "Все организации" card resets filter

### Tab Switcher
- [ ] Tabs (Задачи / Закупки / Report) visible, active indicator styled correctly
- [ ] Switching tabs shows correct content

### Tasks Tab — List Mode
- [ ] Table renders columns in correct order (№, Название, Приоритет, Статус, Исполнители, Срок, Закупка)
- [ ] Row hover + click opens task dialog

### Tasks Tab — Kanban Mode
- [ ] Status columns visible in correct order (К выполнению, В работе, На проверке, Архив)
- [ ] Drag-drop a task between columns triggers backend PATCH and persists

### Purchases Tab — List Mode
- [ ] v-data-table columns identical to pre-refactor

### Purchases Tab — Kanban Mode
- [ ] Kanban columns render correctly
- [ ] Drag-drop a purchase updates kanban_status

### Header Counters / OrgSummaryBar
- [ ] Pending-consent, pending-approvals, consent-declines all render with correct counts
- [ ] Consent card: accept/decline buttons work

### Task Edit Dialog (click any task)
- [ ] Dialog opens with correct task data
- [ ] ChatEmbed renders in dialog (Чат section)
- [ ] Delegation dialog (Делегировать button) opens correctly
- [ ] Dismiss-field highlight works (yellow outline on unseen fields, click to dismiss)
- [ ] Save / Delete / Cancel buttons work

### Report Tab
- [ ] Dept filter + period selector render
- [ ] "Сформировать" button loads report data

### General Health
- [ ] No console errors in devtools
- [ ] No layout shifts / jank / spacing differences vs pre-refactor

## How to Approve

After verifying, update this file's frontmatter to `status: approved` and commit:

```bash
git add -f .planning/phases/16-refactor-monoliths/16-15-UAT.md
git commit -m "docs(16-15): UAT sign-off for MyTasksView refactor"
```

Or if issues found, update to `status: needs-fix` and list specific issues below.

## Issues Found

_(fill in during UAT)_

None yet — pending human review.
