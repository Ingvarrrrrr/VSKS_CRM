---
phase: 16-refactor-monoliths
plan: 14
type: execute
wave: 5
depends_on: [16-12]
files_modified:
  - frontend/src/components/my-tasks/PurchasesTable.vue
  - frontend/src/components/my-tasks/PurchasesKanban.vue
  - frontend/src/views/MyTasksView.vue
autonomous: true
requirements: [REFACTOR-03, REFACTOR-05, REFACTOR-07]
must_haves:
  truths:
    - "PurchasesTable.vue renders v-data-table of purchases (list mode)"
    - "PurchasesKanban.vue renders kanban columns grouped by kanban_status (kanban mode)"
    - "MyTasksView.vue toggles between PurchasesTable and PurchasesKanban via v-if on viewMode"
    - "Clicking a purchase still opens PurchaseDetail or navigates (emit 'open-purchase')"
    - "Drag-drop in purchases kanban still persists via emit"
    - "npm run build exits 0 with zero warnings"
  artifacts:
    - path: "frontend/src/components/my-tasks/PurchasesTable.vue"
      provides: "v-data-table list view of purchases"
      min_lines: 250
    - path: "frontend/src/components/my-tasks/PurchasesKanban.vue"
      provides: "Kanban columns grouped by kanban_status"
      min_lines: 250
    - path: "frontend/src/views/MyTasksView.vue"
      provides: "Toggles between PurchasesTable and PurchasesKanban"
      max_lines: 900
  key_links:
    - from: "frontend/src/views/MyTasksView.vue"
      to: "frontend/src/components/my-tasks/PurchasesTable.vue"
      via: "renders PurchasesTable with :purchases + :selected-org-id props, @open-purchase emit"
      pattern: "import PurchasesTable from"
    - from: "frontend/src/views/MyTasksView.vue"
      to: "frontend/src/components/my-tasks/PurchasesKanban.vue"
      via: "renders PurchasesKanban with :purchases prop, @open-purchase + @update-kanban-status emits"
      pattern: "import PurchasesKanban from"
---

<objective>
Extract the Закупки tab into `PurchasesTable.vue` + `PurchasesKanban.vue` (D-17). Same rationale as Plan 16-13: two components, parent toggles visibility by viewMode. Runs in parallel with Plan 16-13 (both depend only on 16-12, no shared files). Implements D-17.
</objective>

<execution_context>
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-refactor-monoliths/16-CONTEXT.md
@.planning/phases/16-refactor-monoliths/16-RESEARCH.md
@.planning/phases/16-refactor-monoliths/16-VALIDATION.md

<interfaces>
Parent MyTasksView.vue keeps all apiFetch (D-14 + §3.3).
Props in: purchases array, selectedOrgId number | null, loading boolean.
Emits out: 'open-purchase' (purchaseId: number), 'update-kanban-status' (purchaseId: number, newStatus: string) — drag-drop.
Two separate components per D-17 + §3.2 (list and kanban share zero DOM).
Parallel-safe with Plan 16-13 — only shared edit file is MyTasksView.vue (sequential commits required; this plan assumes 16-13 lands first if executed in same wave, or parent merges in Task 3).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PurchasesTable.vue (list view of purchases)</name>
  <read_first>
    - frontend/src/views/MyTasksView.vue (identify the `v-if="viewMode === 'list'"` block inside the Закупки tab — v-data-table with columns: number, title, kanban_status, assignees, amount, contractor, subsidy, etc.)
    - frontend/src/components/my-tasks/TasksTable.vue (Plan 16-13, if landed — pattern reference)
    - frontend/src/components/my-tasks/OrgSelector.vue (Plan 16-12 — pattern reference)
    - .planning/phases/16-refactor-monoliths/16-CONTEXT.md D-17
  </read_first>
  <files>frontend/src/components/my-tasks/PurchasesTable.vue</files>
  <action>
    1. Create `frontend/src/components/my-tasks/PurchasesTable.vue` as a Vue 3 SFC.
    2. Template: COPY BYTE-FOR-BYTE the purchases list v-data-table block from MyTasksView.vue. Replace row click handler with `@click:row="(_, { item }) => $emit('open-purchase', item.id)"`.
    3. Script setup: define `Purchase` interface matching MyTasksView.vue's `purchases` ref shape (fields: id, number, title, kanban_status, assignees, amount, subsidy_id, contractor_id, etc. — inspect ref to confirm). Declare `defineProps<{ purchases: Purchase[]; selectedOrgId: number | null; loading: boolean }>`. Declare `defineEmits<{ 'open-purchase': [purchaseId: number] }>`.
    4. Style scoped: MOVE (or duplicate) selectors used only by this table — e.g. `.purchase-row`, `.purchase-status-chip`, any custom v-data-table theming.
    5. No apiFetch.
  </action>
  <verify>
    <automated>test -f frontend/src/components/my-tasks/PurchasesTable.vue && grep -q "defineProps<" frontend/src/components/my-tasks/PurchasesTable.vue && grep -q "open-purchase" frontend/src/components/my-tasks/PurchasesTable.vue && grep -q "v-data-table" frontend/src/components/my-tasks/PurchasesTable.vue && ! grep -q "apiFetch" frontend/src/components/my-tasks/PurchasesTable.vue</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/my-tasks/PurchasesTable.vue` exists
    - Contains `<script setup lang="ts">`
    - Contains `defineProps<` and `defineEmits<`
    - Contains `open-purchase` emit name
    - Contains `v-data-table`
    - Does NOT contain `apiFetch`
    - `wc -l frontend/src/components/my-tasks/PurchasesTable.vue` ≥ 250
  </acceptance_criteria>
  <done>
    PurchasesTable.vue self-contained list view.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PurchasesKanban.vue (kanban view with drag-drop)</name>
  <read_first>
    - frontend/src/views/MyTasksView.vue (identify the `v-if="viewMode === 'kanban'"` block inside Закупки tab — columns grouped by kanban_status, draggable cards)
    - frontend/src/components/my-tasks/PurchasesTable.vue (just created)
    - .planning/phases/16-refactor-monoliths/16-CONTEXT.md D-17
  </read_first>
  <files>frontend/src/components/my-tasks/PurchasesKanban.vue</files>
  <action>
    1. Create `frontend/src/components/my-tasks/PurchasesKanban.vue` as a Vue 3 SFC.
    2. Template: COPY BYTE-FOR-BYTE the purchases kanban block from MyTasksView.vue. Replace drag-end handler with `$emit('update-kanban-status', purchase.id, newStatus)`. Replace card click with `$emit('open-purchase', purchase.id)`.
    3. Script setup: reuse `Purchase` interface. Declare `defineProps<{ purchases: Purchase[]; selectedOrgId: number | null }>`. Declare `defineEmits<{ 'open-purchase': [purchaseId: number], 'update-kanban-status': [purchaseId: number, newStatus: string] }>`.
    4. Preserve drag library (vuedraggable/sortable) import and usage exactly — do not change.
    5. Style scoped: MOVE kanban CSS (.purchase-kanban-column, .purchase-kanban-card, etc.).
    6. No apiFetch.
  </action>
  <verify>
    <automated>test -f frontend/src/components/my-tasks/PurchasesKanban.vue && grep -q "defineProps<" frontend/src/components/my-tasks/PurchasesKanban.vue && grep -q "open-purchase" frontend/src/components/my-tasks/PurchasesKanban.vue && grep -q "update-kanban-status" frontend/src/components/my-tasks/PurchasesKanban.vue && ! grep -q "apiFetch" frontend/src/components/my-tasks/PurchasesKanban.vue</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/my-tasks/PurchasesKanban.vue` exists
    - Contains `<script setup lang="ts">`
    - Contains `defineProps<` and `defineEmits<`
    - Contains `open-purchase` AND `update-kanban-status` emit names
    - Does NOT contain `apiFetch`
    - `wc -l frontend/src/components/my-tasks/PurchasesKanban.vue` ≥ 250
  </acceptance_criteria>
  <done>
    PurchasesKanban.vue self-contained kanban view.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire PurchasesTable + PurchasesKanban into MyTasksView.vue and verify</name>
  <read_first>
    - frontend/src/views/MyTasksView.vue (current state — post-16-12, possibly post-16-13 if sequential)
  </read_first>
  <files>frontend/src/views/MyTasksView.vue</files>
  <action>
    1. Add imports in `<script setup>`:
       ```
       import PurchasesTable from '@/components/my-tasks/PurchasesTable.vue'
       import PurchasesKanban from '@/components/my-tasks/PurchasesKanban.vue'
       ```
    2. REPLACE the inline purchases list + kanban blocks with:
       ```
       <PurchasesTable
         v-if="viewMode === 'list'"
         :purchases="purchases"
         :selected-org-id="selectedOrgId"
         :loading="loading"
         @open-purchase="openPurchaseDialog"
       />
       <PurchasesKanban
         v-if="viewMode === 'kanban'"
         :purchases="purchases"
         :selected-org-id="selectedOrgId"
         @open-purchase="openPurchaseDialog"
         @update-kanban-status="handleUpdateKanbanStatus"
       />
       ```
    3. Ensure `handleUpdateKanbanStatus(purchaseId, newStatus)` exists — body: apiFetch PATCH `/api/purchases/{pid}/kanban-status`, refetch purchases.
    4. Remove from MyTasksView.vue `<style scoped>` CSS selectors moved exclusively to children.
    5. Build: `docker exec vsks_crm-frontend-1 sh -c "cd /app && npm run build"` exits 0 with 0 warnings.
    6. Line count: `wc -l frontend/src/views/MyTasksView.vue` — goal ≤ 900 (Plan 16-15 will enforce ≤ 600 final).
    7. Push to deploy. E2E: `BASE_URL=http://85.239.53.155 npx playwright test e2e/02-all-pages.spec.ts e2e/05-orders.spec.ts` → pass.
    8. Commit: `refactor(16-14): extract PurchasesTable + PurchasesKanban from MyTasksView` — 3 files.
  </action>
  <verify>
    <automated>grep -q "import PurchasesTable from" frontend/src/views/MyTasksView.vue && grep -q "import PurchasesKanban from" frontend/src/views/MyTasksView.vue && grep -q "<PurchasesTable" frontend/src/views/MyTasksView.vue && grep -q "<PurchasesKanban" frontend/src/views/MyTasksView.vue && git log --oneline -1 | grep -qE "refactor\(16-14\): extract PurchasesTable"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/views/MyTasksView.vue` contains `import PurchasesTable from`
    - `frontend/src/views/MyTasksView.vue` contains `import PurchasesKanban from`
    - `frontend/src/views/MyTasksView.vue` contains `<PurchasesTable` and `<PurchasesKanban` in template
    - `wc -l frontend/src/views/MyTasksView.vue` ≤ 900
    - `docker exec vsks_crm-frontend-1 sh -c "cd /app && npm run build"` exits 0 with 0 warnings
    - `BASE_URL=http://85.239.53.155 npx playwright test e2e/02-all-pages.spec.ts e2e/05-orders.spec.ts` exits 0
    - `git log --oneline -1` matches `^[a-f0-9]+ refactor\(16-14\): extract PurchasesTable`
    - Commit diff: exactly 3 files
  </acceptance_criteria>
  <done>
    Purchases tab delegated to two child components. Build green. E2E green.
  </done>
</task>

</tasks>

<verification>
- `docker exec vsks_crm-frontend-1 sh -c "cd /app && npm run build"` exits 0 with 0 warnings
- `BASE_URL=http://85.239.53.155 npx playwright test e2e/02-all-pages.spec.ts e2e/05-orders.spec.ts` → pass
- `wc -l frontend/src/components/my-tasks/PurchasesTable.vue` ≥ 250
- `wc -l frontend/src/components/my-tasks/PurchasesKanban.vue` ≥ 250
- `wc -l frontend/src/views/MyTasksView.vue` ≤ 900
</verification>

<success_criteria>
Purchases tab split into PurchasesTable + PurchasesKanban. Commit: `refactor(16-14): extract PurchasesTable + PurchasesKanban from MyTasksView`.
</success_criteria>

<output>
After completion, create `.planning/phases/16-refactor-monoliths/16-14-SUMMARY.md`.
</output>
