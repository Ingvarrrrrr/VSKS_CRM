# Plan 29-02: check_schema._ensure_* для 9 таблиц + ALTER (purchases/users/tasks)

**Phase:** 29 (Vehicle Fleet)
**Wave:** 0
**Depends on:** 29-01
**Estimated complexity:** L (~450 LOC в check_schema.py + lifespan вызов в __init__.py)
**Decisions covered:** D-06, D-08, D-13, D-17, D-18

## Goal
Применить DDL для 9 новых таблиц и ALTER 3 существующих через паттерн `check_schema._ensure_*_table`. После мерджа: backend стартует без 502, все таблицы существуют, индексы и FK на месте, alembic не трогаем (chain сломан).

## Files

### Modify
- `c:\Users\1\Desktop\Cursor\VSKS_CRM\backend\check_schema.py` — добавить 9 новых функций `_ensure_*_table` и 3 ALTER-функции, регистрация в `--apply` блоке. Pattern: `check_schema.py:204-258` (Phase 27.1 contract_items)
- `c:\Users\1\Desktop\Cursor\VSKS_CRM\backend\app\__init__.py` — добавить вызовы в lifespan ПОСЛЕ существующих `_ensure_*` блоков, ДО `yield`. Pattern: `__init__.py:537-553`

## Tasks
1. В `check_schema.py` добавить `async def _ensure_vehicles_table(conn)` — `CREATE TABLE IF NOT EXISTS vehicles (...)` со всеми колонками из 29-01 task #1. UNIQUE на `plate`. Индексы: `ix_vehicles_owner_org_id`, `ix_vehicles_assigned_org_id`, `ix_vehicles_plate`, `ix_vehicles_state`, `ix_vehicles_type`. Каждый CREATE INDEX через `CREATE INDEX IF NOT EXISTS`. props JSONB default `'{}'::jsonb`.
2. Добавить `async def _ensure_vehicle_attachments_table(conn)`: вся структура из 29-01 task #2 + FK `vehicle_id REFERENCES vehicles(id) ON DELETE CASCADE` + индекс `ix_vehicle_attachments_vehicle_id` + `ix_vehicle_attachments_sha256`.
3. Добавить `async def _ensure_vehicle_repairs_table(conn)`: FK `vehicle_id ... ON DELETE CASCADE`, FK `purchase_id REFERENCES purchases(id) ON DELETE SET NULL`. Индексы: `ix_vehicle_repairs_vehicle_id`, `ix_vehicle_repairs_status`, `ix_vehicle_repairs_purchase_id`.
4. Добавить `async def _ensure_repair_attachments_table(conn)`: FK `repair_id REFERENCES vehicle_repairs(id) ON DELETE CASCADE` + индексы.
5. Добавить `async def _ensure_vehicle_field_history_table(conn)`: FK `vehicle_id ... ON DELETE CASCADE`, FK `changed_by_user_id REFERENCES users(id) ON DELETE SET NULL`. Индексы: `ix_vehicle_field_history_vehicle_id`, `ix_vehicle_field_history_field_key`, `ix_vehicle_field_history_changed_at`.
6. Добавить `async def _ensure_vehicle_odometer_table(conn)`: FK `vehicle_id ... CASCADE`. **UNIQUE constraint:** `CONSTRAINT uq_vehicle_odometer_vehicle_date UNIQUE (vehicle_id, date)`. Индексы: `ix_vehicle_odometer_vehicle_id_date_desc` (BTREE по `vehicle_id, date DESC` для быстрого max).
7. Добавить `async def _ensure_fuel_logs_table(conn)`: FK `vehicle_id CASCADE`, FK `receipt_attachment_id REFERENCES vehicle_attachments(id) ON DELETE SET NULL`. Индекс по `vehicle_id, date DESC`.
8. Добавить `async def _ensure_trips_table(conn)`: FK `vehicle_id`, FK `driver_user_id REFERENCES users(id) ON DELETE SET NULL`, FK `driver_external_id REFERENCES external_drivers(id) ON DELETE SET NULL`. CHECK constraint `ck_trip_driver_xor: (driver_user_id IS NOT NULL OR driver_external_id IS NOT NULL)`. Индексы по `vehicle_id`, `date DESC`.
9. Добавить `async def _ensure_external_drivers_table(conn)`: FK `org_id REFERENCES organizations(id) ON DELETE SET NULL`. Индексы по `full_name`, `org_id`.
10. Добавить `async def _ensure_purchases_vehicle_id(conn)`: `ALTER TABLE purchases ADD COLUMN IF NOT EXISTS vehicle_id INTEGER REFERENCES vehicles(id) ON DELETE SET NULL`. Индекс `CREATE INDEX IF NOT EXISTS ix_purchases_vehicle_id ON purchases(vehicle_id) WHERE vehicle_id IS NOT NULL`.
11. Добавить `async def _ensure_users_driver_columns(conn)`: 7 ALTER COLUMN операций (по одному `ALTER TABLE users ADD COLUMN IF NOT EXISTS` на каждое поле из D-04): `can_drive BOOLEAN NOT NULL DEFAULT FALSE`, `license_series VARCHAR(10)`, `license_number VARCHAR(20)`, `license_categories VARCHAR(50)`, `license_issued_at DATE`, `license_expires_at DATE`, `medical_cert_expires_at DATE`.
12. Добавить `async def _ensure_tasks_system_tag(conn)`: `ALTER TABLE tasks ADD COLUMN IF NOT EXISTS system_tag VARCHAR(200)` + `CREATE INDEX IF NOT EXISTS ix_tasks_system_tag ON tasks(system_tag) WHERE system_tag IS NOT NULL` (partial index).
13. В `--apply` блоке `check_schema.py` (в самом низу файла) добавить вызовы 12 новых функций в порядке зависимостей: external_drivers → vehicles → vehicle_attachments → vehicle_repairs → repair_attachments → vehicle_field_history → vehicle_odometer → fuel_logs → trips, потом ALTER (purchases/users/tasks). КАЖДУЮ функцию обернуть в `try/except` с `print(f"⚠️ ... failed: {e}")` чтобы один сбой не блокировал остальные (паттерн Phase 27.1).
14. В `backend/app/__init__.py` lifespan (после блока Phase 27.1 на ~line 537), добавить новый блок:
    ```python
    # Phase 29: vehicles fleet tables
    try:
        from check_schema import (
            _ensure_external_drivers_table, _ensure_vehicles_table,
            _ensure_vehicle_attachments_table, _ensure_vehicle_repairs_table,
            _ensure_repair_attachments_table, _ensure_vehicle_field_history_table,
            _ensure_vehicle_odometer_table, _ensure_fuel_logs_table,
            _ensure_trips_table, _ensure_purchases_vehicle_id,
            _ensure_users_driver_columns, _ensure_tasks_system_tag,
        )
        from .database import engine as _engine
        async with _engine.begin() as conn:
            for fn in [_ensure_external_drivers_table, _ensure_vehicles_table, _ensure_vehicle_attachments_table, _ensure_vehicle_repairs_table, _ensure_repair_attachments_table, _ensure_vehicle_field_history_table, _ensure_vehicle_odometer_table, _ensure_fuel_logs_table, _ensure_trips_table, _ensure_purchases_vehicle_id, _ensure_users_driver_columns, _ensure_tasks_system_tag]:
                try:
                    await fn(conn)
                except Exception as e:
                    logging.getLogger(__name__).warning(f"Phase 29 schema {fn.__name__} skipped: {e}")
    except Exception as e:
        logging.getLogger(__name__).warning(f"Phase 29 schema bootstrap failed (non-fatal): {e}")
    ```
15. Commit message: `feat(29-02): check_schema — 9 vehicle tables + ALTER purchases/users/tasks`

## Verification
- `grep -c "_ensure_vehicles_table\|_ensure_vehicle_attachments_table\|_ensure_vehicle_repairs_table\|_ensure_repair_attachments_table\|_ensure_vehicle_field_history_table\|_ensure_vehicle_odometer_table\|_ensure_fuel_logs_table\|_ensure_trips_table\|_ensure_external_drivers_table\|_ensure_purchases_vehicle_id\|_ensure_users_driver_columns\|_ensure_tasks_system_tag" backend/check_schema.py` — ≥12 (определение + использование).
- `grep -n "Phase 29: vehicles fleet" backend/app/__init__.py` — найдено.
- Push на claude → autodeploy → backend стартует БЕЗ 502 (502 от 29-01 закроется этим коммитом).
- `git log origin/claude..HEAD` — пусто.
- На проде (curl): `curl -s -o /dev/null -w "%{http_code}" http://85.239.53.155/api/users/me` → 401 (не 502). Это значит check_schema применился, backend поднялся.

## Risks / Gotchas
- **Каждый statement = отдельный `await conn.execute(text(...))`** (asyncpg запрещает multi-statement) — паттерн Phase 27.1.
- **Порядок важен.** external_drivers ДО trips. vehicles ДО vehicle_attachments / vehicle_repairs / vehicle_odometer / fuel_logs / trips. ALTER purchases ПОСЛЕ vehicles (FK).
- **Если backend упал с 502 после push** — посмотреть `docker logs vsks_crm-backend-1 --tail 50 | grep -i 'undefinedcolumn\|relation .* does not exist'`. Это значит либо `lifespan` отработал с ошибкой, либо запрос пришёл до завершения lifespan (race condition — но autodeploy.sh ждёт sleep 8).
- **Lesson 2026-05-04 (UndefinedColumnError):** между 29-01 push и 29-02 push backend будет 502. Это OK; ускоряем 29-02 to-push.
- **Lesson 2026-05-04 (`check_schema.py` race):** lifespan вызывает функции до того, как FastAPI принимает запросы — не race.
- **TasksTable system_tag:** уже использует `system_tag` в существующих коммитах? Проверить `grep -rn 'system_tag' backend/app/models/task.py` ДО push — если есть, удалить дубликат. R-1 утверждает что нет.

## Definition of Done
- [ ] 12 функций добавлены в `check_schema.py`, каждая идемпотентная
- [ ] Lifespan-блок Phase 29 добавлен в `__init__.py` ПОСЛЕ Phase 27.1 блока
- [ ] Commit `feat(29-02): check_schema — 9 vehicle tables …` запушен в claude
- [ ] После autodeploy: `curl ... /api/users/me` → 401 (не 502), значит backend поднялся
- [ ] `git log origin/claude..HEAD` — пусто

## Executor boilerplate
1. No verification loops. `docker exec psql -c "\d vehicles"` запустить **ОДИН раз** в SSH после autodeploy для глазной проверки — не цикл.
2. После commit/push — `git log origin/claude..HEAD`. Пусто = ок.
3. **Recent-commit check:** грепнуть `git log origin/claude..HEAD --grep="Column(" -- backend/app/models/` — должно быть 29-01 уже запушен.
4. Если 502 после deploy — `docker logs vsks_crm-backend-1 --tail 50` чтобы понять что не зашло.
