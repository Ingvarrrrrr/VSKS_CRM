---
phase: 08-торговые-площадки
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/views/CreateOrderView.vue
autonomous: true
requirements:
  - PUB-06
  - PUB-07

must_haves:
  truths:
    - "При нажатии 'Опубликовать' на Росэлторг в диалоге появляется dropdown выбора типа процедуры"
    - "Кнопка подтверждения публикации на Росэлторг неактивна пока тип процедуры не выбран"
    - "Выбранный procedure_type передаётся в POST /api/publications/purchases/{id} в теле запроса"
    - "Публикация на Фабрикант работает как прежде (без промежуточного шага)"
  artifacts:
    - path: "frontend/src/views/CreateOrderView.vue"
      provides: "Диалог публикации с dropdown типа процедуры для Росэлторг"
      contains: "roseltorgProcedureType"
  key_links:
    - from: "Кнопка 'Опубликовать' (Росэлторг)"
      to: "v-select procedure_type"
      via: "pendingPlatform ref показывает/скрывает секцию"
      pattern: "pendingPlatform.*roseltorg_rb"
    - from: "doPublish(platform, procedureType)"
      to: "POST /api/publications/purchases/{id}"
      via: "apiFetch с body.procedure_type"
      pattern: "procedure_type"
---

<objective>
Добавить в диалог публикации CreateOrderView.vue dropdown выбора типа процедуры при публикации на Росэлторг.Бизнес.

Purpose: Пользователь должен выбрать тип процедуры перед публикацией (решение заблокировано: dropdown обязателен для Росэлторг). procedure_type передаётся в API и затем в n8n для маппинга на templateId.
Output: Обновлённый CreateOrderView.vue с двухшаговым диалогом для Росэлторг.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md

<interfaces>
Из CreateOrderView.vue (существующие refs и функции в секции публикаций, ~line 2484):
```typescript
// Существующие
const publishDialog = ref(false)
const publishingPlatform = ref<string | null>(null)
const PLATFORM_LABELS: Record<string, string> = {
  fabrikant: 'Фабрикант',
  roseltorg_rb: 'Росэлторг.Бизнес',
}

// Существующая функция (будет изменена)
async function doPublish(platform: string) {
  publishingPlatform.value = platform
  try {
    const pub = await apiFetch<Publication>(`/publications/purchases/${purchaseId.value}`, {
      method: 'POST',
      body: { platform },
    })
    // ...
  }
}

// Существующий polling (НЕ ТРОГАТЬ)
async function pollPublication(pubId: number) { ... }
```

Диалог публикации (template, ~line 1227-1267):
```vue
<v-dialog v-model="publishDialog" max-width="500">
  <v-card>
    <v-card-title>Опубликовать закупку</v-card-title>
    <v-card-text>
      <!-- Список площадок с кнопками "Опубликовать" для каждой -->
      <div v-for="platform in AVAILABLE_PLATFORMS" :key="platform">
        <!-- chip/row с площадкой и кнопкой -->
        <v-btn @click="doPublish(platform)">Опубликовать</v-btn>
      </div>
    </v-card-text>
  </v-card>
</v-dialog>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить dropdown procedure_type в диалог публикации</name>
  <files>frontend/src/views/CreateOrderView.vue</files>
  <action>
В `frontend/src/views/CreateOrderView.vue` сделать следующие изменения:

**1. Добавить новые refs в секцию скрипта (рядом с существующими refs публикации):**
```typescript
const pendingPlatform = ref<string | null>(null)
const roseltorgProcedureType = ref<string | null>(null)
const ROSELTORG_PROCEDURE_TYPES = [
  { value: 'request_quotations', title: 'Запрос котировок' },
  { value: 'request_proposals',  title: 'Запрос предложений' },
  { value: 'competition',        title: 'Конкурс' },
  { value: 'auction',            title: 'Аукцион' },
]
```

**2. Изменить функцию doPublish — добавить параметр procedureType:**
```typescript
async function doPublish(platform: string, procedureType?: string | null) {
  publishingPlatform.value = platform
  try {
    const body: Record<string, any> = { platform }
    if (procedureType) body.procedure_type = procedureType
    const pub = await apiFetch<Publication>(`/publications/purchases/${purchaseId.value}`, {
      method: 'POST',
      body,
    })
    publications.value.unshift(pub)
    showSnack(`Отправлено на публикацию: ${PLATFORM_LABELS[platform]}`)
    publishDialog.value = false
    pendingPlatform.value = null
    roseltorgProcedureType.value = null
    pollPublication(pub.id)
  } catch (e: any) {
    showSnack(e?.detail || 'Ошибка при отправке на публикацию', 'error')
  } finally {
    publishingPlatform.value = null
  }
}
```

**3. В template диалога публикации — добавить логику двух шагов для Росэлторг:**

Для кнопки "Опубликовать" на Росэлторг — вместо прямого вызова doPublish('roseltorg_rb'):
- При клике устанавливать `pendingPlatform = 'roseltorg_rb'` (промежуточный шаг)
- Кнопки других площадок (Фабрикант) — вызывают doPublish напрямую как раньше

После блока со списком площадок добавить секцию выбора типа процедуры:
```vue
<!-- Выбор типа процедуры для Росэлторг (показывается только когда pendingPlatform = 'roseltorg_rb') -->
<v-expand-transition>
  <div v-if="pendingPlatform === 'roseltorg_rb'" class="mt-3 px-1">
    <v-divider class="mb-3" />
    <div class="text-subtitle-2 mb-2">Тип процедуры Росэлторг.Бизнес</div>
    <v-select
      v-model="roseltorgProcedureType"
      :items="ROSELTORG_PROCEDURE_TYPES"
      item-title="title"
      item-value="value"
      label="Выберите тип процедуры"
      variant="outlined"
      density="compact"
      hide-details
    />
    <div class="d-flex gap-2 mt-3">
      <v-btn
        variant="text"
        @click="pendingPlatform = null; roseltorgProcedureType = null"
      >Назад</v-btn>
      <v-btn
        color="deep-purple"
        :disabled="!roseltorgProcedureType"
        :loading="publishingPlatform === 'roseltorg_rb'"
        @click="doPublish('roseltorg_rb', roseltorgProcedureType)"
      >Опубликовать на Росэлторг</v-btn>
    </div>
  </div>
</v-expand-transition>
```

Кнопка "Опубликовать" в строке Росэлторг меняется на:
```vue
<v-btn
  size="small"
  color="deep-purple"
  variant="outlined"
  :loading="publishingPlatform === 'roseltorg_rb'"
  @click="pendingPlatform = 'roseltorg_rb'"
>
  Опубликовать
</v-btn>
```

Кнопки других площадок (Фабрикант) остаются без изменений — вызывают `doPublish(platform)`.

ВАЖНО: НЕ менять `pollPublication()`, НЕ менять секцию отображения таблицы публикаций, НЕ менять логику получения publications при загрузке страницы.
  </action>
  <verify>
    <automated>cd /c/Users/1/VSKS_CRM/frontend && grep -n "roseltorgProcedureType\|pendingPlatform\|ROSELTORG_PROCEDURE_TYPES\|procedure_type" src/views/CreateOrderView.vue | head -20</automated>
  </verify>
  <done>CreateOrderView.vue содержит roseltorgProcedureType ref, ROSELTORG_PROCEDURE_TYPES константу, v-select в диалоге и doPublish передаёт procedure_type</done>
</task>

</tasks>

<verification>
1. `grep -c "roseltorgProcedureType\|pendingPlatform" frontend/src/views/CreateOrderView.vue` показывает > 0 совпадений
2. `grep -n "procedure_type" frontend/src/views/CreateOrderView.vue` — строка с `body.procedure_type = procedureType`
3. TypeScript компиляция: `cd frontend && npx vue-tsc --noEmit 2>&1 | head -20` — нет новых ошибок
</verification>

<success_criteria>
- Диалог публикации на Росэлторг показывает dropdown с 4 типами процедур
- Кнопка "Опубликовать на Росэлторг" неактивна пока тип не выбран
- procedure_type передаётся в API запросе
- Публикация на Фабрикант работает без изменений
</success_criteria>

<output>
После завершения создать `.planning/phases/08-email-e2e-n8n-workflow-rest-api-test-mode-smtp-z-vsks-ru-zakupki-vsks-ru-e2e-playwright-email/08-03-SUMMARY.md`
</output>
