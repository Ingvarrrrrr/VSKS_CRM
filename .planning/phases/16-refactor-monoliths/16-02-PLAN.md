---
phase: 16-refactor-monoliths
plan: 02
type: execute
wave: 1
depends_on: [16-01]
files_modified:
  - backend/app/routers/purchase_export.py
  - backend/app/routers/purchases.py
  - backend/app/__init__.py
autonomous: true
requirements: [REFACTOR-01, REFACTOR-04, REFACTOR-08]
must_haves:
  truths:
    - "GET /api/purchases/export/columns returns same shape as before refactor"
    - "GET /api/purchases/export/excel returns a valid .xlsx stream"
    - "GET /api/purchases/import/template returns template xlsx"
    - "POST /api/purchases/import still accepts Scroller-format xlsx"
    - "All export-related code removed from purchases.py"
  artifacts:
    - path: "backend/app/routers/purchase_export.py"
      provides: "Excel export + import-template + Scroller-format import router"
      min_lines: 200
    - path: "backend/app/routers/purchases.py"
      provides: "No longer contains export_* endpoints or ALL_EXPORT_COLUMNS constants"
  key_links:
    - from: "backend/app/__init__.py"
      to: "backend/app/routers/purchase_export.py"
      via: "app.include_router(purchase_export.router)"
      pattern: "app\\.include_router\\(purchase_export\\.router\\)"
    - from: "backend/app/routers/purchase_export.py"
      to: "backend/app/models/purchase.py"
      via: "imports Purchase model"
      pattern: "from app.models.purchase import Purchase"
---

<objective>
Extract Excel export + Scroller-format payment import (zero cross-deps per RESEARCH.md §1.1) into `purchase_export.py`. First atomic extract — validates the pattern. No behaviour change: same URLs, same response shape, same auth guards. Implements D-07 per CONTEXT.md.
</objective>

<execution_context>
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-refactor-monoliths/16-CONTEXT.md
@.planning/phases/16-refactor-monoliths/16-RESEARCH.md
@.planning/phases/16-refactor-monoliths/16-VALIDATION.md

<interfaces>
<!-- Pattern reference: backend/app/routers/purchase_files.py is a canonical extracted router. -->
<!-- Follow its structure: `router = APIRouter(prefix="/api/purchases", tags=["purchase-export"])` -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create purchase_export.py with all export endpoints + constants</name>
  <read_first>
    - backend/app/routers/purchases.py (full file — identify lines to move)
    - backend/app/routers/purchase_files.py (pattern reference — extraction template)
    - .planning/phases/16-refactor-monoliths/16-RESEARCH.md §1.1 and §G-07 (gotchas)
    - .planning/phases/16-refactor-monoliths/16-CONTEXT.md D-07, D-22, D-23, D-24
  </read_first>
  <files>backend/app/routers/purchase_export.py</files>
  <action>
    1. Create `backend/app/routers/purchase_export.py` starting with router definition:
       ```python
       from fastapi import APIRouter, Depends, Query, HTTPException, UploadFile, File
       from fastapi.responses import StreamingResponse
       from sqlalchemy import select, func
       from sqlalchemy.ext.asyncio import AsyncSession
       from io import BytesIO
       from typing import Optional

       from app.database import get_db
       from app.models.purchase import Purchase
       from app.models.subsidy import Subsidy
       from app.models.contractor import Contractor
       from app.auth.jwt import get_current_user, require_role
       try:
           from openpyxl import Workbook, load_workbook
           from openpyxl.styles import Font, PatternFill, Alignment
       except ImportError:
           Workbook = None
           load_workbook = None

       router = APIRouter(prefix="/api/purchases", tags=["purchase-export"])
       ```
    2. MOVE (cut from purchases.py, paste into purchase_export.py) the following identifiers — per G-07 (research lines L193-321 of purchases.py):
       - Constants: `ALL_EXPORT_COLUMNS`, `DEFAULT_EXPORT_COLUMNS`, `_PURCHASE_METHOD_LABELS`, `_PURCHASE_BASIS_LABELS`, `_CONTRACT_TYPE_LABELS`, `_STATUS_LABELS`
       - Helpers: `_get_cell_value`
       - Endpoints: `GET /export/columns`, `GET /export/excel`, `GET /import/template`, `POST /import` (payment/Scroller import)
    3. Preserve EVERY `Depends(require_role(...))` and `Depends(get_current_user)` exactly.
    4. If an endpoint calls `_purchase_to_full` or `_item_to_out` from purchases.py, add: `from app.routers.purchases import _purchase_to_full, _item_to_out`.
    5. Do NOT rename any function. Do NOT change URL paths.
  </action>
  <verify>
    <automated>test -f backend/app/routers/purchase_export.py && grep -q '@router.get("/export/columns")' backend/app/routers/purchase_export.py && grep -q '@router.get("/export/excel")' backend/app/routers/purchase_export.py && grep -q 'ALL_EXPORT_COLUMNS' backend/app/routers/purchase_export.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/routers/purchase_export.py` exists
    - Contains `router = APIRouter(prefix="/api/purchases"` (exact substring)
    - Contains `@router.get("/export/columns")`
    - Contains `@router.get("/export/excel")`
    - Contains `@router.get("/import/template")`
    - Contains `@router.post("/import")`
    - Contains `ALL_EXPORT_COLUMNS = `
    - Contains `DEFAULT_EXPORT_COLUMNS = `
    - File line count ≥ 200 (`wc -l backend/app/routers/purchase_export.py` ≥ 200)
  </acceptance_criteria>
  <done>
    purchase_export.py is a self-contained module with all export/import-payment logic.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove extracted code from purchases.py + update imports</name>
  <read_first>
    - backend/app/routers/purchases.py (current state after Task 1 may already have some removals — verify)
    - backend/app/routers/purchase_export.py (confirm what was moved)
  </read_first>
  <files>backend/app/routers/purchases.py</files>
  <action>
    1. Delete from purchases.py every constant/function/endpoint moved in Task 1.
    2. Search purchases.py for remaining references to moved names (`grep -n "ALL_EXPORT_COLUMNS\|_get_cell_value\|_PURCHASE_METHOD_LABELS" backend/app/routers/purchases.py`) — there MUST be zero matches after this task.
    3. Check other files: `grep -rn "from app.routers.purchases import ALL_EXPORT_COLUMNS\|from app.routers.purchases import _get_cell_value" backend/` — if any, update to `from app.routers.purchase_export import ...`.
    4. Do NOT remove `_purchase_to_full`, `_item_to_out`, `STATUS_ORDER` — these stay in purchases.py per D-20 + G-08.
  </action>
  <verify>
    <automated>! grep -n "async def export_purchases_to_excel\|ALL_EXPORT_COLUMNS =" backend/app/routers/purchases.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ALL_EXPORT_COLUMNS" backend/app/routers/purchases.py` returns 0
    - `grep -c "DEFAULT_EXPORT_COLUMNS" backend/app/routers/purchases.py` returns 0
    - `grep -c '@router.get("/export/' backend/app/routers/purchases.py` returns 0
    - `grep -c '@router.get("/import/template")' backend/app/routers/purchases.py` returns 0
    - `wc -l backend/app/routers/purchases.py` ≤ 2900 (was 3233; export is ~400 lines)
    - No caller file imports removed names from `app.routers.purchases` (grep external files returns 0 hits)
  </acceptance_criteria>
  <done>
    purchases.py no longer owns any export code. All cross-file imports updated.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register purchase_export router in app/__init__.py + verify + commit</name>
  <read_first>
    - backend/app/__init__.py (L281-318 — router registration block)
    - .planning/phases/16-refactor-monoliths/16-RESEARCH.md §5 (insertion point after purchase_approvals)
  </read_first>
  <files>backend/app/__init__.py</files>
  <action>
    1. In `backend/app/__init__.py`, find the line `app.include_router(purchase_approvals.router)`.
    2. Immediately after it, add (preserving indentation):
       ```python
       from app.routers import purchase_export
       app.include_router(purchase_export.router)
       ```
       If `from app.routers import purchase_approvals` already batches imports at top of file, add `purchase_export` to that import line instead. Match the project's existing pattern.
    3. Run local verification:
       - `docker compose build backend` exits 0 (no syntax/import errors)
       - `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py::test_export_mount -q` passes
       - `BASE_URL=http://85.239.53.155 npx playwright test e2e/05-orders.spec.ts` passes (may require deploy push first — follow autodeploy flow)
    4. Commit: `refactor(16-02): extract purchase_export from purchases` with files: `backend/app/routers/purchase_export.py backend/app/routers/purchases.py backend/app/__init__.py`.
  </action>
  <verify>
    <automated>grep -q "app.include_router(purchase_export.router)" backend/app/__init__.py && git log --oneline -1 | grep -qE "refactor\(16-02\): extract purchase_export"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/__init__.py` contains `app.include_router(purchase_export.router)`
    - `backend/app/__init__.py` contains `purchase_export` in imports
    - `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py::test_export_mount -q` exits 0
    - `git log --oneline -1` matches pattern `^[a-f0-9]+ refactor\(16-02\): extract purchase_export`
    - Commit touches exactly 3 files: purchase_export.py (new), purchases.py (modified), app/__init__.py (modified)
  </acceptance_criteria>
  <done>
    Atomic commit landed. Router mounted. Smoke tests green. E2E green.
  </done>
</task>

</tasks>

<verification>
- `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` → all pass
- `BASE_URL=http://85.239.53.155 npx playwright test e2e/05-orders.spec.ts e2e/02-all-pages.spec.ts` → all pass
- `wc -l backend/app/routers/purchase_export.py` ≥ 200 AND ≤ 800 (fits size budget)
</verification>

<success_criteria>
Export logic lives in purchase_export.py. purchases.py is smaller. Zero behaviour change. Commit pushed.
</success_criteria>

<output>
After completion, create `.planning/phases/16-refactor-monoliths/16-02-SUMMARY.md`.
</output>
