---
phase: 16-refactor-monoliths
plan: 13
type: execute
wave: 5
depends_on: [16-12]
files_modified:
  - frontend/src/components/my-tasks/TasksTable.vue
  - frontend/src/components/my-tasks/TasksKanban.vue
  - frontend/src/views/MyTasksView.vue
autonomous: true
requirements: [REFACTOR-03, REFACTOR-05, REFACTOR-07]
must_haves:
  truths:
    - "TasksTable.vue renders v-data-table of tasks (list mode)"
    - "TasksKanban.vue renders kanban columns grouped by status (kanban mode)"
    - "MyTasksView.vue toggles between TasksTable and TasksKanban via v-if on taskViewMode"
    - "Clicking a task still opens the task dialog (emit 'open-task' back to parent)"
    - "Drag-drop in kanban still persists via emit to parent handler"
    - "npm run build exits 0 with zero warnings"
  artifacts:
    - path: "frontend/src/components/my-tasks/TasksTable.vue"
      provides: "v-data-table list view of tasks"
      min_lines: 250
    - path: "frontend/src/components/my-tasks/TasksKanban.vue"
      provides: "Kanban columns grouped by status with drag-drop"
      min_lines: 250
    - path: "frontend/src/views/MyTasksView.vue"
      provides: "Toggles between TasksTable and TasksKanban"
      max_lines: 1400
  key_links:
    - from: "frontend/src/views/MyTasksView.vue"
      to: "frontend/src/components/my-tasks/TasksTable.vue"
      via: "renders TasksTable with :tasks + :selected-org-id props, @open-task emit"
      pattern: "import TasksTable from"
    - from: "frontend/src/views/MyTasksView.vue"
      to: "frontend/src/components/my-tasks/TasksKanban.vue"
      via: "renders TasksKanban with :tasks prop, @open-task + @update-status emits"
      pattern: "import TasksKanban from"
---

<objective>
Extract the Задачи tab into TWO separate components (D-16 explicitly names both; RESEARCH §3.2 rationale: list and kanban share zero DOM structure, two components cleaner than one prop-switched). `TasksTable.vue` (v-data-table) and `TasksKanban.vue` (status columns). Parent toggles visibility by `taskViewMode`. Implements D-16.
</objective>

<execution_context>
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-refactor-monoliths/16-CONTEXT.md
@.planning/phases/16-refactor-monoliths/16-RESEARCH.md
@.planning/phases/16-refactor-monoliths/16-VALIDATION.md

<interfaces>
Parent MyTasksView.vue keeps all apiFetch (D-14 + §3.3) — including task reload after status change.
Props in: tasks array, selectedOrgId number | null, loading boolean, maybe categories list.
Emits out: 'open-task' (taskId: number), 'update-status' (taskId: number, newStatus: string) — for kanban drag-drop.
Two separate components per D-16 and RESEARCH §3.2 — not a single :mode prop.
Pattern reference: PurchaseItemsEditor.vue (Phase 15), OrgSelector.vue (Plan 16-12).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TasksTable.vue (list view of tasks)</name>
  <read_first>
    - frontend/src/views/MyTasksView.vue (identify the `v-if="taskViewMode === 'list'"` block — the v-data-table of tasks with columns: title, assignees, status, due date, org, etc.)
    - frontend/src/components/my-tasks/OrgSelector.vue (pattern reference from Plan 16-12)
    - .planning/phases/16-refactor-monoliths/16-RESEARCH.md §3.1, §3.2, §3.3
    - .planning/phases/16-refactor-monoliths/16-CONTEXT.md D-16
  </read_first>
  <files>frontend/src/components/my-tasks/TasksTable.vue</files>
  <action>
    1. Create `frontend/src/components/my-tasks/TasksTable.vue` as a Vue 3 SFC.
    2. Template: COPY BYTE-FOR-BYTE the `v-if="taskViewMode === 'list'"` block from MyTasksView.vue (the v-data-table of tasks). Replace `@click:row="openTaskDialog(item)"` with `@click:row="(_, { item }) => $emit('open-task', item.id)"` (or matching event signature).
    3. Script setup: define `Task` interface (or import from shared types if available) matching the shape used in MyTasksView.vue's `tasks` ref. Declare `defineProps` typed as { tasks: Task[]; selectedOrgId: number | null; loading: boolean }. Declare `defineEmits` with 'open-task' (taskId: number).
    4. Style scoped: MOVE (or duplicate) selectors targeting only the table DOM — e.g. `.task-row`, `.task-status-chip`, any v-data-table custom classes used only here.
    5. No apiFetch. No axios. Pure presentation.
  </action>
  <verify>
    <automated>test -f frontend/src/components/my-tasks/TasksTable.vue && grep -q "defineProps<" frontend/src/components/my-tasks/TasksTable.vue && grep -q "defineEmits<" frontend/src/components/my-tasks/TasksTable.vue && grep -q "open-task" frontend/src/components/my-tasks/TasksTable.vue && grep -q "v-data-table" frontend/src/components/my-tasks/TasksTable.vue && ! grep -q "apiFetch" frontend/src/components/my-tasks/TasksTable.vue</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/my-tasks/TasksTable.vue` exists
    - Contains `<script setup lang="ts">`
    - Contains `defineProps<` and `defineEmits<`
    - Contains `open-task` emit name
    - Contains `v-data-table` (Vuetify table)
    - Does NOT contain `apiFetch`
    - `wc -l frontend/src/components/my-tasks/TasksTable.vue` ≥ 250
  </acceptance_criteria>
  <done>
    TasksTable.vue self-contained list view.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TasksKanban.vue (kanban view with drag-drop)</name>
  <read_first>
    - frontend/src/views/MyTasksView.vue (identify the `v-if="taskViewMode === 'kanban'"` block — typically v-for over status columns, each column is draggable list of task cards)
    - frontend/src/components/my-tasks/TasksTable.vue (just created — pattern reference)
    - .planning/phases/16-refactor-monoliths/16-CONTEXT.md D-16
  </read_first>
  <files>frontend/src/components/my-tasks/TasksKanban.vue</files>
  <action>
    1. Create `frontend/src/components/my-tasks/TasksKanban.vue` as a Vue 3 SFC.
    2. Template: COPY BYTE-FOR-BYTE the `v-if="taskViewMode === 'kanban'"` block from MyTasksView.vue — status columns, draggable task cards. Replace any direct `onDragEnd(task, newStatus)` call with `$emit('update-status', task.id, newStatus)`. Replace card click with `@click="$emit('open-task', task.id)"`.
    3. Script setup: reuse `Task` interface (import from shared types or redefine locally matching MyTasksView). Declare `defineProps` typed as { tasks: Task[]; selectedOrgId: number | null }. Declare `defineEmits` with 'open-task' (taskId: number) and 'update-status' (taskId: number, newStatus: string).
    4. If the kanban uses a third-party drag library (vuedraggable / sortable) — preserve the import and usage exactly. Do NOT introduce new drag library.
    5. Style scoped: MOVE kanban-specific CSS (.kanban-column, .kanban-card, etc.).
    6. No apiFetch.
  </action>
  <verify>
    <automated>test -f frontend/src/components/my-tasks/TasksKanban.vue && grep -q "defineProps<" frontend/src/components/my-tasks/TasksKanban.vue && grep -q "defineEmits<" frontend/src/components/my-tasks/TasksKanban.vue && grep -q "open-task" frontend/src/components/my-tasks/TasksKanban.vue && grep -q "update-status" frontend/src/components/my-tasks/TasksKanban.vue && ! grep -q "apiFetch" frontend/src/components/my-tasks/TasksKanban.vue</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/my-tasks/TasksKanban.vue` exists
    - Contains `<script setup lang="ts">`
    - Contains `defineProps<` and `defineEmits<`
    - Contains `open-task` AND `update-status` emit names
    - Does NOT contain `apiFetch`
    - `wc -l frontend/src/components/my-tasks/TasksKanban.vue` ≥ 250
  </acceptance_criteria>
  <done>
    TasksKanban.vue self-contained kanban view with drag-drop emits.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire TasksTable + TasksKanban into MyTasksView.vue and verify</name>
  <read_first>
    - frontend/src/views/MyTasksView.vue (current state post-16-12)
  </read_first>
  <files>frontend/src/views/MyTasksView.vue</files>
  <action>
    1. Add imports in `<script setup>`:
       ```
       import TasksTable from '@/components/my-tasks/TasksTable.vue'
       import TasksKanban from '@/components/my-tasks/TasksKanban.vue'
       ```
    2. REPLACE the two inline blocks in template with:
       ```
       <TasksTable
         v-if="taskViewMode === 'list'"
         :tasks="tasks"
         :selected-org-id="selectedOrgId"
         :loading="loading"
         @open-task="openTaskDialog"
       />
       <TasksKanban
         v-if="taskViewMode === 'kanban'"
         :tasks="tasks"
         :selected-org-id="selectedOrgId"
         @open-task="openTaskDialog"
         @update-status="handleUpdateTaskStatus"
       />
       ```
    3. Ensure `handleUpdateTaskStatus(taskId, newStatus)` exists in script setup — extract from the original inline drag-end handler. Its body: apiFetch PATCH, refetch tasks.
    4. Remove from MyTasksView.vue `<style scoped>` CSS selectors that moved exclusively to children.
    5. Build: `docker exec vsks_crm-frontend-1 sh -c "cd /app && npm run build"` → exit 0, zero warnings.
    6. Line count: `wc -l frontend/src/views/MyTasksView.vue` ≤ 1400.
    7. Push to deploy. E2E: `BASE_URL=http://85.239.53.155 npx playwright test e2e/02-all-pages.spec.ts e2e/05-orders.spec.ts` → pass.
    8. Commit: `refactor(16-13): extract TasksTable + TasksKanban from MyTasksView` — 3 files.
  </action>
  <verify>
    <automated>grep -q "import TasksTable from" frontend/src/views/MyTasksView.vue && grep -q "import TasksKanban from" frontend/src/views/MyTasksView.vue && grep -q "<TasksTable" frontend/src/views/MyTasksView.vue && grep -q "<TasksKanban" frontend/src/views/MyTasksView.vue && git log --oneline -1 | grep -qE "refactor\(16-13\): extract TasksTable"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/views/MyTasksView.vue` contains `import TasksTable from`
    - `frontend/src/views/MyTasksView.vue` contains `import TasksKanban from`
    - `frontend/src/views/MyTasksView.vue` contains `<TasksTable` and `<TasksKanban` in template
    - `wc -l frontend/src/views/MyTasksView.vue` ≤ 1400
    - `docker exec vsks_crm-frontend-1 sh -c "cd /app && npm run build"` exits 0 with 0 warnings
    - `BASE_URL=http://85.239.53.155 npx playwright test e2e/02-all-pages.spec.ts` exits 0
    - `git log --oneline -1` matches `^[a-f0-9]+ refactor\(16-13\): extract TasksTable`
    - Commit diff: exactly 3 files
  </acceptance_criteria>
  <done>
    Tasks tab delegated to two child components. Build green. E2E green.
  </done>
</task>

</tasks>

<verification>
- `docker exec vsks_crm-frontend-1 sh -c "cd /app && npm run build"` exits 0 with 0 warnings
- `BASE_URL=http://85.239.53.155 npx playwright test e2e/02-all-pages.spec.ts e2e/05-orders.spec.ts` → pass
- `wc -l frontend/src/components/my-tasks/TasksTable.vue` ≥ 250
- `wc -l frontend/src/components/my-tasks/TasksKanban.vue` ≥ 250
- `wc -l frontend/src/views/MyTasksView.vue` ≤ 1400
</verification>

<success_criteria>
Tasks tab split into TasksTable + TasksKanban. Commit: `refactor(16-13): extract TasksTable + TasksKanban from MyTasksView`.
</success_criteria>

<output>
After completion, create `.planning/phases/16-refactor-monoliths/16-13-SUMMARY.md`.
</output>
