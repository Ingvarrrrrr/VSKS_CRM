---
phase: 09-vnutrenniy-chat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/chat_manager.py
  - nginx/nginx.conf
autonomous: true
requirements: [CHAT-06]

must_haves:
  truths:
    - "ConnectionManager хранит active: Dict[int, List[WebSocket]] для поддержки нескольких вкладок"
    - "manager.send_to_user(user_id, data) не падает если пользователь оффлайн"
    - "nginx /api location содержит Upgrade и Connection headers для WebSocket"
    - "proxy_read_timeout 86400s задан — WS соединение не закрывается через 60s"
  artifacts:
    - path: "backend/app/chat_manager.py"
      provides: "ConnectionManager singleton + manager instance"
      exports: ["ConnectionManager", "manager"]
    - path: "nginx/nginx.conf"
      provides: "WebSocket proxy support for /api/ws/chat"
      contains: "proxy_set_header Upgrade"
  key_links:
    - from: "nginx/nginx.conf"
      to: "backend:8000"
      via: "proxy_pass с Upgrade headers"
      pattern: "proxy_set_header Upgrade \\$http_upgrade"
---

<objective>
Создать in-memory ConnectionManager для WebSocket broadcast и обновить nginx для поддержки WebSocket Upgrade.

Purpose: ConnectionManager — единый реестр активных WS-соединений; без него сообщения нельзя доставить в реальном времени. nginx без Upgrade headers будет сбрасывать WS-соединения с 502.
Output: backend/app/chat_manager.py с singleton `manager`; обновлённый nginx.conf.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-vnutrenniy-chat/09-RESEARCH.md

<interfaces>
<!-- Текущий nginx.conf — /api location без WebSocket headers -->
```nginx
location /api {
    set $backend_upstream http://backend:8000;
    proxy_pass $backend_upstream;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header Authorization $http_authorization;
    proxy_pass_header Authorization;
    # WebSocket headers ОТСУТСТВУЮТ — нужно добавить
}

# /n8n/ location — пример уже работающего WS proxy в этом проекте:
location /n8n/ {
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ConnectionManager</name>
  <files>backend/app/chat_manager.py</files>
  <action>
Создать `backend/app/chat_manager.py` со следующим содержимым:

```python
"""
In-memory WebSocket connection manager for chat.
Stores user_id -> list of active WebSocket connections (multi-tab support).
Single-instance Docker deployment — no Redis needed.
"""
from __future__ import annotations

import logging
from typing import Dict, List

from fastapi import WebSocket

logger = logging.getLogger(__name__)


class ConnectionManager:
    def __init__(self) -> None:
        # user_id -> list of active websocket connections (multi-tab support)
        self.active: Dict[int, List[WebSocket]] = {}

    async def connect(self, user_id: int, ws: WebSocket) -> None:
        await ws.accept()
        self.active.setdefault(user_id, []).append(ws)
        logger.debug("WS connected: user_id=%s, total_conns=%s", user_id, len(self.active.get(user_id, [])))

    def disconnect(self, user_id: int, ws: WebSocket) -> None:
        conns = self.active.get(user_id, [])
        if ws in conns:
            conns.remove(ws)
        if not conns:
            self.active.pop(user_id, None)
        logger.debug("WS disconnected: user_id=%s", user_id)

    async def send_to_user(self, user_id: int, data: dict) -> None:
        """Push JSON message to all connections of a user. Silent on offline/error."""
        dead: list[WebSocket] = []
        for ws in self.active.get(user_id, []):
            try:
                await ws.send_json(data)
            except Exception:
                dead.append(ws)
        for ws in dead:
            self.disconnect(user_id, ws)

    async def send_to_users(self, user_ids: list[int], data: dict) -> None:
        """Push JSON message to all connections of multiple users."""
        for uid in user_ids:
            await self.send_to_user(uid, data)

    def is_online(self, user_id: int) -> bool:
        return bool(self.active.get(user_id))


# Module-level singleton — imported by routers/chat.py
manager = ConnectionManager()
```
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'backend'); from app.chat_manager import manager, ConnectionManager; print('ConnectionManager OK')" 2>/dev/null || grep -c "ConnectionManager\|manager = " /c/Users/1/Desktop/Cursor/VSKS_CRM/backend/app/chat_manager.py</automated>
  </verify>
  <done>Файл создан; содержит класс ConnectionManager с методами connect, disconnect, send_to_user, send_to_users, is_online; содержит singleton `manager = ConnectionManager()` в конце файла.</done>
</task>

<task type="auto">
  <name>Task 2: Update nginx.conf for WebSocket support</name>
  <files>nginx/nginx.conf</files>
  <action>
Открыть `nginx/nginx.conf`. Найти блок `location /api {`. Добавить внутрь блока следующие строки ПОСЛЕ существующих `proxy_set_header` строк и `proxy_pass_header Authorization`:

```nginx
        # WebSocket support — required for /api/ws/chat
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
```

Результирующий блок должен выглядеть так:
```nginx
        location /api {
            set $backend_upstream http://backend:8000;
            proxy_pass $backend_upstream;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header Authorization $http_authorization;
            proxy_pass_header Authorization;
            # WebSocket support — required for /api/ws/chat
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_read_timeout 86400s;
            proxy_send_timeout 86400s;
        }
```

ВАЖНО: Не трогать другие location блоки (/n8n/, /deploy/, /). Изменять ТОЛЬКО блок `location /api`.
  </action>
  <verify>
    <automated>grep -A 12 "location /api" /c/Users/1/Desktop/Cursor/VSKS_CRM/nginx/nginx.conf | grep -c "Upgrade"</automated>
  </verify>
  <done>grep находит "Upgrade" внутри блока location /api; grep находит "proxy_read_timeout 86400s" в nginx.conf; существующие блоки /n8n/ и /deploy/ не тронуты.</done>
</task>

</tasks>

<verification>
1. `grep "Upgrade" nginx/nginx.conf` — должна быть строка `proxy_set_header Upgrade $http_upgrade;` внутри `/api` блока
2. `grep "86400" nginx/nginx.conf` — должны быть строки read/send timeout
3. `grep -c "location" nginx/nginx.conf` — количество location блоков не изменилось (должно быть 4: /api, /n8n/, /deploy/, /)
4. `grep "send_to_user\|send_to_users\|is_online" backend/app/chat_manager.py` — все методы присутствуют
</verification>

<success_criteria>
- backend/app/chat_manager.py создан с ConnectionManager и singleton manager
- nginx.conf содержит Upgrade, Connection, proxy_read_timeout 86400s внутри /api location
- Остальные nginx location блоки не тронуты
- ConnectionManager поддерживает multi-tab (список WS per user_id)
- send_to_user silent on error (не бросает исключение если пользователь оффлайн)
</success_criteria>

<output>
После завершения создать `.planning/phases/09-vnutrenniy-chat/09-02-SUMMARY.md`
</output>
