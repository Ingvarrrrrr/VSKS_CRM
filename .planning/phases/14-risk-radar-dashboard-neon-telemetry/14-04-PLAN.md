---
phase: 14-risk-radar-dashboard-neon-telemetry
plan: 04
plan_id: 14-04
type: execute
wave: 3
depends_on: [14-03]
files_modified: []
files_created: []
autonomous: false
estimated_tokens: 8000
requirements:
  - RISK-RADAR-09  # Polish pass — build green, UI-SPEC compliance grep audit
  - RISK-RADAR-10  # Theme/UAT checkpoint — user verifies 4 combos: {dark, light} × {classic, radar}
requirements_addressed:
  - "RISK-RADAR-09: Automated verification — build passes, no DashboardView.vue diff, UI-SPEC tokens/copy present"
  - "RISK-RADAR-10: Manual human verification — 4 theme×mode combos render correctly, toggle persists per user"

must_haves:
  truths:
    - "`cd frontend && npm run build` exits 0 with zero errors mentioning Phase 14 files"
    - "`git diff --exit-code frontend/src/views/DashboardView.vue` returns 0 (D-01 final compliance)"
    - "All UI-SPEC copywriting literals present in RiskRadarView.vue (grep-verified)"
    - "User confirms: dark+radar renders full-neon, light+radar renders muted palette, both readable"
    - "User confirms: toggle 'Классик' navigates to /dashboard, toggle 'Радар' returns, localStorage key `dashboard-mode-{userId}` updates"
    - "User confirms: metric card click routes to correct drill-down destination per UI-SPEC §Metric Card Click"
    - "User confirms: alerts ticker hidden when no scores ≥ 40, visible and clickable otherwise"
  artifacts:
    - path: ".planning/phases/14-risk-radar-dashboard-neon-telemetry/14-04-SUMMARY.md"
      provides: "Phase 14 final verification report — build status, grep audit results, user sign-off on 4 combos"
      contains: "VERIFIED"
  key_links:
    - from: "14-04 automated grep audit"
      to: "UI-SPEC.md copywriting + token contracts"
      via: "grep against RiskRadarView.vue, RiskMetricCard.vue, AlertsTicker.vue"
      pattern: "Risk Radar|Мониторинг рисков|--rr-ok|--rr-critical"
    - from: "14-04 human checkpoint"
      to: "User verification of 4 theme×mode combos"
      via: "Dev server at http://localhost:5173/dashboard/radar"
      pattern: "manual"
---

<objective>
Final verification for Phase 14. Two tasks:
1. Automated audit: build passes, UI-SPEC contract satisfied (tokens, copy, charts all present), D-01 byte-exact compliance on DashboardView.vue.
2. Human checkpoint: user visually verifies the 4 theme×mode combos and the toggle behavior across browser reloads.

This plan does NOT create or modify any source files. It verifies what 14-01/02/03 produced and fails loudly if anything slipped.

Purpose:
- Lock in Phase 14 before it ships. No more silent regressions.
- Honor CONTEXT.md D-03 (both themes first-class) by making the user confirm each combo — grep alone cannot verify visual quality.
- Produce SUMMARY.md that documents the verified state for future milestones.

Output:
- .planning/phases/14-risk-radar-dashboard-neon-telemetry/14-04-SUMMARY.md (new) with pass/fail per check.
</objective>

<execution_context>
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-risk-radar-dashboard-neon-telemetry/14-CONTEXT.md
@.planning/phases/14-risk-radar-dashboard-neon-telemetry/14-UI-SPEC.md
@.planning/phases/14-risk-radar-dashboard-neon-telemetry/14-01-SUMMARY.md
@.planning/phases/14-risk-radar-dashboard-neon-telemetry/14-02-SUMMARY.md
@.planning/phases/14-risk-radar-dashboard-neon-telemetry/14-03-SUMMARY.md
@frontend/src/views/RiskRadarView.vue
@frontend/src/components/RiskMetricCard.vue
@frontend/src/components/AlertsTicker.vue
@frontend/src/composables/useRiskScores.ts
@frontend/src/composables/useDashboardMode.ts
@frontend/src/router/index.ts

<interfaces>
<!-- This plan reads code produced by Plans 14-01/02/03. No new contracts. -->
<!-- Key files to audit (all must exist before this plan runs): -->
/* - frontend/src/views/RiskRadarView.vue (from 14-03)
 * - frontend/src/components/RiskMetricCard.vue (from 14-02)
 * - frontend/src/components/AlertsTicker.vue (from 14-02)
 * - frontend/src/composables/useRiskScores.ts (from 14-01)
 * - frontend/src/composables/useDashboardMode.ts (from 14-01)
 * - frontend/src/router/index.ts (modified in 14-01)
 */
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated audit — build, UI-SPEC grep contract, D-01 byte-check</name>
  <read_first>
    - frontend/src/views/RiskRadarView.vue (ENTIRE FILE — you are auditing it)
    - frontend/src/components/RiskMetricCard.vue
    - frontend/src/components/AlertsTicker.vue
    - frontend/src/composables/useRiskScores.ts
    - frontend/src/composables/useDashboardMode.ts
    - frontend/src/router/index.ts (changed section — /dashboard/radar entry)
    - .planning/phases/14-risk-radar-dashboard-neon-telemetry/14-UI-SPEC.md — sections "Copywriting Contract", "Color → Neon Telemetry Tokens", "Chart Specifications", "Toggle Mechanism — Complete Specification"
    - .planning/phases/14-risk-radar-dashboard-neon-telemetry/14-CONTEXT.md — D-01/02/03/04
  </read_first>
  <files></files>
  <action>
    Run the following audit as a single bash pipeline from the repo root. DO NOT skip any check — they are cumulative. Write the pass/fail outcomes into `.planning/phases/14-risk-radar-dashboard-neon-telemetry/14-04-SUMMARY.md` as you go (use the template at the bottom of this action block).

    === CHECK 1: Build ===
    ```bash
    cd frontend && npm run build 2>&1 | tail -40
    ```
    PASS if exit code is 0 and no line contains `error TS` or `error during build` referencing any of: `RiskRadarView.vue`, `RiskMetricCard.vue`, `AlertsTicker.vue`, `useRiskScores.ts`, `useDashboardMode.ts`, `router/index.ts`.

    === CHECK 2: D-01 byte-exact DashboardView ===
    ```bash
    git diff --exit-code frontend/src/views/DashboardView.vue
    ```
    PASS if exit code is 0 (empty diff). FAIL if any byte changed — blocker.

    === CHECK 3: Route registered ===
    ```bash
    grep -n "path: '/dashboard/radar'" frontend/src/router/index.ts
    grep -n "name: 'radar-dashboard'" frontend/src/router/index.ts
    grep -n "import('../views/RiskRadarView.vue')" frontend/src/router/index.ts
    ```
    All three must return exactly one line. Also verify `! grep -q "^import RiskRadarView" frontend/src/router/index.ts` (no static top-level import — must be lazy).

    === CHECK 4: Composable contracts ===
    ```bash
    grep -q "export function useRiskScores" frontend/src/composables/useRiskScores.ts
    grep -q "export function severityFromScore" frontend/src/composables/useRiskScores.ts
    grep -q "export function useDashboardMode" frontend/src/composables/useDashboardMode.ts
    grep -q "'dashboard-mode-'" frontend/src/composables/useDashboardMode.ts
    ```
    All four must pass.

    === CHECK 5: Risk metric keys + weights (UI-SPEC §Overall Risk Score) ===
    ```bash
    for k in budget_overrun contract_delays stalled_wishes framework_saturation feo_imbalance overdue_payments; do
      grep -q "'$k'" frontend/src/composables/useRiskScores.ts || echo "MISSING KEY: $k"
    done
    for w in 0.25 0.20 0.15 0.10; do
      grep -q "$w" frontend/src/composables/useRiskScores.ts || echo "MISSING WEIGHT: $w"
    done
    ```
    No "MISSING" lines allowed.

    === CHECK 6: API endpoints reached ===
    ```bash
    grep -q "apiFetch<ChartsResponse>('/dashboard/charts')" frontend/src/composables/useRiskScores.ts
    grep -q "apiFetch.*'/contracts'" frontend/src/composables/useRiskScores.ts
    grep -q "apiFetch.*'/wishes'" frontend/src/composables/useRiskScores.ts
    grep -q "apiFetch.*'/purchases'" frontend/src/composables/useRiskScores.ts
    ```

    === CHECK 7: UI-SPEC copy present in RiskRadarView.vue ===
    ```bash
    for phrase in "Risk Radar" "Мониторинг рисков" "Классик" "Радар" "Общий риск" "Превышение бюджета" \
                  "Просрочки договоров" "Зависшие заявки" "Насыщение рамочных" "Дисбаланс ФЭО" \
                  "Просрочки оплат" "Не удалось загрузить данные" "Нет данных для анализа рисков" \
                  "Выберите субсидию и год" "Обновлено"; do
      grep -q "$phrase" frontend/src/views/RiskRadarView.vue || echo "MISSING COPY: $phrase"
    done
    ```
    Labels for metrics 1, 5, 6 may live in useRiskScores.ts instead — if not in the view, grep `frontend/src/composables/useRiskScores.ts` as a fallback. No "MISSING COPY" lines allowed after fallback check.

    === CHECK 8: Both theme palettes defined (D-03, D-04) ===
    ```bash
    # Dark palette hex in RiskRadarView.vue scoped style or script (ApexCharts colors)
    for hex in "#22D3EE" "#FBBF24" "#F97316" "#F43F5E"; do
      grep -q "$hex" frontend/src/views/RiskRadarView.vue || echo "MISSING DARK HEX: $hex"
    done
    # Light palette hex
    for hex in "#0891B2" "#B45309" "#C2410C" "#BE123C"; do
      grep -q "$hex" frontend/src/views/RiskRadarView.vue || echo "MISSING LIGHT HEX: $hex"
    done
    # Light-theme CSS override
    grep -q '.risk-radar\[data-theme="light"\]' frontend/src/views/RiskRadarView.vue || echo "MISSING LIGHT OVERRIDE"
    ```

    === CHECK 9: Charts present (UI-SPEC §Chart Specifications) ===
    ```bash
    grep -c 'type="polarArea"' frontend/src/views/RiskRadarView.vue  # expect >= 1
    grep -c 'type="radialBar"' frontend/src/views/RiskRadarView.vue  # expect >= 1
    grep -q 'mode: isDark.value' frontend/src/views/RiskRadarView.vue   # theme passed to ApexCharts
    ```

    === CHECK 10: Component wiring ===
    ```bash
    grep -q '<RiskMetricCard' frontend/src/views/RiskRadarView.vue
    grep -q '<AlertsTicker' frontend/src/views/RiskRadarView.vue
    grep -q 'v-for="s in scores"' frontend/src/views/RiskRadarView.vue
    grep -q ':items="tickerItems"' frontend/src/views/RiskRadarView.vue
    grep -q ':visible="tickerVisible"' frontend/src/views/RiskRadarView.vue
    ```

    === CHECK 11: Drill-down routes present (UI-SPEC §Metric Card Click) ===
    ```bash
    for target in "/contracts?filter=expiring" "/wishes?filter=stalled" "/contracts?filter=saturated" "/orders?filter=overdue_payment"; do
      grep -q "router.push('$target')" frontend/src/views/RiskRadarView.vue || echo "MISSING DRILL: $target"
    done
    ```

    === CHECK 12: No hardcoded neon hex inside components (all via var(--rr-*)) ===
    ```bash
    # RiskMetricCard and AlertsTicker must NOT inline neon hex — they consume CSS vars
    grep -E "#22D3EE|#FBBF24|#F97316|#F43F5E|#0891B2|#B45309|#C2410C|#BE123C" frontend/src/components/RiskMetricCard.vue && echo "HARDCODED HEX IN CARD"
    grep -E "#22D3EE|#FBBF24|#F97316|#F43F5E|#0891B2|#B45309|#C2410C|#BE123C" frontend/src/components/AlertsTicker.vue && echo "HARDCODED HEX IN TICKER"
    ```
    No output = PASS. Any output = FAIL (violates D-04 adaptability).

    === CHECK 13: Typography contract (max 4 sizes, 2 weights) ===
    ```bash
    # Inside the scoped <style> of RiskRadarView.vue, only 26/20/14/12 px allowed
    awk '/<style scoped>/,/<\/style>/' frontend/src/views/RiskRadarView.vue | grep -oE "font-size: [0-9]+px" | sort -u
    # Expected output is subset of: font-size: 12px / font-size: 14px / font-size: 20px / font-size: 26px
    # FAIL if any other size appears (e.g., 16px, 18px, 24px).
    # Weights: only 400 and 700
    awk '/<style scoped>/,/<\/style>/' frontend/src/views/RiskRadarView.vue | grep -oE "font-weight: [0-9]+" | sort -u
    # Expected subset of: font-weight: 400 / font-weight: 700
    ```

    === CHECK 14: Accessibility surface ===
    ```bash
    grep -q 'role="button"' frontend/src/components/RiskMetricCard.vue
    grep -q 'tabindex="0"' frontend/src/components/RiskMetricCard.vue
    grep -q 'role="marquee"' frontend/src/components/AlertsTicker.vue
    grep -q 'aria-live="off"' frontend/src/components/AlertsTicker.vue
    grep -q 'role="main"' frontend/src/views/RiskRadarView.vue
    grep -q 'aria-label="Обновить данные Risk Radar"' frontend/src/views/RiskRadarView.vue
    ```

    === WRITE SUMMARY ===
    After all checks, write `.planning/phases/14-risk-radar-dashboard-neon-telemetry/14-04-SUMMARY.md` using this template. DO NOT invent outcomes — record actual results.

    ```markdown
    # Phase 14 — Final Verification Summary

    **Date:** {YYYY-MM-DD}
    **Auditor:** gsd-executor (Claude Sonnet)

    ## Automated Checks

    | # | Check | Result |
    |---|-------|--------|
    | 1 | `npm run build` exits 0 | PASS / FAIL |
    | 2 | DashboardView.vue byte-exact (D-01) | PASS / FAIL |
    | 3 | Route /dashboard/radar registered, lazy-loaded | PASS / FAIL |
    | 4 | Composables exported: useRiskScores, severityFromScore, useDashboardMode | PASS / FAIL |
    | 5 | 6 metric keys + 4 weights present | PASS / FAIL |
    | 6 | 4 API endpoints called | PASS / FAIL |
    | 7 | UI-SPEC copywriting present | PASS / FAIL |
    | 8 | Dark + light palettes defined; light override present (D-03, D-04) | PASS / FAIL |
    | 9 | Polar + radial charts present; isDark passed to theme | PASS / FAIL |
    | 10 | RiskMetricCard + AlertsTicker wired with correct props | PASS / FAIL |
    | 11 | 4 drill-down routes present | PASS / FAIL |
    | 12 | No hardcoded neon hex in RiskMetricCard.vue / AlertsTicker.vue | PASS / FAIL |
    | 13 | Typography: only sizes 12/14/20/26 and weights 400/700 | PASS / FAIL |
    | 14 | Accessibility roles/aria present | PASS / FAIL |

    ## Build Output (tail)

    ```
    {paste final 10 lines of npm run build output}
    ```

    ## Failures (if any)

    - {fill in with any FAIL rows from above; include exact grep output}

    ## D-01 Compliance

    `git diff frontend/src/views/DashboardView.vue` output:
    ```
    {empty if compliant}
    ```

    ## Ready for Human Checkpoint

    - [ ] Task 2 pending (checkpoint:human-verify)
    ```

    Do NOT proceed past this task if any automated check fails. Report failures to the user, apply fixes (or request new plan if fixes exceed scope), and re-run this task.

    Addresses: RISK-RADAR-09.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20 && cd .. && git diff --exit-code frontend/src/views/DashboardView.vue && grep -q "path: '/dashboard/radar'" frontend/src/router/index.ts && grep -q '<apexchart' frontend/src/views/RiskRadarView.vue && grep -q 'role="main"' frontend/src/views/RiskRadarView.vue && test -f .planning/phases/14-risk-radar-dashboard-neon-telemetry/14-04-SUMMARY.md && grep -q "VERIFIED\|PASS" .planning/phases/14-risk-radar-dashboard-neon-telemetry/14-04-SUMMARY.md && echo "AUDIT_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits 0
    - `git diff --exit-code frontend/src/views/DashboardView.vue` exits 0 (D-01 confirmed)
    - Route `/dashboard/radar` lazy-loaded present in router/index.ts
    - All 14 automated checks above pass (evidence in 14-04-SUMMARY.md)
    - File .planning/phases/14-risk-radar-dashboard-neon-telemetry/14-04-SUMMARY.md exists with all 14 check rows filled in
    - Any FAIL row documented with actual grep output and a follow-up action (fix now or escalate)
    - No hardcoded neon hex codes in RiskMetricCard.vue or AlertsTicker.vue
    - Only font-size values 12px / 14px / 20px / 26px in RiskRadarView.vue scoped styles
    - Only font-weight values 400 / 700 in RiskRadarView.vue scoped styles
  </acceptance_criteria>
  <done>
    14-04-SUMMARY.md exists with every automated check marked PASS. Any FAIL is either fixed by a small amendment or escalated to the user before the human-verify task runs.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — 4 theme×mode combos + toggle persistence</name>
  <what-built>
    All of Phase 14: RiskRadarView.vue at /dashboard/radar, toggle chip group persisting to localStorage per user, both Vuetify themes (dark + light) with adapted neon palette, 6 risk metric cards with live scores, polar area chart, radial gauge, alerts ticker.
  </what-built>
  <how-to-verify>
    **Prerequisite:** dev server running.
    ```bash
    cd frontend && npm run dev
    # leave running — note the URL, typically http://localhost:5173
    ```
    Sign in as a non-employee user (superadmin, admin, or manager — employee role is blocked from Dashboard).

    **Combo 1 — Dark + Radar (primary target):**
    1. Open http://localhost:5173/dashboard/radar in Chrome or Firefox.
    2. Open DevTools → Application → Local Storage. Confirm key `dashboard-mode-{yourUserId}` exists with value `"radar"`.
    3. Confirm visual:
       - Title "Risk Radar" with gradient animation
       - Subtitle "ВСКС · Мониторинг рисков · {year}"
       - Year chip group (current year selected), subsidy multi-select, refresh button, toggle chip group (Радар chip active, Классик inactive)
       - Quick subsidy chips bar below header (if any subsidies exist for selected year)
       - Radar panel (left, ~340px): polar area chart with 6 slices in severity colors, radial gauge below ("Общий риск"), "Обновлено HH:MM" row
       - 6 metric cards (right, 2×3 grid): each shows score 0–100, label, internal progress bar in severity color, short description
       - Alerts ticker ONLY if any score ≥ 40: horizontal scrolling strip at bottom, critical items with ⚠ icons
       - No layout breaks, no overlapping elements

    **Combo 2 — Dark + Classic:**
    1. Click "Классик" chip in the toggle.
    2. Confirm: URL changes to `/dashboard`, existing Dashboard renders normally (the 2142-line view should look exactly as before Phase 14).
    3. Confirm localStorage key `dashboard-mode-{userId}` now equals `"classic"`.
    4. Confirm: DashboardView did NOT visually change vs. before Phase 14 (a reference screenshot is acceptable evidence).

    **Combo 3 — Light + Radar:**
    1. Navigate back to `/dashboard/radar` (browser back or directly).
    2. Toggle Vuetify theme to light (however this project exposes it — typically AppBar theme toggle).
    3. Confirm visual:
       - Palette shifts to MUTED tones per UI-SPEC §Color → Neon Telemetry Tokens (Light column): cyan-700, amber-700, orange-700, rose-700. NOT bright neon.
       - Polar area chart slices use muted colors (readable on white background).
       - Radial gauge color matches overall severity in the muted palette.
       - Card border glows are SUBTLE (no blinding neon).
       - Alerts ticker background shifts to soft `--rr-high` tint (8% opacity).
       - Text is readable (high contrast): dark text on white background.
       - No element becomes invisible (e.g., cyan-400 text on white would fail — that's why we use cyan-700 in light).

    **Combo 4 — Light + Classic:**
    1. Click "Классик" while in light theme.
    2. Confirm URL = /dashboard, DashboardView renders normally in light theme.
    3. Confirm localStorage key persists as `"classic"`.

    **Persistence check:**
    1. Close the tab, reopen the browser, visit http://localhost:5173/ (root).
    2. After login redirect, confirm you land on whichever mode (`/dashboard` or `/dashboard/radar`) the composable's `syncRouteWithMode` decides. For typical Phase 14 behavior: the landing page routes to `/dashboard` by default, but the stored mode should be available to any component that calls `useDashboardMode`.

    **Drill-down spot check:**
    1. From /dashboard/radar, click ONE non-ok metric card (any severity ≥ warn). Confirm it navigates to the correct destination per UI-SPEC §Metric Card Click:
       - Budget Overrun → /dashboard?drill=budget_overrun (classic dashboard opens with query)
       - Contract Delays → /contracts?filter=expiring
       - Stalled Wishes → /wishes?filter=stalled
       - Framework Saturation → /contracts?filter=saturated
       - FEO Imbalance → /dashboard?drill=feo_imbalance
       - Overdue Payments → /orders?filter=overdue_payment
    2. Destination view may ignore the ?filter or ?drill query param — that is acceptable per UI-SPEC §Metric Card Click "pass-through" rule.

    **Accessibility spot check (optional but recommended):**
    1. Tab through the page with keyboard. Metric cards should receive focus with a visible focus ring.
    2. Press Enter on a focused card — it should trigger the same navigation as a click.

    **Expected outcome:** All four combos render correctly, toggle persists across sessions, no console errors in DevTools, DashboardView.vue visually identical to pre-Phase-14 state.

    **If any combo fails:** describe the failure precisely (which combo, what you saw, what UI-SPEC section it violates). Claude will create a gap-closure plan.
  </how-to-verify>
  <resume-signal>
    Type `approved` if all four combos pass, the toggle persists correctly, and at least one drill-down works.
    Type `issues: {combo} {description}` if any combo fails or has visual defects — include which section of UI-SPEC.md was violated if you can pinpoint it.
    Type `defer: {topic}` if you want to accept Phase 14 as-is but flag an issue for a follow-up phase.
  </resume-signal>
  <files>
    .planning/phases/14-risk-radar-dashboard-neon-telemetry/14-04-SUMMARY.md
  </files>
  <action>
    Human checkpoint — see <how-to-verify> above for the full verification script.

    Executor responsibilities:
    1. Start the dev server: `cd frontend && npm run dev`.
    2. Open the provided URL and wait for the user to complete all four theme×mode combos plus the persistence + drill-down spot checks described above.
    3. Do NOT proceed until the user responds with `approved`, `issues: ...`, or `defer: ...` per the resume-signal contract.
    4. After the user replies:
       - If `approved` → update 14-04-SUMMARY.md: add "## Human Verification" section with the verbatim response + timestamp; set Status: COMPLETE; append `VERIFIED: {YYYY-MM-DD HH:MM}`.
       - If `issues: ...` → append the reported issues to 14-04-SUMMARY.md under "## Human Verification → Issues", set Status: ISSUES, and escalate to the user to run /gsd:plan-phase 14 --gaps.
       - If `defer: ...` → set Status: DEFERRED with the reason; acceptable when the issue is cosmetic and does not violate D-01/D-02/D-03/D-04.
    5. Do NOT automate any of the visual checks — the whole point of this task is that a human eyeball is required.

    Do NOT modify any frontend/ source files in this task. If the user reports a BLOCKING issue, STOP and request a gap-closure plan — do not hotfix in a checkpoint task.
  </action>
  <verify>
    <automated>test -f .planning/phases/14-risk-radar-dashboard-neon-telemetry/14-04-SUMMARY.md && grep -q "## Human Verification" .planning/phases/14-risk-radar-dashboard-neon-telemetry/14-04-SUMMARY.md && grep -qE "Status: (COMPLETE|ISSUES|DEFERRED)" .planning/phases/14-risk-radar-dashboard-neon-telemetry/14-04-SUMMARY.md && echo "CHECKPOINT_RESOLVED"</automated>
  </verify>
  <done>
    User has inspected all four theme×mode combos, toggle persistence, and at least one drill-down. 14-04-SUMMARY.md has a "## Human Verification" section with the user's verbatim resume-signal response and a Status line (COMPLETE / ISSUES / DEFERRED). Phase 14 is either shipped or has a clear next step.
  </done>

</task>

</tasks>

<verification>
Plan-level checks:

1. Task 1 (automated) has written 14-04-SUMMARY.md with 14 rows, all PASS (or documented FAIL + fix).
2. Task 2 (checkpoint) has received user sign-off: "approved" OR user-reported issues addressed via gap-closure plan.
3. Git status shows 14-04-SUMMARY.md tracked and no unexpected changes to DashboardView.vue.
4. If user approves, Phase 14 is COMPLETE — append "status: complete" to 14-04-SUMMARY.md frontmatter before commit.
</verification>

<success_criteria>
- 14-04-SUMMARY.md exists with automated audit results
- User has signed off on all four theme×mode combos
- DashboardView.vue byte-exact unchanged since the start of Phase 14 (D-01 final lock)
- Toggle persists mode to `dashboard-mode-{userId}` localStorage key (D-02 final lock)
- Both Vuetify themes render Risk Radar with the correct palette (D-03, D-04 final lock)
- No regressions reported in classic DashboardView
</success_criteria>

<output>
After checkpoint resolution, update `.planning/phases/14-risk-radar-dashboard-neon-telemetry/14-04-SUMMARY.md`:
- Add "## Human Verification" section with user's resume-signal verbatim
- Add "## Status" with one of: COMPLETE / ISSUES (link to gap-closure plan) / DEFERRED (with reason)
- Append final line: `VERIFIED: {YYYY-MM-DD HH:MM}` if COMPLETE
</output>
