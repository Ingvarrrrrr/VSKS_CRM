---
phase: 15-reusable-purchase-items-editor
plan: 03
plan_id: 15-03
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - frontend/src/views/CreateOrderView.vue
files_created: []
autonomous: true
estimated_tokens: 35000
requirements:
  - ITEMS-EDITOR-06
requirements_addressed:
  - "ITEMS-EDITOR-06: CreateOrderView.vue mounts `<PurchaseItemsEditor v-model=\"items\" item-shape=\"purchase\" :purchase-id=\"purchaseId\">`, preserves Purchase-specific columns (`final_unit_price`, `final_total`, `feo_planned_item_id`) via the component's purchase branch, forwards `syncContractPriceIfSingle` through `@items-changed`, forwards `loadPurchase` through `@reload-requested`, and strips `_selectedProduct/_photo_url/_description/_description_44fz` at save. ~2000 lines of inline logic removed from CreateOrderView.vue."

must_haves:
  truths:
    - "CreateOrderView.vue contains a single `<PurchaseItemsEditor>` mount with `item-shape=\"purchase\"` and `v-model=\"items\"`"
    - "The inline items `<v-table>` block (former lines 290-436), full product dialog (former 1913-2018), and import dialog (former 2163-2308) are REMOVED from CreateOrderView.vue"
    - "The script block refs/methods extracted in Plan 15-01 (addItem, removeItem, calcItemTotal, productPickerDialog, productPickerResults, openProductPicker, selectFromPicker, createProductFromPicker, onItemProductSelect, productFilter, productItemsFor, hasProducts, clearItem, selectedItemIdxs, allItemsSelected, toggleSelectAll, toggleItemSelect, removeSelectedItems, fullProductDialog, fullProductSaving, fullProductIdx, fullProductPhotoFile, fullProductPhotoFileList, fullProductPhotoPreview, fullProductForm, fullProductNameSearch, fullProductNameSuggestions, isFullProductDuplicate, fullProductTypeOptions, fullProductCategoryOptions, fullAvgPrice, onFullPhotoFileChange, openFullProduct, saveFullProduct, itemsImportDialog, itemsImportFile, itemsImportLoading, itemsImportResult, importStep, importPreviewData, importSelectedSheet, dragMapping, ignoredColumns, dragOverTarget, importError, TARGET_FIELDS, autoDetectMapping, getColumnLabel, getSamples, isTargetFilled, onDragStart, onDropToTarget, onDropToUnresolved, onDragOverCol, onDragLeave, unmapTarget, ignoreColumn, doImportPreview, doMappedImport, smartImportFile, smartImportLoading, smartImportPreview, smartImportColumns, smartImportResult, columnFieldMapping, columnMappingApplied, showMappingPanel, CRM_MAPPING_FIELDS, doSmartPreview, doSmartImport) are DELETED from CreateOrderView.vue — they live in PurchaseItemsEditor.vue now"
    - "`items` ref stays in CreateOrderView.vue as the v-model source of truth (used by other CreateOrderView logic: displayNmck, saveMold, contract price sync, etc.)"
    - "The save path strips helper fields using `.map(({ _selectedProduct, _photo_url, _description, _description_44fz, ...rest }) => rest)` — preserve CreateOrderView:5441 pattern"
    - "syncContractPriceIfSingle still fires when items change — wired via `@items-changed=\"syncContractPriceIfSingle\"` on the component"
    - "loadPurchase() still fires when the component requests a reload after a pid-bound Excel import — wired via `@reload-requested=\"loadPurchase\"`"
    - "The FEO linkage cell (if currently rendered per row from CreateOrderView's `feoPlannedItemsBySubsidy` state) is injected via the `#row-extra` scoped slot provided by PurchaseItemsEditor — parent owns the FEO picker, child owns the table structure"
    - "`cd frontend && npm run build` exits 0"
    - "Opening an existing purchase and saving (no changes) produces an identical request payload to before this plan (smoke test in plan 15-05)"
  artifacts:
    - path: "frontend/src/views/CreateOrderView.vue"
      provides: "Purchase create/edit view, now mounts PurchaseItemsEditor instead of inlining the position editor"
      imports:
        - "PurchaseItemsEditor from '@/components/PurchaseItemsEditor.vue'"
      contains:
        - "<PurchaseItemsEditor"
        - "item-shape=\"purchase\""
        - "@items-changed=\"syncContractPriceIfSingle\""
        - "@reload-requested=\"loadPurchase\""
        - "_description_44fz"
      line_reduction: "≥ 1500 lines net smaller than pre-15-03 version"
  key_links:
    - from: "CreateOrderView.vue"
      to: "PurchaseItemsEditor.vue"
      via: "direct component mount + v-model=\"items\" + emit wiring"
      pattern: "<PurchaseItemsEditor"
---

<objective>
Replace ~2000 lines of inline position-editor logic in `frontend/src/views/CreateOrderView.vue` with a single mount of `<PurchaseItemsEditor>` (created in Plan 15-01). Preserve all existing Purchase-side behaviours: Purchase-only columns (`final_unit_price`, `final_total`, `feo_planned_item_id`), contract-price sync, purchase reload after pid-bound Excel import, helper-field stripping on save, NMCK display, budget check gate.
</objective>

<execution_context>
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-reusable-purchase-items-editor/15-CONTEXT.md
@.planning/phases/15-reusable-purchase-items-editor/15-RESEARCH.md
@.planning/phases/15-reusable-purchase-items-editor/15-01-PLAN.md
@.planning/phases/15-reusable-purchase-items-editor/15-01-SUMMARY.md
@CLAUDE.md
@frontend/src/views/CreateOrderView.vue
@frontend/src/components/PurchaseItemsEditor.vue
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove inline items table template, mount PurchaseItemsEditor, wire emits</name>
  <files>frontend/src/views/CreateOrderView.vue</files>
  <read_first>
    - frontend/src/components/PurchaseItemsEditor.vue (verify props + emits + slots API from Plan 15-01)
    - .planning/phases/15-reusable-purchase-items-editor/15-01-SUMMARY.md (handoff notes — read carefully)
    - frontend/src/views/CreateOrderView.vue lines 290-436 (inline items `<v-table>` block — TO BE REPLACED)
    - frontend/src/views/CreateOrderView.vue lines 5441-5470 (item save-time stripping — PRESERVE pattern)
    - .planning/phases/15-reusable-purchase-items-editor/15-RESEARCH.md section "Q1 — Reactive State to Extract" (list of refs/methods to DELETE)
  </read_first>
  <action>
    In CreateOrderView.vue, replace the inline items `<v-table>` block (lines 290-436 in current file; re-read for exact bounds before editing) with:

    ```vue
    <PurchaseItemsEditor
      v-model="items"
      item-shape="purchase"
      :purchase-id="purchaseId"
      :default-item-type="defaultItemType"
      :default-unit="'шт.'"
      :default-country="'Российская Федерация'"
      :allowed-item-types="['товар','услуга','работа']"
      :supports-excel-import="true"
      :supports-smart-import="true"
      :supports-full-product-dialog="true"
      :supports-photo-upload="true"
      :readonly="viewOnly"
      @items-changed="syncContractPriceIfSingle"
      @reload-requested="loadPurchase"
      @product-created="onProductCreatedFromEditor"
    >
      <template #row-extra="{ item, idx }">
        <!-- FEO planned item picker column — parent-owned because it needs feoPlannedItemsBySubsidy -->
        <v-autocomplete
          v-model="item.feo_planned_item_id"
          :items="feoPlannedItemsForRow(item)"
          item-title="display_name"
          item-value="id"
          density="compact"
          variant="outlined"
          hide-details
          clearable
          placeholder="—"
          style="min-width: 200px"
          @update:model-value="onFeoPlannedChange(idx, $event)"
        />
      </template>
      <template #toolbar-actions>
        <!-- any CreateOrderView-only actions (e.g. «Копировать НМЦК») stay here -->
      </template>
    </PurchaseItemsEditor>
    ```

    **FEO column decision procedure (MANDATORY — do not invent, but do not silently drop):**

    1. Before editing, grep CreateOrderView.vue for how FEO planned items are wired per row:
       ```bash
       grep -nE "feo_planned_item_id|feoPlannedItemsBySubsidy|feoPlannedItemsForRow|onFeoPlannedChange" frontend/src/views/CreateOrderView.vue | head -30
       ```
    2. Also grep the OLD `<v-table>` block region (lines 290-436) for any `<td>` / `<v-autocomplete>` that ties to `item.feo_planned_item_id`.
    3. Decision table:
       - **If the grep shows an inline per-row FEO picker inside the old items `<v-table>` block** → the `#row-extra` slot is REQUIRED. Wire it exactly as in the code block above. Verify the mount contains `<template #row-extra=`.
       - **If FEO is wired via `item.feo_planned_item_id` but NOT inside the old items table** (e.g. a separate FEO section above/below the table) → no `#row-extra` slot needed; FEO continues to live in its own section unchanged. Document this finding in 15-03-SUMMARY.md.
       - **If there is NO per-row FEO picker in current code at all** → no `#row-extra` slot needed. Document in SUMMARY.
    4. In all cases, `item.feo_planned_item_id` continues to be stored per-item (field exists on PurchaseItem). The component neither creates nor destroys this field — it flows through v-model.

    DO NOT skip the grep. DO NOT "verify before editing" without documenting the result — the SUMMARY must state which of the 3 decision branches was taken, with grep output.

    Then REMOVE the old `<v-table>` block entirely (lines 290-436 of current file).

    Add the import at the top of `<script setup>`:
    ```ts
    import PurchaseItemsEditor from '@/components/PurchaseItemsEditor.vue'
    ```

    Add a small handler:
    ```ts
    function onProductCreatedFromEditor(product: Product) {
      // mirror the existing behaviour when CreateOrderView creates a product inline:
      // push into local products list used by any parent-side displays (if applicable);
      // otherwise no-op — PurchaseItemsEditor manages its own products ref
    }
    ```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<PurchaseItemsEditor" frontend/src/views/CreateOrderView.vue` ≥ 1
    - `grep -c "item-shape=\"purchase\"" frontend/src/views/CreateOrderView.vue` ≥ 1
    - `grep -c "@items-changed=\"syncContractPriceIfSingle\"" frontend/src/views/CreateOrderView.vue` ≥ 1
    - `grep -c "@reload-requested=\"loadPurchase\"" frontend/src/views/CreateOrderView.vue` ≥ 1
    - `grep -c "import PurchaseItemsEditor" frontend/src/views/CreateOrderView.vue` ≥ 1
    - The old inline items block is gone: `grep -n "v-for=\"(item, idx) in items\"" frontend/src/views/CreateOrderView.vue` returns 0 matches in the original form-section region (verify by line count reduction below)
    - **FEO column decision documented** — 15-03-SUMMARY.md contains a "## FEO column wiring" section stating which of the 3 decision branches was taken, with grep output. If branch 1 taken (per-row FEO picker): `grep -c "#row-extra\|row-extra" frontend/src/views/CreateOrderView.vue` ≥ 1. If branch 2 or 3 taken: `grep -c "feo_planned_item_id" frontend/src/views/CreateOrderView.vue` ≥ 1 (field still referenced in form logic outside the items block).
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Items table is now a single component mount. Inline template removed. Type-check passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete extracted refs + methods from CreateOrderView.vue script block</name>
  <files>frontend/src/views/CreateOrderView.vue</files>
  <read_first>
    - frontend/src/views/CreateOrderView.vue (after Task 1)
    - frontend/src/components/PurchaseItemsEditor.vue (reference — anything here is DUPLICATE in CreateOrderView and must be removed)
    - .planning/phases/15-reusable-purchase-items-editor/15-RESEARCH.md section "Q1 — Reactive State to Extract" (definitive list)
  </read_first>
  <action>
    Delete the following refs, computeds, methods, and constants from CreateOrderView.vue's `<script setup>` block, using the line-range anchors below. Work top-to-bottom by region; for each deletion group, first `sed -n 'START,ENDp'` to confirm the region matches the expected code, then delete.

    **Line-range anchors (from RESEARCH Q1 — re-verify with `grep -n "<function name>"` before deleting since edits in Task 1 may have shifted line numbers by ~100):**

    ### Region A — Refs block (~lines 4120-4211 pre-Task-1 / shift by item-block removal)
    Anchor grep: `grep -n "const fullProductDialog\|const productPickerDialog\|const itemsImportDialog" frontend/src/views/CreateOrderView.vue`

    **Refs to DELETE:**
    - `productPickerDialog`, `productPickerSearch`, `productPickerIdx`, `productPickerResults` (~4929-4933)
    - `fullProductDialog`, `fullProductSaving`, `fullProductIdx`, `fullProductPhotoFile`, `fullProductPhotoFileList`, `fullProductPhotoPreview`, `fullProductForm` (~4120-4130)
    - `itemsImportDialog`, `itemsImportFile`, `itemsImportLoading`, `itemsImportResult`, `importStep`, `importPreviewData`, `importSelectedSheet`, `dragMapping`, `ignoredColumns`, `dragOverTarget`, `importError` (~4438-4545)
    - `smartImportFile`, `smartImportLoading`, `smartImportPreview`, `smartImportColumns`, `smartImportResult`, `columnFieldMapping`, `columnMappingApplied`, `showMappingPanel` (~4771-4820)
    - `selectedItemIdxs` (~4417 — moved to child)

    ### Region B — Computeds (~lines 4132-4161)
    Anchor grep: `grep -n "const fullProductNameSearch\|const fullAvgPrice\|const allItemsSelected\|const hasProducts" frontend/src/views/CreateOrderView.vue`

    **Computeds to DELETE:**
    - `fullProductNameSearch`, `fullProductNameSuggestions`, `isFullProductDuplicate`, `fullProductTypeOptions`, `fullProductCategoryOptions`, `fullAvgPrice` (~4132-4161)
    - `allItemsSelected`, `hasProducts` (~4432, end of items-methods region)

    ### Region C — Methods block (~lines 4404-4960)
    Anchor grep: `grep -n "function addItem\|function calcItemTotal\|function doImportPreview\|function doSmartImport" frontend/src/views/CreateOrderView.vue`

    **Methods to DELETE:**
    - `addItem`, `removeItem`, `clearItem`, `calcItemTotal` (~4407-4470)
    - `toggleSelectAll`, `toggleItemSelect`, `removeSelectedItems` (~4417-4436)
    - `openProductPicker`, `selectFromPicker`, `createProductFromPicker` (~4935-4950)
    - `onItemProductSelect`, `productFilter`, `productItemsFor` (~4869-4925)
    - `openFullProduct`, `saveFullProduct`, `onFullPhotoFileChange` (~4164-4211)
    - `doImportPreview`, `doMappedImport`, `autoDetectMapping`, `getColumnLabel`, `getSamples`, `isTargetFilled`, `onDragStart`, `onDropToTarget`, `onDropToUnresolved`, `onDragOverCol`, `onDragLeave`, `unmapTarget`, `ignoreColumn` (~4547-4770)
    - `doSmartPreview`, `doSmartImport` (~4821-4856)

    ### Region D — Constants (scope-sensitive — DO NOT blindly delete)
    Anchor grep: `grep -n "const UNIT_OPTIONS\|const COUNTRIES\|const TARGET_FIELDS\|const CRM_MAPPING_FIELDS" frontend/src/views/CreateOrderView.vue`

    **For EACH constant, run a usage check BEFORE deleting:**
    ```bash
    # Example for UNIT_OPTIONS:
    grep -n "UNIT_OPTIONS" frontend/src/views/CreateOrderView.vue | head
    ```
    - `UNIT_OPTIONS` — DELETE only if usage count (excluding the definition line) is 0. If it's still referenced (e.g. by a unit-related v-combobox outside the items table — common in CreateOrderView's delivery/location fields), KEEP it; PurchaseItemsEditor's own UNIT_OPTIONS is self-contained.
    - `COUNTRIES` — same rule. Often referenced by delivery address or contractor forms.
    - `TARGET_FIELDS` — DELETE unconditionally (import-dialog-only constant).
    - `CRM_MAPPING_FIELDS` — DELETE unconditionally (smart-import-only constant).

    ### Region E — Template dialog blocks (pre-Task-1 line numbers — already removed if Task 1 covered them; double-check)
    Anchor grep: `grep -n "v-model=\"fullProductDialog\"\|v-model=\"itemsImportDialog\"" frontend/src/views/CreateOrderView.vue`

    **Template blocks to DELETE (if not already removed in Task 1):**
    - `<v-dialog v-model="fullProductDialog" ...>` (former lines ~1913-2018)
    - `<v-dialog v-model="itemsImportDialog" ...>` (former lines ~2163-2308)
    - Any standalone smart-import dialog block

    ### Region F — Scoped CSS (imap-* classes)
    Anchor grep: `grep -n "\.imap-grid\|\.imap-col\|\.imap-card\|\.imap-unresolved" frontend/src/views/CreateOrderView.vue`

    **Delete all imap-* styles from CreateOrderView's `<style scoped>`** (former lines ~6040-6210). They already live in PurchaseItemsEditor.vue (per Plan 15-01 Task 1).

    **Keep:**
    - `items` ref — v-model source of truth, used by `displayNmck`, `doSave`, budget check, contract-price sync
    - `products` ref — if used elsewhere in CreateOrderView; otherwise delete. Verify via grep.
    - `syncContractPriceIfSingle` function — wired via emit, must remain
    - `loadPurchase` function — wired via emit, must remain
    - Save-path item stripping (approx line 5441):
      ```ts
      items: items.value.map(({ _selectedProduct, _photo_url, _description, _description_44fz, ...rest }) => rest),
      ```
      Verify `_description_44fz` is present in the destructuring per RESEARCH Q2.

    After deletion, the file should shrink by ≥1500 lines net. Verify: `wc -l frontend/src/views/CreateOrderView.vue` before and after, and log the delta in the SUMMARY.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -30</automated>
    <automated>cd frontend && npm run build 2>&1 | tail -10</automated>
    <automated>wc -l frontend/src/views/CreateOrderView.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function addItem\\|function removeItem\\|function calcItemTotal\\|function onItemProductSelect\\|function openProductPicker\\|function selectFromPicker\\|function openFullProduct\\|function saveFullProduct\\|function doImportPreview\\|function doMappedImport\\|function doSmartPreview\\|function doSmartImport\\|function autoDetectMapping" frontend/src/views/CreateOrderView.vue` returns 0
    - `grep -c "const fullProductDialog\\|const fullProductPhotoFile\\|const itemsImportDialog\\|const dragMapping\\|const smartImportFile\\|const TARGET_FIELDS\\|const CRM_MAPPING_FIELDS" frontend/src/views/CreateOrderView.vue` returns 0
    - `grep -c "<v-dialog v-model=\"fullProductDialog\"\\|<v-dialog v-model=\"itemsImportDialog\"" frontend/src/views/CreateOrderView.vue` returns 0
    - `grep -c "items.value.map(({ _selectedProduct, _photo_url, _description" frontend/src/views/CreateOrderView.vue` ≥ 1 (save-strip preserved)
    - `grep -c "_description_44fz" frontend/src/views/CreateOrderView.vue` ≥ 1 (added to stripper per RESEARCH Q2)
    - `wc -l frontend/src/views/CreateOrderView.vue` shows a file at least 1500 lines smaller than before this plan
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>
    CreateOrderView.vue is slim. All extracted state/methods removed. Parent-owned behaviours (syncContractPriceIfSingle, loadPurchase, save-path stripping) intact. Build + type-check green.
  </done>
</task>

<task type="checkpoint:manual" tdd="false">
  <name>Task 3: Manual smoke test — load existing purchase, verify items render, save round-trip is idempotent</name>
  <files></files>
  <read_first>
    - .planning/phases/15-reusable-purchase-items-editor/15-01-SUMMARY.md
    - .planning/phases/15-reusable-purchase-items-editor/15-RESEARCH.md section "Q13 — Risk Analysis"
  </read_first>
  <action>
    Start dev server (or reuse running one), open an existing purchase with multiple items in the browser, and verify by eyeball + network inspector:

    1. Page loads without console errors.
    2. Inline table renders all items with correct columns (Purchase shape): № / checkbox / Наименование / Тип / Кол-во / Ед.изм. / Цена ед. / Сумма / Страна / Итоговая цена ед. / ФЭО / delete.
    3. Photo tooltip still appears on hover (for items whose product has a photo).
    4. Click "Сохранить" with NO changes → network payload body includes the `items` array with the original items intact (log the payload).
    5. Edit quantity on row 1 → total_price recomputes → "Плановая сумма" (displayNmck) updates → contract-price sync fires if purchase is single-lot.
    6. Add a new row via "+ Добавить позицию" → opens the product picker → select a product → row fills with product_id, name, price, country.
    7. Open "+ Создать полную карточку" → upload a photo → save → row gets the photo thumb.
    8. Open "Импорт из файла" → upload a 3-row .xlsx → Step 2 drag-map → "Применить" → rows appear, network shows POST to `/api/purchases/{pid}/items/import-mapped`.

    This is a checkpoint: executor must paste the network payload for step 4 into the SUMMARY and confirm it matches the pre-refactor shape field-by-field.

    If any step fails, report in SUMMARY with console logs + network screenshots and DO NOT mark plan complete — trigger revision loop.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -5 ; cd frontend && npm run build 2>&1 | tail -5</automated>
    <manual>Browser smoke — 8 steps above. Document pass/fail per step in 15-03-SUMMARY.md</manual>
  </verify>
  <acceptance_criteria>
    - All 8 smoke steps pass and are documented in SUMMARY
    - Save-path network payload for unmodified purchase matches pre-refactor shape (diffable JSON paste in SUMMARY)
    - Zero console errors on page load
  </acceptance_criteria>
  <done>
    CreateOrderView wired end-to-end. No behavioural regression. Ready for Plan 15-04 (WishesView wiring) and Plan 15-05 (E2E + UAT).
  </done>
</task>

</tasks>

<verification>
Plan-level:
1. `cd frontend && npm run build` exits 0
2. `cd frontend && npx tsc --noEmit` exits 0
3. `grep -c "<PurchaseItemsEditor" frontend/src/views/CreateOrderView.vue` ≥ 1
4. `grep -c "function addItem\\|function doMappedImport\\|function saveFullProduct" frontend/src/views/CreateOrderView.vue` returns 0
5. `wc -l frontend/src/views/CreateOrderView.vue` delta ≥ 1500 smaller than pre-plan
6. Manual smoke: 8-step checklist documented in SUMMARY with pass/fail per step
</verification>

<success_criteria>
- PurchaseItemsEditor mounted in CreateOrderView with correct props + emit wiring
- All extracted state/methods/dialogs deleted from CreateOrderView.vue
- syncContractPriceIfSingle + loadPurchase wired via emits (not direct calls)
- Save path preserves `_description_44fz` in helper-field stripping
- FEO linkage cell (if it exists) wired via `#row-extra` slot
- Manual smoke passes all 8 steps
- Git commit: `refactor(15-03): mount PurchaseItemsEditor in CreateOrderView, remove ~2000 lines of inline logic`
</success_criteria>

<output>
Create `.planning/phases/15-reusable-purchase-items-editor/15-03-SUMMARY.md`:
- Line count delta (before vs after)
- List of deleted refs/methods/dialogs
- Pre/post JSON payload comparison for the save-path idempotency check
- 8-step manual smoke checklist with pass/fail per step
- Any quirks discovered during wiring (e.g. template refs that needed relocation)
</output>
