---
phase: 09-vnutrenniy-chat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/chat_room.py
  - backend/app/models/chat_message.py
  - backend/app/models/__init__.py
autonomous: true
requirements: [CHAT-01, CHAT-02, CHAT-05, CHAT-10]

must_haves:
  truths:
    - "Таблицы chat_rooms, chat_participants, chat_messages, message_reads существуют в БД после рестарта контейнера"
    - "ChatRoom с is_group=False создаётся для 1-on-1 чата"
    - "ChatRoom с is_group=True создаётся для группового чата с name"
    - "Поле org_id присутствует в chat_rooms — изоляция между организациями"
    - "message_reads содержит уникальный индекс (room_id, user_id) — для эффективного подсчёта непрочитанных"
  artifacts:
    - path: "backend/app/models/chat_room.py"
      provides: "ChatRoom, ChatParticipant SQLAlchemy models"
      exports: ["ChatRoom", "ChatParticipant"]
    - path: "backend/app/models/chat_message.py"
      provides: "ChatMessage, MessageRead SQLAlchemy models"
      exports: ["ChatMessage", "MessageRead"]
  key_links:
    - from: "backend/app/models/chat_room.py"
      to: "backend/app/models/user.py"
      via: "ForeignKey users.id"
      pattern: "ForeignKey.*users.id"
    - from: "backend/app/models/chat_message.py"
      to: "backend/app/models/chat_room.py"
      via: "ForeignKey chat_rooms.id"
      pattern: "ForeignKey.*chat_rooms.id"
---

<objective>
Создать SQLAlchemy-модели для чата: ChatRoom, ChatParticipant, ChatMessage, MessageRead.

Purpose: Без моделей невозможны ни REST API, ни WebSocket. Это фундамент всей фазы.
Output: 2 файла моделей, зарегистрированных в __init__.py; таблицы создаются автоматически при старте FastAPI через create_all.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-vnutrenniy-chat/09-RESEARCH.md

<interfaces>
<!-- Существующие паттерны проекта для следования -->

From backend/app/models/user.py:
```python
class User(Base):
    __tablename__ = "users"
    id = Column(Integer, primary_key=True, index=True)
    username = Column(String(100), unique=True, nullable=False)
    role = Column(String(20), nullable=False, default="employee")
    full_name = Column(String(255))
    org_id = Column(Integer, ForeignKey("organizations.id", ondelete="SET NULL"), nullable=True)
```

From backend/app/models/purchase_file.py (file storage pattern):
```python
# Project stores files on filesystem at /app/uploads, NOT bytea
# Chat files go to /app/uploads/chat/{room_id}/
```

From backend/app/database.py (Base import):
```python
from app.database import Base
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ChatRoom and ChatParticipant models</name>
  <files>backend/app/models/chat_room.py</files>
  <action>
Create `backend/app/models/chat_room.py` with two SQLAlchemy models:

```python
from sqlalchemy import Column, Integer, String, Boolean, ForeignKey, DateTime, UniqueConstraint, Index
from sqlalchemy.orm import relationship
from sqlalchemy.sql import func
from app.database import Base


class ChatRoom(Base):
    __tablename__ = "chat_rooms"

    id = Column(Integer, primary_key=True, index=True)
    name = Column(String(255), nullable=True)         # NULL for 1-on-1 (name derived from participants)
    is_group = Column(Boolean, default=False, nullable=False)
    created_by = Column(Integer, ForeignKey("users.id", ondelete="SET NULL"), nullable=True)
    org_id = Column(Integer, ForeignKey("organizations.id", ondelete="CASCADE"), nullable=False)
    created_at = Column(DateTime(timezone=True), server_default=func.now(), nullable=False)

    participants = relationship("ChatParticipant", back_populates="room", cascade="all, delete-orphan")
    messages = relationship("ChatMessage", back_populates="room", cascade="all, delete-orphan")


class ChatParticipant(Base):
    __tablename__ = "chat_participants"

    id = Column(Integer, primary_key=True, index=True)
    room_id = Column(Integer, ForeignKey("chat_rooms.id", ondelete="CASCADE"), nullable=False)
    user_id = Column(Integer, ForeignKey("users.id", ondelete="CASCADE"), nullable=False)
    joined_at = Column(DateTime(timezone=True), server_default=func.now(), nullable=False)

    room = relationship("ChatRoom", back_populates="participants")

    __table_args__ = (
        UniqueConstraint("room_id", "user_id", name="uq_chat_participant"),
        Index("ix_chat_participants_user_id", "user_id"),
        Index("ix_chat_participants_room_id", "room_id"),
    )
```
  </action>
  <verify>
    <automated>cd /c/Users/1/Desktop/Cursor/VSKS_CRM && python -c "from backend.app.models.chat_room import ChatRoom, ChatParticipant; print('OK')" 2>/dev/null || docker exec vsks_crm_backend python -c "from app.models.chat_room import ChatRoom, ChatParticipant; print('ChatRoom OK')"</automated>
  </verify>
  <done>Файл существует; ChatRoom и ChatParticipant импортируются без ошибок; поля id, name, is_group, created_by, org_id, created_at присутствуют в ChatRoom; UniqueConstraint на (room_id, user_id) в ChatParticipant.</done>
</task>

<task type="auto">
  <name>Task 2: Create ChatMessage and MessageRead models</name>
  <files>backend/app/models/chat_message.py</files>
  <action>
Create `backend/app/models/chat_message.py`:

```python
from sqlalchemy import Column, Integer, String, Text, ForeignKey, DateTime, Index, UniqueConstraint
from sqlalchemy.orm import relationship
from sqlalchemy.sql import func
from app.database import Base


class ChatMessage(Base):
    __tablename__ = "chat_messages"

    id = Column(Integer, primary_key=True, index=True)
    room_id = Column(Integer, ForeignKey("chat_rooms.id", ondelete="CASCADE"), nullable=False)
    sender_id = Column(Integer, ForeignKey("users.id", ondelete="SET NULL"), nullable=True)
    content = Column(Text, nullable=True)         # NULL if file-only message
    file_path = Column(String(1000), nullable=True)  # NULL if text-only; stored at /app/uploads/chat/{room_id}/
    file_name = Column(String(255), nullable=True)
    file_mime = Column(String(100), nullable=True)
    file_size = Column(Integer, nullable=True)
    created_at = Column(DateTime(timezone=True), server_default=func.now(), nullable=False)

    room = relationship("ChatRoom", back_populates="messages")

    __table_args__ = (
        Index("ix_chat_messages_room_id_desc", "room_id", "id"),  # keyset pagination
    )


class MessageRead(Base):
    __tablename__ = "message_reads"

    id = Column(Integer, primary_key=True, index=True)
    room_id = Column(Integer, ForeignKey("chat_rooms.id", ondelete="CASCADE"), nullable=False)
    user_id = Column(Integer, ForeignKey("users.id", ondelete="CASCADE"), nullable=False)
    last_read_message_id = Column(Integer, ForeignKey("chat_messages.id", ondelete="SET NULL"), nullable=True)

    __table_args__ = (
        UniqueConstraint("room_id", "user_id", name="uq_message_read"),
        Index("ix_message_reads_user_id", "user_id"),
    )
```
  </action>
  <verify>
    <automated>docker exec vsks_crm_backend python -c "from app.models.chat_message import ChatMessage, MessageRead; print('ChatMessage OK')" 2>/dev/null || echo "Will verify after container rebuild"</automated>
  </verify>
  <done>Файл существует; ChatMessage содержит поля id, room_id, sender_id, content, file_path, file_name, file_mime, file_size, created_at; MessageRead содержит UniqueConstraint на (room_id, user_id); составной индекс на (room_id, id) для keyset pagination.</done>
</task>

<task type="auto">
  <name>Task 3: Register models in __init__.py and main app</name>
  <files>backend/app/models/__init__.py, backend/app/__init__.py</files>
  <action>
1. Открыть `backend/app/models/__init__.py`. Добавить импорты новых моделей в конец файла (после существующих):

```python
from .chat_room import ChatRoom, ChatParticipant   # noqa: F401
from .chat_message import ChatMessage, MessageRead  # noqa: F401
```

2. Открыть `backend/app/__init__.py`. Найти блок с импортами моделей (`.models import platform_publication` и похожие). Добавить после них:

```python
from .models.chat_room import ChatRoom, ChatParticipant     # ensure chat_rooms, chat_participants tables are created
from .models.chat_message import ChatMessage, MessageRead   # ensure chat_messages, message_reads tables are created
```

Это гарантирует что SQLAlchemy зарегистрирует таблицы при старте и `create_all` их создаст.

ВАЖНО: Не трогать существующие импорты в __init__.py — только добавлять.
  </action>
  <verify>
    <automated>grep -n "chat_room\|chat_message" /c/Users/1/Desktop/Cursor/VSKS_CRM/backend/app/__init__.py</automated>
  </verify>
  <done>Оба файла содержат импорты ChatRoom, ChatParticipant, ChatMessage, MessageRead; grep подтверждает наличие строк с "chat_room" и "chat_message" в backend/app/__init__.py.</done>
</task>

</tasks>

<verification>
После выполнения всех задач:
1. `grep -r "ChatRoom\|ChatMessage" backend/app/models/__init__.py backend/app/__init__.py` — обе строки присутствуют
2. Все 4 класса модели имеют корректные ForeignKey-связи с `users` и `organizations`
3. `message_reads` имеет UniqueConstraint (room_id, user_id) — критично для upsert при mark-as-read
4. `chat_messages` имеет составной индекс (room_id, id) — критично для keyset pagination
</verification>

<success_criteria>
- 2 файла моделей созданы: backend/app/models/chat_room.py, backend/app/models/chat_message.py
- Модели зарегистрированы в backend/app/__init__.py
- Все 4 таблицы создаются при `create_all` (проверяется после сборки Docker образа)
- org_id присутствует в chat_rooms — изоляция по организации
- UniqueConstraint и индексы добавлены согласно схеме из RESEARCH.md
</success_criteria>

<output>
После завершения создать `.planning/phases/09-vnutrenniy-chat/09-01-SUMMARY.md`
</output>
