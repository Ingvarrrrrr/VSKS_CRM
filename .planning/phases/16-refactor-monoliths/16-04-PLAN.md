---
phase: 16-refactor-monoliths
plan: 04
type: execute
wave: 1
depends_on: [16-01]
files_modified:
  - backend/app/routers/purchase_budget.py
  - backend/app/routers/purchases.py
autonomous: true
requirements: [REFACTOR-01, REFACTOR-04, REFACTOR-08]
must_haves:
  truths:
    - "_check_budget is importable from app.routers.purchase_budget"
    - "_assign_framework_seq is importable from app.routers.purchase_budget"
    - "FEO cap validation helpers live in purchase_budget.py"
    - "purchases.py no longer contains _check_budget or _assign_framework_seq function bodies"
    - "POST /api/purchases/ over-budget still returns 422 (behaviour preserved)"
  artifacts:
    - path: "backend/app/routers/purchase_budget.py"
      provides: "Helper-only module (no router): _check_budget + _assign_framework_seq + FEO cap helpers + FRAMEWORK_TYPES"
      min_lines: 100
    - path: "backend/app/routers/purchases.py"
      provides: "No longer contains def _check_budget or def _assign_framework_seq body"
  key_links:
    - from: "backend/app/routers/purchases.py"
      to: "backend/app/routers/purchase_budget.py"
      via: "imports _check_budget, _assign_framework_seq, FRAMEWORK_TYPES"
      pattern: "from app.routers.purchase_budget import"
---

<objective>
Extract budget-validation helpers (`_check_budget`, `_assign_framework_seq`, FEO cap helpers, `FRAMEWORK_TYPES`) into `purchase_budget.py` per RESEARCH.md §1.1 extraction step 3. Helper-only module — NO router, NO endpoints. purchases.py CRUD and (later) purchase_transitions.py both import from this module. Implements D-05 + D-19 per CONTEXT.md.
</objective>

<execution_context>
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/1/Desktop/Cursor/VSKS_CRM/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-refactor-monoliths/16-CONTEXT.md
@.planning/phases/16-refactor-monoliths/16-RESEARCH.md
@.planning/phases/16-refactor-monoliths/16-VALIDATION.md

<interfaces>
<!-- Helper-only module. No APIRouter instance. No include_router call. -->
<!-- Current location in purchases.py: FRAMEWORK_TYPES at L324, _check_budget + _assign_framework_seq somewhere below. -->
<!-- Downstream consumers: purchases.py CRUD (create/update), purchase_transitions.py (Plan 16-06). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create purchase_budget.py helper module</name>
  <read_first>
    - backend/app/routers/purchases.py (grep for `def _check_budget`, `def _assign_framework_seq`, `FRAMEWORK_TYPES`, any FEO cap helpers — note exact function bodies + imports they need)
    - .planning/phases/16-refactor-monoliths/16-RESEARCH.md §1.1 step 3, §2.3 underscore convention, §G-08 (FRAMEWORK_TYPES scope)
    - .planning/phases/16-refactor-monoliths/16-CONTEXT.md D-05, D-19
  </read_first>
  <files>backend/app/routers/purchase_budget.py</files>
  <action>
    1. Create `backend/app/routers/purchase_budget.py` starting with imports needed by the helpers (read purchases.py to confirm the exact set — likely includes):
       ```python
       from fastapi import HTTPException
       from sqlalchemy import select, func
       from sqlalchemy.ext.asyncio import AsyncSession
       from typing import Optional

       from app.models.purchase import Purchase
       from app.models.subsidy import Subsidy
       ```
       **No `APIRouter` import, no `router = APIRouter(...)` line. This is a pure helper module.**
    2. MOVE (cut from purchases.py, paste into purchase_budget.py) these identifiers:
       - Constant `FRAMEWORK_TYPES` (purchases.py L324)
       - Function `_check_budget` (keep underscore prefix per §2.3)
       - Function `_assign_framework_seq`
       - Any FEO cap validation helpers (grep purchases.py for `feo_cap`, `FEO_CAP`, `feo_limit` — move if self-contained)
    3. Preserve every function signature and body BYTE-FOR-BYTE. No refactoring of logic.
    4. If a helper calls `_purchase_to_full` or `_item_to_out`, do NOT import those here — they stay in purchases.py (D-20). Instead, call sites in purchases.py keep using them locally; budget helpers should not need them.
  </action>
  <verify>
    <automated>test -f backend/app/routers/purchase_budget.py && grep -q "def _check_budget" backend/app/routers/purchase_budget.py && grep -q "def _assign_framework_seq" backend/app/routers/purchase_budget.py && grep -q "FRAMEWORK_TYPES" backend/app/routers/purchase_budget.py && ! grep -q "APIRouter" backend/app/routers/purchase_budget.py</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/routers/purchase_budget.py` exists
    - Contains `def _check_budget(`
    - Contains `def _assign_framework_seq(`
    - Contains `FRAMEWORK_TYPES = `
    - Does NOT contain `APIRouter` (helper-only — verify: `grep -c "APIRouter" backend/app/routers/purchase_budget.py` returns 0)
    - Does NOT contain `@router.` decorator (`grep -c "@router\." backend/app/routers/purchase_budget.py` returns 0)
    - `wc -l backend/app/routers/purchase_budget.py` ≥ 100 and ≤ 250
  </acceptance_criteria>
  <done>
    purchase_budget.py is a clean helper module with budget-validation logic, no endpoints.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove moved helpers from purchases.py and import them back</name>
  <read_first>
    - backend/app/routers/purchases.py (current state — locate callers of _check_budget, _assign_framework_seq, FRAMEWORK_TYPES)
    - backend/app/routers/purchase_budget.py (confirm what was moved)
  </read_first>
  <files>backend/app/routers/purchases.py</files>
  <action>
    1. Delete from purchases.py every function body + constant moved in Task 1:
       - Delete `def _check_budget(...)` body
       - Delete `def _assign_framework_seq(...)` body
       - Delete `FRAMEWORK_TYPES = {...}` constant line
       - Delete any FEO cap helpers moved
    2. Add import near top of purchases.py (after existing `from app.*` imports, before `router = APIRouter(...)` at L144):
       ```python
       from app.routers.purchase_budget import _check_budget, _assign_framework_seq, FRAMEWORK_TYPES
       ```
       If FEO helpers were also moved, include them in this import line.
    3. Grep external files for cross-references (must be zero outside purchase_budget.py):
       - `grep -rn "from app.routers.purchases import _check_budget" backend/` — update any hits to `from app.routers.purchase_budget import _check_budget`
       - Same for `_assign_framework_seq`, `FRAMEWORK_TYPES`
    4. Do NOT remove `STATUS_ORDER`, `TRANSITION_REQUIRED`, `_purchase_to_full`, `_item_to_out` — those stay in purchases.py per G-08 + D-20.
  </action>
  <verify>
    <automated>! grep -nE "^def _check_budget|^def _assign_framework_seq|^FRAMEWORK_TYPES = " backend/app/routers/purchases.py && grep -q "from app.routers.purchase_budget import" backend/app/routers/purchases.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^def _check_budget" backend/app/routers/purchases.py` returns 0
    - `grep -cE "^def _assign_framework_seq" backend/app/routers/purchases.py` returns 0
    - `grep -cE "^FRAMEWORK_TYPES = " backend/app/routers/purchases.py` returns 0
    - `grep -c "from app.routers.purchase_budget import" backend/app/routers/purchases.py` returns 1
    - `grep -rn "from app.routers.purchases import _check_budget\|_assign_framework_seq" backend/app/` returns 0 hits
    - `wc -l backend/app/routers/purchases.py` ≤ 2100 (was ~2200 after Plan 16-03; budget is ~150 lines)
  </acceptance_criteria>
  <done>
    purchases.py imports budget helpers from purchase_budget.py instead of defining them locally.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify build + smoke test + commit atomic extract</name>
  <read_first>
    - backend/tests/test_routers_mounted.py (from Plan 16-01)
  </read_first>
  <files>backend/app/routers/purchase_budget.py, backend/app/routers/purchases.py</files>
  <action>
    1. Verify Python-level import (no circular import): `docker exec vsks_crm-backend-1 python -c "from app.routers.purchase_budget import _check_budget, _assign_framework_seq, FRAMEWORK_TYPES; print('ok')"` → prints `ok`.
    2. Rebuild backend: `docker compose build backend` exits 0.
    3. Run router mount smoke: `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` → all tests still pass (no new router added, existing mounts unchanged).
    4. Run E2E fast subset: `BASE_URL=http://85.239.53.155 npx playwright test e2e/05-orders.spec.ts e2e/02-all-pages.spec.ts` → pass (create/update/transition flows still enforce budget).
    5. Commit: `refactor(16-04): extract purchase_budget from purchases` — files: `backend/app/routers/purchase_budget.py` (new), `backend/app/routers/purchases.py` (modified). NO change to `backend/app/__init__.py` (helper module, no router).
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -qE "refactor\(16-04\): extract purchase_budget" && docker exec vsks_crm-backend-1 python -c "from app.routers.purchase_budget import _check_budget, _assign_framework_seq, FRAMEWORK_TYPES; print('ok')" | grep -q ok</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1` matches `^[a-f0-9]+ refactor\(16-04\): extract purchase_budget from purchases`
    - Commit diff shows exactly 2 files changed (purchase_budget.py new, purchases.py modified)
    - `backend/app/__init__.py` is NOT in commit diff
    - `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` exits 0
    - `BASE_URL=http://85.239.53.155 npx playwright test e2e/05-orders.spec.ts` exits 0
  </acceptance_criteria>
  <done>
    Atomic commit landed. Budget helpers extracted. Build green. E2E green. No URL change.
  </done>
</task>

</tasks>

<verification>
- `docker exec vsks_crm-backend-1 pytest /app/tests/test_routers_mounted.py -q` → all pass
- `BASE_URL=http://85.239.53.155 npx playwright test e2e/05-orders.spec.ts e2e/02-all-pages.spec.ts` → all pass
- `wc -l backend/app/routers/purchase_budget.py` ≥ 100 AND ≤ 250
- No circular import: `docker exec vsks_crm-backend-1 python -c "import app"` exits 0
</verification>

<success_criteria>
Budget helpers in own module. purchases.py smaller by ~150 lines. Zero behaviour change. Commit: `refactor(16-04): extract purchase_budget from purchases`.
</success_criteria>

<output>
After completion, create `.planning/phases/16-refactor-monoliths/16-04-SUMMARY.md`.
</output>
